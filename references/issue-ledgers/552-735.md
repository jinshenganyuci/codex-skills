# KernelSU closed-issue ledger — JSONL 552–735

口径：每条均按题干、`stateReason`、关联 PR 和存档评论归纳。`COMPLETED` 仅表示 Issue 被关闭；没有可核对的维护者结论、变更或测试确认时，明确写为“未证实关闭”。这里的“教训”面向 KernelSU 模块、WebUI 和设备集成开发，不能把临时恢复命令或社区猜测误写成稳定 API。

## 552–600

- #1360 | 问：给 SystemUI 授予 root 后，Chrome/Kiwi 等同 UID 组件网页空白；能否只授权 SystemUI。 | 解决：NOT_PLANNED，仅引用 #1095；共享 UID 不能按单个包隔离，未有修复。 | 教训：不要将共享 system UID 当成普通 app 做细粒度授权；模块内调用 SystemUI/root helper 前要评估同 UID 进程的连带影响。
- #1361 | 问：请求 ARMv7（32 位）Manager 支持。 | 解决：NOT_PLANNED，维护者指向 #1193，用户接受；无实现证据。 | 教训：模块 APK/二进制需声明 ABI；不能假定 KSU Manager 会在所有 32 位设备上提供运行环境。
- #1365 | 问：浅 clone 让 `git describe` 生成错误 KernelSU 版本号，建议 Makefile 自动 unshallow。 | 解决：COMPLETED，但无 PR、维护者回复或测试回报，未证实具体实现。 | 教训：内核/模块构建须显式处理浅 clone 或使用可复现版本注入；不要把 Git 历史深度当成必然存在。
- #1366 | 问：能否把重新打包内核改为手机端内核注入。 | 解决：NOT_PLANNED，机器人指向 #1254，无新方案。 | 教训：不要把另一 root 方案的注入设计假定为 KSU 支持的安装路径；遵循官方 GKI/LKM/内核集成流程。
- #1367 | 问：卸载大模块后能否自动缩小模块镜像。 | 解决：讨论说明无法预知会自解压或联网下载的模块最终空间；新 CI/重建流程被认为可改善，但无关联 PR，未证实为稳定的自动缩小功能。 | 教训：动态内容模块不能依赖预估镜像容量；对备份/收缩只使用实际提供的 `ksud module shrink` 或 Manager 设置，并说明后续安装可能再扩大。
- #1368 | 问：iQOO 上 AnyKernel3 刷 0.7.6 没有写入 boot。 | 解决：NOT_PLANNED；更换可刷 AK3 包中的 Image 后仍正常，提问者确认不是 KSU，而是刷入脚本/流程问题。 | 教训：把“镜像未写入”与“内核启动失败”分开验证，核对分区 hash；模块开发不能把 AK3 脚本故障归因于 KSU。
- #1369 | 问：不安装 Manager 时如何默认给 shell root。 | 解决：NOT_PLANNED，维护者答“自己改代码”。 | 教训：无公开的“默认授权 shell”开关；不要让模块直接改 allowlist 或伪造授权状态。
- #1370 | 问：请求随机化 Manager 包名以应对银行 app 检测。 | 解决：NOT_PLANNED，维护者要求检索已关闭的同类 issue。 | 教训：不把改包名、重签或规避环境检测写为模块功能；Manager 可在不需要时卸载/冻结，但不承诺绕过检测。
- #1373 | 问：4.14 上 app 已获 root 但 `mount` 失败。 | 解决：NOT_PLANNED；没有日志且未按 bug 模板提交，维护者要求按模板重开。 | 教训：模块挂载失败必须提交 `mountinfo`、命令、日志和设备/内核信息；“已授权”不证明 mount namespace 或 SELinux 正常。
- #1375 | 问：Manager 白屏闪退、模块仍在工作。 | 解决：维护者让删除字体模块，提问者确认有效（COMPLETED）。 | 教训：Manager/UI 异常先禁用字体、主题、注入等模块做控制变量；模块不要全局篡改资源/字体而缺少回滚。
- #1379 | 问：升级到新的模块镜像后卡启动、镜像显示 1T。 | 解决：NOT_PLANNED；日志版本与描述不符，维护者认为无 KSU 证据，建议在恢复环境删除 `modules.img` 和 `modules_update.img` 后重建。 | 教训：1T 是稀疏文件逻辑大小，不等于物理占用；旧→新镜像迁移被中断后又降级会有风险，恢复删除镜像仅应作为有备份的故障处理。
- #1380 | 问：模块放入 `vendor/lib/modules` 后覆盖整个目录。 | 解决：维护者确认 `/vendor/lib/modules` 是指向 `/vendor_dlkm/lib/modules` 的符号链接，KSU 当时不支持直接挂载 vendor_dlkm；可在 `post-fs-data.sh` 自行处理 overlay。 | 教训：模块不能假设 `/vendor` 是普通合并目录；先解析符号链接，必要时在早期开机脚本显式挂载，并评估厂商分区兼容性。
- #1381 | 问：0.8.0 的 sparse modules 镜像操作后疑似持续占空间。 | 解决：维护者定位为旧格式迁移/镜像 journal 问题，给出测试 CI；报告者用旧 Manager 建模块再升级测试，实际占用稳定在 13 MB，确认该 CI 修复有效。 | 教训：镜像格式迁移必须完成且不要中途切回旧 Manager；模块安装器应等待迁移结束，出现异常优先完整重建受影响镜像并验证 `du` 实际大小。
- #1382 | 问：Manager WebView 不能访问 HTTP 本地 API。 | 解决：明文 HTTP 默认禁用是有意安全策略；可用 `127.0.0.1`，若有合理需求可改 network security config 提 PR，不能泛开放明文网络。 | 教训：WebUI 用 HTTPS 或 loopback `127.0.0.1`；HTTPS 页面向 HTTP 请求还会触发 mixed-content 限制，不能把它误诊为模块命令错误。
- #1383 | 问：请求 Android Emulator 支持。 | 解决：关联 PR #1405（`ci: support android 14 AVD`）已合并；同时维护者说明可自行编译开源模拟器内核。 | 教训：模拟器支持以目标 AVD 内核源码、架构和 CI 产物为准；模块测试不能把手机 GKI boot 镜像直接套给 AVD。
- #1384 | 问：`modules.img` 实际占用不断增长。 | 解决：维护者定位旧镜像扩容产生过大的 journal，建议重新设置 journal 或删除 `modules.img` 后装模块重建；报告者复测新 CI 后实际占用稳定，确认修复。 | 教训：迁移问题优先用可验证的新镜像重建；不要把 `ls` 的 1T 逻辑长度当作磁盘写满，使用 `du` 和安装/重启后的实际占用判断。
- #1386 | 问：Modules umount by default 会怎样影响应用。 | 解决：COMPLETED；维护者说明若 app 需要读取模块挂载文件，应全局关闭该选项或为特定 app 关闭。 | 教训：模块若要让普通 app/服务读取其挂载文件，必须文档化 umount 要求；用 `/proc/<pid>/mountinfo` 验证 app 所在 namespace，而非只看 root shell。
- #1389 | 问：Samsung 的恢复环境把稀疏 1T 镜像算成异常存储，无法备份。 | 解决：NOT_PLANNED；维护者说明 KSU 镜像本来就是 sparse，1T 是逻辑长度，OEM/TWRP 统计不正确；备份前可 root 下 `ksud module shrink`，但后续模块变更会重新扩大。 | 教训：模块发布须警告稀疏镜像与 recovery/OEM 存储统计兼容性；收缩后动态写入模块可能失败，备份前做完整实机验证。
- #1390 | 问：把 ksud 移进内核以减少版本不匹配。 | 解决：NOT_PLANNED；没有人接手相关 #130/PR，维护者说明关掉即无人实现。 | 教训：ksud 是用户态接口，不要依赖“内置到内核”的未来设计；模块调用只使用当前公开 CLI/API。
- #1396 | 问：app 内 `mount -o bind` 失败。 | 解决：维护者建议 `su -mm` 切换全局 mount namespace；随后要求用 Termux 对比，若 Termux 可用则是 app 自己的 shell/代码问题，未形成通用内核修复。 | 教训：模块/辅助 app 需要正确取得 global mount namespace，并分别测试 shell、Java `exec` 和 SELinux；不要只在 app 内裸写 `su -mm` 后假设后续命令继承。
- #1397 | 问：升级 0.8.1 后不能装/更新模块，迁移报 `os error 2`。 | 解决：发现已装模块目录含 `REMOVE` 或 `mknod` 创建文件时会触发，先禁用该模块即可继续更新；无关联 PR。 | 教训：模块安装脚本避免留下不兼容的特殊节点/控制文件；升级失败时先定位并禁用可疑模块，再做镜像重建，而不是盲目反复刷内核。
- #1398 | 问：更新后被 Momo/Remote Play 判 root。 | 解决：NOT_PLANNED；提问者最终确认是 hosts 模块，不是 KSU。 | 教训：root/完整性检测回归要逐个卸载模块做二分，不应仅因更新 KSU 的时间相关性就归因于核心。
- #1399 | 问：题干只有“ker”。 | 解决：COMPLETED，无评论、PR 或可分析事实，未证实关闭。 | 教训：无最小复现、日志、设备信息的报告不能转化为模块兼容性结论。
- #1401 | 问：0.8.1 后系统存储持续变大。 | 解决：维护者先建议删除 KSU 镜像；最终报告者确认是自己的 AnyKernel3 仓库未更新，更新后正常（COMPLETED）。 | 教训：系统存储/启动问题要同时核对 KSU、Manager、AK3 安装器和模块版本；设备集成者应持续同步刷机脚本。
- #1403 | 问：自管理模块的 app 能否不使用镜像/ZIP、直接操作模块目录。 | 解决：维护者明确不保证内部目录/稀疏镜像行为；app 应生成模块 ZIP 再调用 `ksud module install <ZIP>`，也可用公开 `ksud debug xcp`，不能依赖 `/data/adb/modules` 黑客式写入。 | 教训：只依赖公开模块安装与复制接口；不要硬编码 modules 路径或直接篡改镜像，框架演进（如 ksufs）时内部行为可变。
- #1404 | 问：请求 arm32 支持。 | 解决：NOT_PLANNED，指向 #701。 | 教训：同 #1361，模块原生二进制要做 ABI 拒绝/降级，而不是假定 ARM32 Manager/ksud 可用。
- #1406 | 问：某 Magisk 音频模块更新依赖 Magisk mirror，KSU 能否兼容。 | 解决：NOT_PLANNED；维护者说明 KSU 使用 overlayfs，没有也不能提供该 mirror。 | 教训：移植 Magisk 模块不要依赖其 mirror filesystem；改为 KSU 的模块安装、overlay 和脚本生命周期。
- #1408 | 问：`ksud sepolicy check` 怎样检查某条规则已加载。 | 解决：维护者说明该命令只校验表达式语法合法性，不检查策略中是否存在规则。 | 教训：模块 `sepolicy.rule` 调试要区分“规则语法正确”和“规则已加载/生效”；用 AVC、策略状态和实际访问验证。
- #1411 | 问：OnePlus/TWRP 把安装模块后的数据分区显示为 1TB，无法备份。 | 解决：COMPLETED；维护者解释 sparse 逻辑长度与物理占用区别，建议 `du` 检查；备份前可 `ksud module shrink`，但安装/更新模块可能再次扩展。 | 教训：同 #1389；模块文档应显式写 recovery 备份前的收缩步骤及风险，不能承诺全部 recovery 正确统计。
- #1417 | 问：手动建的 `/data/adb/service.d`、`post-fs-data.d` 脚本不在开机执行。 | 解决：COMPLETED，但无评论、PR 或确认，未证实关闭。 | 教训：不要假定 KSU 完全复刻 Magisk 的全局脚本目录；模块需使用其支持的 `post-fs-data.sh`/`service.sh` 生命周期并实机验证。
- #1419 | 问：HyperOS 安装多个模块后设置显示 TB 级存储。 | 解决：讨论归为 #1411/#1389 与 FAQ，维护者称新版 HyperOS 已修复统计问题。 | 教训：这属于 OEM 对 sparse file 的统计兼容性；模块作者不要为“设置页显示”去破坏镜像格式。
- #1420 | 问：请求补 Android 14 GKI protected exports 构建文档。 | 解决：维护者说不教授内核构建，但欢迎文档 PR；COMPLETED 不等于文档已经合并。 | 教训：内核编译/受保护导出问题要以 AOSP 和具体内核树为准；模块 skill 不应把未经验证的删 export 操作当通用步骤。
- #1422 | 问：Shamiko 兼容问题下如何降回 0.8.0 前。 | 解决：维护者给出 `rm -rf /data/adb/ksu` 后重新安装的恢复路径（COMPLETED）。 | 教训：版本降级会丢 KSU 状态/模块，必须先备份；不要在稀疏镜像迁移半途降级。
- #1425 | 问：刷原厂 boot 后仍不能卸载 KSU。 | 解决：NOT_PLANNED；维护者明确若刷原厂镜像仍不能恢复，则不是 KSU 问题。 | 教训：卸载排查先确认实际刷入的 slot/boot 或 init_boot、AVB 和厂商刷机流程，不能只删 app/目录。
- #1426 | 问：4.4 集成编译 `groups_sort` 未定义。 | 解决：评论指向 KernelSU-4.4 分支，无维护者修复/PR，属社区移植建议。 | 教训：旧内核 API 缺失必须采用对应维护分支或自行 backport；不要把主线 KSU 对 4.4 的兼容当承诺。
- #1428 | 问：Manager 显示未安装，发布页找不到同名 5.10 内核。 | 解决：NOT_PLANNED，维护者要求读文档，无具体修复。 | 教训：选择包必须按 KMI/官方安装规则，而不是只按 `uname` 的完整版本字符串搜文件名。
- #1430 | 问：升级后 `modules.img` 持续占用内部存储。 | 解决：维护者关联 #1401/#1384，建议更新 Manager 到 11583；社区另给 `ksud module shrink` 或删除镜像重建，未见单独 PR。 | 教训：先升级包含迁移修复的 Manager，并测量物理占用；收缩/删除会影响模块状态，需在可恢复条件下做。
- #1435 | 问：模块把 app 放入 `/system/app` 或 `/product/app` 后文件可见却打不开。 | 解决：COMPLETED，无评论、PR 或复现资料，未证实关闭。 | 教训：system-app 注入需要验证包扫描时机、权限、SELinux、ABI 和 mount namespace；不能由“文件已挂载”推断 app 一定能被 ClassLoader/PackageManager 使用。
- #1436 | 问：题干只有“SU”。 | 解决：NOT_PLANNED，空题干、无日志和回复。 | 教训：root 故障必须提供调用命令、授权对象、版本和日志，否则无法转化为模块兼容性规则。
- #1438 | 问：0.8.1 模块挂载 app 到系统分区后不能打开。 | 解决：NOT_PLANNED；维护者称未改过挂载，不认为有 KSU 证据。 | 教训：应用注入问题先比较模块自身文件、SELinux 标签、包扫描和 ROM 差异；报告没有日志不应作为 KSU 行为结论。
- #1439 | 问：Default profile 的 root 进程 capability 位全为 1，是否错误。 | 解决：维护者解释所有 cap 位为 1 等价于拥有全部 capability（COMPLETED）。 | 教训：模块的最小权限设计不要把十六进制全 1 误判为异常；需要收紧时应使用自定义 profile 并逐项测试实际 capability 需求。
- #1441 | 问：Android 9/4.4 上有授权却装模块报 `os error 2`。 | 解决：NOT_PLANNED，题干无后续日志/回复，未给修复。 | 教训：老内核模块安装失败须采集完整 install log、mkfs/ext4 工具和挂载状态，不能只凭错误码猜测。
- #1446 | 问：请求适配一个特定 5.10.168 Android 12 KMI/镜像。 | 解决：NOT_PLANNED，无维护者答复或发布承诺。 | 教训：预编译包按 KMI 而非单台设备需求维护；不匹配时走 LKM 或自行编译，不应等待任意版本 boot 产物。
- #1450 | 问：ARM64 WSA 替换 KSU 内核后 WSA 无法正常启动。 | 解决：NOT_PLANNED；维护者说明微软将结束 WSA 支持，因此不会再修该类 bug。 | 教训：WSA/虚拟化平台不是长期受支持的模块验证目标；在项目明确退役的平台上不要承诺 KSU 模块兼容性。
- #1452 | 问：BlissOS/自编 6.1 内核如何从旧 KSU 版本升级。 | 解决：维护者答应自行编译内核（COMPLETED），没有通用下载/原地替换方案。 | 教训：内置式 KernelSU 与内核绑定；模块开发者应区分 Manager 更新和内核侧 KSU 更新，不能在 `/data/adb/ksud` 手工替换来升级内核能力。
- #1455 | 问：KSU Manager 被部分银行 app 检出，卸载后恢复。 | 解决：NOT_PLANNED；维护者仅称可自行处理，未给规避方案。 | 教训：不要实现或承诺绕过金融 app 环境检测；将模块功能设计为不必长期保留 Manager，并遵守目标 app 的安全策略。
- #1458 | 问：题干仅“内核”。 | 解决：NOT_PLANNED，信息为空，无可验证处理。 | 教训：内核/模块 bug 必须有完整版本、最小复现和日志。
- #1466 | 问：0.8.0 安装模块后 `modules.img` 在 recovery 中显示异常大。 | 解决：COMPLETED；评论指向 sparse-file FAQ，并称安装一个模块后重启可修复旧格式带来的慢/统计问题。 | 教训：把逻辑长度与实际块占用分开看；升级后先让一次标准模块安装+重启完成迁移，再对 recovery 备份做验证。
- #1467 | 问：请求 LKM 直接支持 `.lz4` boot。 | 解决：NOT_PLANNED；维护者认为 LKM 使用 magiskboot 不应本质缺失 LZ4，报告者仍只能先解压，未有新增功能证据。 | 教训：镜像压缩格式应在修补前解包并按设备刷机工具要求处理；不要把“可刷 `.lz4`”假定为 Manager 的通用输入接口。

## 601–660

- #1469 | 问：ColorOS 14 经 LKM 获 root 后模块开关灰、不能生效/卸载。 | 解决：维护者根据早期启动日志给出提交 `2a64784`，并让安装 CI Manager；提问者确认“修好了”。 | 教训：模块系统异常优先保留开机阶段 dmesg；LKM/ROM 兼容修复须用对应 CI Manager 和完整重启验证。
- #1470 | 问：超级用户 app 搜索遇到空格后大小写敏感。 | 解决：COMPLETED，无评论、PR 或测试确认，未证实关闭。 | 教训：模块/Manager 的 app 搜索应对空白、Unicode、大小写做一致的归一化测试，不要从关闭状态推断已修。
- #1474 | 问：请求 `overlay.d` 支持及公开 ksuinit。 | 解决：NOT_PLANNED；维护者说明 ksuinit 仍在变化、未承诺开源，并称这不妨碍自行实现 overlay.d。 | 教训：不要依赖未公开、未稳定的引导二进制；模块可用标准 boot/module 脚本与 overlay 流程实现自己的兼容层。
- #1478 | 问：模块安装日志很长时 Reboot 按钮遮挡文字。 | 解决：COMPLETED，无评论、PR 或可核对修复，未证实关闭。 | 教训：WebUI/Manager 安装器应对长日志做可滚动布局与回归截图；不要假定 UI 小问题已随关闭修正。
- #1479 | 问：装完模块如何打开图形界面。 | 解决：NOT_PLANNED；维护者说明只有模块实际提供 GUI/WebUI 才能从模块卡打开。 | 教训：模块若要有设置页，必须随模块提供合规 WebUI；安装 KSU 模块本身不会自动生成图形界面。
- #1480 | 问：Telegram 入群考试刷 boot 后页面重置、不显示密码。 | 解决：评论要求仔细阅读 KMI/内核版本安装文档，提问者随后确认已解决。 | 教训：刷机自动化/验证页依赖正确的 KMI 和安装步骤；先验证真实设备完成的流程，再怀疑 Web 页面。
- #1481 | 问：国行 Samsung S23 Ultra 修补 `init_boot` 后无法启动。 | 解决：NOT_PLANNED；维护者确认 Knox/RKP 可能阻止 KSU 直接操作 cred，建议 GKI 或自行编译 Samsung 内核，暂无修复计划。 | 教训：厂商 RKP/Knox 是内核安全边界，模块不能试图绕过；对 Samsung 要明确 LKM/修补的支持限制。
- #1484 | 问：模块安装/卸载长时间卡住并伴随异常存储。 | 解决：评论给出旧 Manager 升新版本的镜像迁移恢复：删除 `modules.img`、用旧 Manager 后再按流程升级重启；维护者认可该步骤。 | 教训：升级模块镜像前备份；遇迁移卡顿先恢复到干净镜像状态，不能持续反复安装/卸载加重损坏。
- #1485 | 问：修补 `init_boot.img` 报 Unknown KMI。 | 解决：NOT_PLANNED；内核名不符合 GKI 规范，维护者给出手动命令：`ksud boot-patch -b <image> --magiskboot <path> --kmi <kmi>`。 | 教训：KMI 自动识别失败时显式指定正确 KMI；模块/工具不要从任意自定义 `uname` 猜测可用 LKM。
- #1490 | 问：第三方内核/原厂 boot 安装时 Unknown KMI，能否自动恢复另槽。 | 解决：NOT_PLANNED，直接指向 #1485 的手动 KMI 修补。 | 教训：A/B、第三方内核与原厂 boot 的混用需要人工确认分区和 KMI；自动恢复不是通用保证。
- #1495 | 问：App profile 的翻译字符串未显示。 | 解决：维护者只追问语言/国家代码，后续无修复或确认，COMPLETED 未证实关闭。 | 教训：本地化 bug 必须提供 locale、资源键与缺失条目；模块 WebUI 应自己覆盖多语言回退。
- #1496 | 问：Unknown KMI 能否通过扫描当前内核自动选择 LKM。 | 解决：维护者称 Manager 已支持手选 LKM，后续会让用户手选 KMI；所提自动方案可行但过于复杂。 | 教训：安装器 UX 应在 KMI 不可解析时要求用户明确选择，不要静默猜测；需清晰标注 LKM 文件与目标 KMI 的对应关系。
- #1499 | 问：0.9.2 Manager 直接安装后内核版本未更新。 | 解决：维护者说明该安装仅适用于 LKM；应先刷回原厂 boot，再修补并刷入 `init_boot`。 | 教训：GKI 内核镜像升级与 LKM 更新是两条路径；模块指南必须禁止在已改 boot 上把“直接安装”当通用升级手段。
- #1501 | 问：Settings > About 按钮问题（只有视频）。 | 解决：COMPLETED，无讨论、PR 或结论，未证实关闭。 | 教训：UI 报告至少要提供版本、最小操作和可访问日志；视频标题不能形成可复用实现规则。
- #1502 | 问：0.9.2 直接安装后 KernelFlasher 不能识别 boot。 | 解决：维护者判为非 KSU 问题；可刷回原厂 boot 后由 Manager 修补，或继续用官方提供 boot。 | 教训：第三方刷机工具对 KSU ramdisk 的解析失败不等于镜像无效；模块发布避免承诺任何 AK3/KF 兼容性。
- #1503 | 问：JuiceSSH 升级后不能用 `su`。 | 解决：NOT_PLANNED；其 bash 与 Android ash/路径差异导致，维护者建议自定义调用 `/system/bin/su`，报告者更新内置 bash 后恢复。 | 教训：模块脚本应显式使用 `/system/bin/su` 或受支持 shell，不能只假定 PATH 中裸 `su` 可解析。
- #1504 | 问：安装 KSU 后系统 WebView 帧率异常。 | 解决：先建议 LKM，随后提问者控制变量确认是 Scene 的 zram 附加模块，非 KSU。 | 教训：性能回归需移除所有模块并做二分；zram/内存类模块尤其要单独测试 WebView、图形和稳定性。
- #1505 | 问：为何缺少特定 GKI 149 版本预编译内核。 | 解决：NOT_PLANNED；维护者宣布今后每个 KMI 只编一个版本，建议 LKM。 | 教训：包选择以 KMI 为核心，不以每个 patchlevel 都有独立发布为前提。
- #1506 | 问：重复安装模块后导出的安装日志仍是首次日志。 | 解决：提问者随后称不用重启日志已能更新，未有代码/PR。 | 教训：模块安装日志要在每次事务后重新读取；修复/复现应确认缓存、重启与并发安装的差异。
- #1507 | 问：想自行获取停发的某个 GKI 构建版本。 | 解决：NOT_PLANNED；维护者建议查看上个版本源码自行构建。 | 教训：不要依赖历史预编译存档；设备维护者应保留可复现的内核/AK3 构建配置。
- #1508 | 问：Manager 在 Magisk root 下安装 LKM 报 create temp dir failed。 | 解决：NOT_PLANNED；该安装功能只支持 KernelSU root，不支持 MagiskSU。 | 教训：不要混合不同 root 管理器的内部安装流程；切换方案前先回到干净原厂 boot/init_boot。
- #1509 | 问：把官方 `kernelsu.ko` 直接 `insmod` 到设备时报未知符号。 | 解决：COMPLETED，但维护者仅答“Expected behavior”，后续转为各自内核集成问题，未给通用修复。 | 教训：LKM 必须与目标内核/导出符号严格匹配，不能把发布的通用 `.ko` 随意手工 `insmod`。
- #1511 | 问：建议 LKM 修补输出名不要都带 `boot`，避免误刷分区。 | 解决：COMPLETED，无评论、PR 或采纳证据，未证实关闭。 | 教训：模块/刷机工具输出必须清楚标注目标分区；用户仍应依据安装日志确认 boot 与 init_boot。
- #1518 | 问：如何从 Manager APK 用命令行提取 ksuinit 给 AVD 自动化。 | 解决：NOT_PLANNED；无公开提取接口，ksuinit 嵌在 `libksud.so`，可从源码取得或修改源码加接口。 | 教训：自动化不得依赖 APK 内部资产布局；使用源码构建或公开 CLI，且把二进制版本固定下来。
- #1520 | 问：OnePlus Ace2 实际只有 boot，却被提示 init_boot。 | 解决：NOT_PLANNED；维护者称该设备分区不规范，无法通用自动修复，只能手动处理。 | 教训：刷机模块必须允许用户明确选分区/镜像；自动探测有歧义时绝不能自动刷错。
- #1521 | 问：WebUI 调用 Zerotier HTTP API 失败。 | 解决：维护者说明 loopback HTTP 已允许，问题是 HTTPS 页面请求 HTTP；改为 `127.0.0.1` 后报告者确认可用。 | 教训：WebUI 后端本地服务绑定 `127.0.0.1`，同时处理 mixed content；不要为此开启全局 cleartext。
- #1522 | 问：无法修补 `.lz4` 镜像。 | 解决：维护者明确先解压 LZ4 再修补；如何按设备重新压缩/刷入未在本条保证。 | 教训：构建/模块安装文档应把“解包→修补→厂商工具打包/刷入”写成独立步骤，并保留原始镜像。
- #1524 | 问：4.9 非 GKI 集成新版后 Manager 找不到 KSU。 | 解决：讨论定位到遍历 `/data/app` 时文件名长度/终止符问题，贡献者提出补丁并被要求提 PR；没有关联已合并 PR 或最终确认，未证实关闭。 | 教训：旧内核文件遍历回调要按 `namelen` 比较，不能假设 C 字符串已终止；移植前对 `/data/app` 扫描做真实目录测试。
- #1527 | 问：希望 WebUI 独立窗口，避免多次返回才回到 Manager。 | 解决：COMPLETED，无评论、PR 或实现证据，未证实关闭。 | 教训：模块 WebUI 不应假定独立窗口 API 存在；自己设计明确的返回/关闭导航。
- #1528 | 问：最新提交下银行 app 仍检测 root。 | 解决：NOT_PLANNED；维护者在自身环境可正常打开，认为与 KSU 无关，未给规避修复。 | 教训：检测类问题要给可复现版本、模块集合与控制变量；不把 root hiding 当模块兼容性承诺。
- #1530 | 问：WSA 上任意模块 WebUI 白屏不能点。 | 解决：NOT_PLANNED；评论归因 WSA WebView，微软已停止/不再可靠维护，未有 KSU 修复。 | 教训：模块 WebUI 应在标准 Android WebView 测试；WSA 特有失败单列为平台限制。
- #1532 | 问：OnePlus 12 无法安装模块。 | 解决：评论建议删除 `/data/adb/ksu/modules.img`，提问者称问题解决（COMPLETED）。 | 教训：模块镜像损坏可在有备份前提下删除重建；删除会清空模块，不能作为无损常规操作。
- #1533 | 问：请求 release 提供 x86_64 LKM/AVD 内核。 | 解决：NOT_PLANNED；AVD 不是可按 GKI 通用的场景，必须匹配其具体版本；可复制 `build-lkm.yml` 自行改目标。 | 教训：模拟器 x86_64 模块需针对 exact AVD kernel 编译，不能依赖 ARM release 产物。
- #1539 | 问：刷多个模块后系统存储显示 1.1TB。 | 解决：NOT_PLANNED；评论指向 sparse-file FAQ/#1419，属于设备统计对 1TB sparse file 的兼容问题。 | 教训：同类存储显示问题先检查 `du` 与 recovery 实测；不要因 UI 数字而修改模块内容或扩大真实占用。
- #1541 | 问：Oppo Pad2 有 init_boot 分区但实际应刷 boot。 | 解决：NOT_PLANNED；维护者表示无法知道这种非标准布局，只能手动刷对应分区。 | 教训：设备专用脚本要维护已验证分区映射；自动安装必须留出人工确认和恢复路径。
- #1542 | 问：请求手动深色模式开关。 | 解决：NOT_PLANNED；维护者不愿加入但欢迎 PR。 | 教训：模块 WebUI 设计应遵从系统主题或自带 CSS 适配，不依赖 Manager 额外主题开关。
- #1544 | 问：OnePlus 装模块后内部存储变为 1TB。 | 解决：NOT_PLANNED，指向 #1419/FAQ 的 sparse file 解释。 | 教训：模块 README 应提前说明 OEM 统计异常及备份前收缩，而不是把逻辑长度当真实磁盘耗尽。
- #1550 | 问：发布页找不到 5.10.177、Android 安全补丁为 2024-03 的 boot。 | 解决：维护者要求读文档；评论指出应按 kernel security patch/KMI，而非 Android platform patch 日期找包。 | 教训：版本选择只看 KMI 与 kernel patch 标记，不能以系统 Android 安全补丁日期匹配镜像。
- #1556 | 问：掉 root、非 GKI 无安全模式、模块导致系统卡住时怎样删除模块。 | 解决：社区建议安全模式/TWRP 删除 `/data/adb`，或临时用 Magisk/APatch root 删除；提问者转 APatch 后成功。 | 教训：高风险模块必须提供失效后恢复手册；解锁 data/恢复环境与可替代 root 是救援前提。
- #1557 | 问：360 加固宣称新增 KernelSU 检测。 | 解决：NOT_PLANNED，仅有质疑/讨论，无对抗方案。 | 教训：不针对商业检测实现规避；模块功能应在 root 可见的前提下安全工作。
- #1558 | 问：从 Zygisk Next WebUI 返回后 Manager 首页偶发“获取 root 失败”，但实际正常。 | 解决：COMPLETED，无评论、PR 或确认，未证实关闭。 | 教训：WebUI/前后台切换后的状态刷新要容错，错误 UI 需用新进程、日志和实际 root 操作交叉验证。
- #1559 | 问：同样的假“获取 root 失败”、模块数为 0。 | 解决：维护者仅要求日志，随后无处理证据；COMPLETED 未证实关闭。 | 教训：不要仅根据首页状态让模块脚本做破坏性恢复；先验证 `ksud`、授权与真实模块挂载。
- #1560 | 问：通过完整性检查仍不能使用 RCS。 | 解决：NOT_PLANNED；维护者/社区指向 PlayIntegrityFix #297，非 KSU 专属问题。 | 教训：Google 服务/完整性模块问题应追踪对应模块上游，不要把 KSU 当唯一变量。
- #1561 | 问：请求 KSU 内置带 su 的终端。 | 解决：NOT_PLANNED；维护者反问是否还要内置文件管理等所有 root 功能。 | 教训：模块需要命令行时使用已有终端/公开 `su`，不依赖 Manager 变成工具箱。
- #1562 | 问：提交 Samsung Galaxy A03 设备内核。 | 解决：GitHub Actions 自动创建 PR #1563（COMPLETED），存档未证明后续合并/发布。 | 教训：设备支持以实际 PR 合并和 release 为准，机器人创建 PR 只是提交入口。
- #1566 | 问：请求模块更新时显示下载进度。 | 解决：NOT_PLANNED；社区建议检查系统下载器通知权限，无官方实现。 | 教训：模块自更新若需要可见进度，应在自身 UI/通知中实现，不能假定 Manager 下载 API 有进度回调。
- #1570 | 问：main 分支 11747 内核已集成但 Manager 找不到 KSU，旧授权/模块仍工作。 | 解决：维护者指出 kprobes 未开/不好用，建议检查编译选项、手工调整并拉最新代码；没有最终确认，COMPLETED 未证实。 | 教训：内核集成要验证 kprobe 配置与实际 hook；“旧模块还能跑”不等于 Manager 识别路径正确。
- #1571 | 问：WebUI 能否获取当前模块根目录，避免硬编码模块 ID。 | 解决：关联 PR #2057（`js: add moduleInfo method`）已合并。 | 教训：WebUI 用 `moduleInfo()` 获取模块元数据/路径，不要硬编码 `/data/adb/modules/<id>`。
- #1572 | 问：为何模块镜像初始为 1T、备份困难。 | 解决：维护者解释预扩容可避免未知动态模块的慢速扩容和损坏；Manager 提供 Settings → Minimize Image file。 | 教训：动态模块不应依赖固定 image 大小；备份前用正式的最小化操作并验证是否会影响后续动态写入。
- #1573 | 问：Android 14 修补报 Not Match Found，改文件权限后才成功。 | 解决：维护者先判断自定义内核名不合 GKI、应手工修补；报告者认为原厂镜像也复现，未见后续结论，COMPLETED 未证实。 | 教训：修补失败需分离 KMI 识别、存储访问权限与镜像有效性，不能把 chmod 777 当通用安全修复。
- #1577 | 问：报告者称“不再需要”。 | 解决：NOT_PLANNED；维护者称这是预期行为，问题不再成立。 | 教训：无技术细节的撤回不能形成模块开发结论。
- #1579 | 问：空白设备提交。 | 解决：COMPLETED，但 Actions 明确“无法创建 PR，请检查内容或手工 PR”，未被接收。 | 教训：设备提交必须填完整仓库/设备/维护者字段；关闭状态不等于设备支持。
- #1580 | 问：空白设备提交。 | 解决：COMPLETED，Actions 同样无法创建 PR，未证实接收。 | 教训：同 #1579，自动化失败后应修正模板或创建合格手工 PR。
- #1581 | 问：空白设备提交。 | 解决：COMPLETED，Actions 同样无法创建 PR，未证实接收。 | 教训：同 #1579，不能把 Issue 编号当设备已收录证据。
- #1582 | 问：提交 Samsung Galaxy Tab S9 FE 内核。 | 解决：GitHub Actions 自动创建 PR #1583（COMPLETED），无合并/发布记录。 | 教训：设备条目需继续跟踪 PR 状态和 CI，而不止提交 issue。
- #1584 | 问：提交 Galaxy S10 5G (KOR) 内核。 | 解决：GitHub Actions 自动创建 PR #1585（COMPLETED），无合并/发布证据。 | 教训：同 #1582，模块兼容矩阵应只列实际可验证的设备构建。
- #1588 | 问：能否在已内置 KSU 的 non-GKI 内核上用通用 LKM 更新 KSU。 | 解决：NOT_PLANNED；维护者说针对单一设备自行编 LKM 或许可行，但通用方案工作量大、无计划。 | 教训：通用 LKM 是 GKI/KMI 前提下的接口；non-GKI 设备只能自行维护专用内核/LKM，不能承诺模块跨设备通用。
- #1591 | 问：能否像 Magisk 一样备份/恢复原厂镜像并完整卸载。 | 解决：关联 PR #1619（`ksud: backup stock image and use it when restore`）已合并。 | 教训：安装/恢复流程应先保存可信原厂镜像和分区元数据；模块不能自行代替 root 核心恢复机制。
- #1594 | 问：AirMusic 的 Magisk 模块在 KSU 下崩溃。 | 解决：维护者让关闭“Umount modules by Default”，提问者确认可用；也可针对特定 app 关闭，并需给其服务 app root。 | 教训：需要 app 读取模块挂载内容的模块必须说明 umount/授权要求，按 app 最小化放开而非全局盲关。
- #1595 | 问：设备显示 2TB、已用 1.9TB 等存储误报。 | 解决：NOT_PLANNED，评论指向 #1419/#1544/#1572 和 sparse-file FAQ；未有 KSU 格式回退。 | 教训：OEM 文件统计对 sparse file 不兼容应视为平台限制；先测真实物理占用、重启与备份行为。

## 661–700

- #1596 | 问：0.9.2 Manager 的超级用户搜索/授权卡顿并可能闪退。 | 解决：NOT_PLANNED；维护者要求先排查其它模块，不认为 KSU 本身会导致卡顿/崩溃。 | 教训：性能/崩溃报告必须先做无模块基线和 crash log；模块不能把全局 UI 注入的副作用归给 root 管理器。
- #1597 | 问：Redmi Note 12 Turbo 无法安装模块。 | 解决：维护者建议删除整个 `/data/adb/ksu` 目录后重来（COMPLETED）。 | 教训：这是破坏性重置，会清除 KSU 模块状态；在救援前先备份、确认可重新授权和重装模块。
- #1599 | 问：提交 Galaxy S10e 设备内核。 | 解决：Actions 自动创建 PR #1600（COMPLETED），无合并/发布证据。 | 教训：设备内核条目不能仅凭自动 PR 视为可用，要核查 PR 和构建产物。
- #1601 | 问：提交 Galaxy S10+ 设备内核。 | 解决：Actions 自动创建 PR #1602（COMPLETED），无合并/发布证据。 | 教训：同 #1599，模块兼容表只列已验证的构建。
- #1603 | 问：提交 Galaxy S10 设备内核。 | 解决：Actions 自动创建 PR #1604（COMPLETED），无合并/发布证据。 | 教训：同 #1599，设备支持需要持续跟踪 CI 与维护者。
- #1605 | 问：WSA 换 KSU kernel 后 COM Surrogate 高 CPU，WSA 闲置会被杀。 | 解决：COMPLETED；社区要求完整复现，报告者发现关闭 WSA “share userfile”即可避免，未有 KSU 核心修复证据。 | 教训：虚拟化平台异常应分离宿主 WSA 设置与 KSU；不把单个 WSA workaround 记为通用模块修复。
- #1609 | 问：DriveDroid 在 KSU 下找不到 SD 卡镜像目录。 | 解决：NOT_PLANNED，维护者仅指向 #2689，无本条可核对修复。 | 教训：外部 app 的存储/挂载假设要查其上游 issue；模块应提供路径、namespace、SELinux 诊断而非猜测。
- #1610 | 问：银行 app 仅因安装 KSU Manager 就检测 root。 | 解决：COMPLETED，讨论指向 #628；建议不需要时冻结/卸载 Manager，未提供改包或隐藏实现。 | 教训：模块功能不能依赖常驻 Manager；不把检测规避、改包名或重签当公开 KSU 能力。
- #1615 | 问：希望可解压安装随机密码保护的模块 ZIP。 | 解决：NOT_PLANNED；评论指出不应依赖受密码保护 ZIP，未有官方解密支持。 | 教训：KSU 模块包必须是标准、可检验的未加密 ZIP；发布密码/自解压包会破坏可审计性和安装兼容。
- #1621 | 问：装模块报 `specified file not found in archive`。 | 解决：NOT_PLANNED；评论认定 ZIP 不符合规范、缺根级必要描述/脚本，且含 KSU 不支持的 `.replace` 行为。 | 教训：模块 ZIP 要按 KSU 模块规范组织，不能带 `.replace` 或依赖 Magisk 私有行为；问题先反馈给模块作者。
- #1624 | 问：第三方内核 + LKM 选择直接安装升级无反应、设备只有 boot。 | 解决：COMPLETED，无评论、PR 或可核对恢复路径，未证实关闭。 | 教训：非标准分区/第三方内核的升级必须手动确认 boot、init_boot 和 KMI；不要把无反馈的关闭当直接安装已可用。
- #1625 | 问：`kernel/setup.sh` 因 git 无 tag/空 pathspec 失败。 | 解决：COMPLETED；报告者后称“突然又能工作”，无补丁或根因证据。 | 教训：自动 setup 脚本应在无 tag、浅 clone、异常 checkout 时给确定错误；不能把暂时恢复当可靠构建方案。
- #1629 | 问：提交 LG V60 timelm 设备内核。 | 解决：Actions 自动创建 PR #1630（COMPLETED），无合并/发布证据。 | 教训：同其它设备提交，PR 创建只是开始，模块兼容性需后续实机验证。
- #1631 | 问：4.19 backport `path_umount` 后 LG app/widget 崩溃，关闭默认 umount 可恢复。 | 解决：NOT_PLANNED；存档未见维护者给出兼容修复。 | 教训：旧内核 backport mount/umount 语义风险很高；模块/ROM 应允许按 app 或全局关闭 umount，并在厂商 ROM 做回归。
- #1634 | 问：请求绕过香港 Octopus app 的环境检测。 | 解决：COMPLETED；维护者仅建议卸载 Manager，不提供 bypass。 | 教训：不为金融/支付 app 构建检测对抗；模块应避免扩大攻击面，也不要承诺完整性规避。
- #1636 | 问：模块处理 `/system/vendor` 时只剩被覆盖的文件。 | 解决：NOT_PLANNED；维护者先质疑手工解压到模块目录，未有本条最终修复。 | 教训：必须用标准 ZIP 安装；vendor 目录模块遵循 `system/vendor` 布局和 KSU 安装期符号链接处理，不能手工放入 `/data/adb/modules`。
- #1639 | 问：再次请求随机化 KSU Manager 包名。 | 解决：NOT_PLANNED，维护者要求先搜索已有 issue。 | 教训：同 #1370/#1610，拒绝把规避检测做成模块依赖或 Manager API。
- #1646 | 问：Huawei MatePad 11.5 安装 APK 后应下载哪份 boot。 | 解决：NOT_PLANNED；社区说明其非 GKI/无 KSU 内核，必须自行编译。 | 教训：安装 Manager 不代表设备可 root；先判断目标是否为受支持 GKI/KMI，非 GKI 要有可维护内核源码。
- #1647 | 问：请求和 `setup.sh` 配套的 KSU 移除脚本。 | 解决：COMPLETED，无评论、PR 或产物证据，未证实关闭。 | 教训：不要假设 setup 的反操作存在；内核集成前记录改动、保留 Git revert 和原厂镜像恢复方案。
- #1652 | 问：Android 14 日志出现 `init second_stage` Fatal，怀疑 KSU。 | 解决：NOT_PLANNED；维护者称没有 KSU 因果证据，报告者仍称无 KSU 时消失但无后续定位。 | 教训：init/SELinux 崩溃要做带/不带 KSU 的最小 diff、完整 tombstone 和 kernel log；不能靠字符串命中归因。
- #1655 | 问：ChromeOS ARM ARCVM 替换 KSU kernel 后 Android app 不能启动。 | 解决：关联 PR #1906（`ci: Fix ARCVM workflow to adapt to the change of kernel/Makefile`）已合并；原报告后来因缺少有效 ARCVM 日志被关闭，不能外推为所有 ARM Chromebook 已验证。 | 教训：ARCVM 不是手机 GKI；按具体 ChromeOS/Android/ABI 获取 ARCVM 日志并使用对应 CI workflow。
- #1659 | 问：请求 recovery ZIP/addon.d 自动在 OTA 后刷入 LKM。 | 解决：NOT_PLANNED，维护者明确无计划。 | 教训：第三方 OTA 自动化不能依赖稳定暴露的内部二进制；若自建 addon.d，必须自行维护版本和失败回滚。
- #1661 | 问：ColorOS 14 下 `fastboot boot` 官方 KSU boot 失败，修补 init_boot 却可用。 | 解决：NOT_PLANNED，维护者仅指向 #1051 作为可能原因，无通用修复。 | 教训：临时 boot 与永久 flash 在厂商 fastboot 上不同；模块安装文档应优先选择已验证的分区/刷写路径。
- #1663 | 问：模块不再处理 `/product` 分区。 | 解决：NOT_PLANNED；报告者删除 `/data/adb/*` 后 KSU 重建目录并称已恢复，未有代码变更。 | 教训：分区挂载异常先验证模块状态目录/迁移是否损坏；完整删除会清状态，必须先备份而非视为常规修复。
- #1664 | 问：希望把日志导出到本地而不是只打开分享菜单。 | 解决：COMPLETED；讨论了保存/分享交互和 Intent，但没有关联 PR 或已上线确认，未证实关闭。 | 教训：模块日志功能应同时考虑本地保存、分享和敏感数据脱敏；不要依赖未确认的 Manager 保存接口。
- #1665 | 问：APK 升到 11807、核心仍 11804，LSPosed 图标消失。 | 解决：维护者解释 Manager 有 3 个仅 app 的 bugfix commit，内核未变，版本号不同正常；图标问题非由该差异证明。 | 教训：诊断时分开记录 Manager/ksud/内核版本；不能只因版本号不同就强制刷内核。
- #1666 | 问：Android 15 beta 上 Manager 卡 splash。 | 解决：NOT_PLANNED；日志显示 LSPosed Bridge，维护者判定 LSPosed 尚不支持 Android 15，非 KSU。 | 教训：新 Android 版本适配要先禁用注入模块/框架；Manager UI 故障不能忽略第三方 zygote hook。
- #1667 | 问：自定义 `KERNEL_SU_OPTION` 并改签名后 Manager 报获取 root 失败。 | 解决：NOT_PLANNED；维护者要求自行加日志追踪哪里未调用。 | 教训：改 KSU 协议/签名/常量属于私有 fork 维护，必须同步内核与 Manager 并有端到端调试，模块不能假设兼容。
- #1669 | 问：APK 11807、镜像 11804 是否异常。 | 解决：维护者确认正常，app 修复不会必然带内核版本变更。 | 教训：同 #1665，版本显示是组件独立发布的结果，升级决定需看变更类型而非简单数字相等。
- #1673 | 问：kprobe `post_handler` 能否把内容追加到文件末尾以覆盖 build.prop。 | 解决：NOT_PLANNED，无维护者回复或实现。 | 教训：不要依赖 kprobe 文件注入修改只读系统属性；优先用受支持的模块 overlay/服务，并避免在 init 读属性后再修改。
- #1674 | 问：请求提供 legacy_lz4 GKI 预编译包。 | 解决：NOT_PLANNED；评论建议按文档修补，报告者后来怀疑实际是 vbmeta mismatch，而非 LZ4 格式。 | 教训：不能把启动失败直接归为压缩格式；先验证 AVB/vbmeta、KMI 和刷写方式，再决定是否需特殊镜像。
- #1675 | 问：4.4 内核加入 throne_tracker 后卡第一屏。 | 解决：NOT_PLANNED；维护者判断旧内核 `iterate_dir` 可能死锁，建议移植较新实现、在 `search_manager` 旁处理抢占，最终仍称是该内核问题并建议用 0.9.0/其他集成法。 | 教训：旧内核目录遍历、抢占和锁语义不能机械 backport；每个 target kernel 必须独立启动/Manager 扫描回归。
- #1676 | 问：能否把 magiskboot 释放到 KSU 工作目录，供命令行/OTA 脚本使用。 | 解决：关联 PR #1684（`Add magiskboot to kernelsu working directory`）已合并。 | 教训：自动化应使用 KSU 正式释放的工具位置与版本，不要从 Manager APK 私自提取 magiskboot。
- #1682 | 问：0.9 代码可在 4.x 编译运行但新版 Manager 显示不支持，是否有意。 | 解决：NOT_PLANNED；无官方恢复 non-GKI 支持，后续公告明确 1.0 放弃。 | 教训：旧非 GKI 即使暂时可编译也不是支持承诺；模块/设备集成要锁定最后受用版本并维护自己的 fork。
- #1683 | 问：4.4 上直接装 0.9.3 Manager 显示不支持，但先装 0.9.2 再升级可用。 | 解决：维护者让装 0.9.3 后任意安装/卸载一个 app 触发扫描，报告者确认恢复。 | 教训：Manager 识别依赖安装包扫描/缓存时需做刷新测试；这只是旧内核临时兼容现象，不替代官方 support matrix。
- #1686 | 问：是否支持 MIUI 第二空间。 | 解决：NOT_PLANNED；维护者明确“不支持”。 | 教训：模块权限、挂载和授权状态不应假设跨 MIUI 多空间可用；多用户/第二空间需单独能力检测。
- #1695 | 问：能否让模块通过 shell 写 allowlist、自动授权其 APK。 | 解决：NOT_PLANNED，维护者明确 `wont fix`。 | 教训：模块绝不能直接授予 app root 或编辑二进制 allowlist；授权必须由用户经 Manager/受支持安全流程确认。
- #1698 | 问：请求为 5.15.123-android13 提供预编译构建。 | 解决：NOT_PLANNED；维护者要求先理解 KMI，报告者最终承认混淆版本并转向正确安装/LKM 路径。 | 教训：不要按内核字符串逐版索要 boot；先查 KMI 兼容性、系统分区和 LKM 可行性。
- #1699 | 问：装任何模块报 `No such file or directory`，导日志也崩。 | 解决：NOT_PLANNED；维护者发现内核不能执行 `/data/adb/ksud`，判定旧内核整体异常；用户手工放 `mkfs.ext4` 后可装，但不是官方兼容修复。 | 教训：模块镜像安装依赖可执行 ksud、SELinux 和兼容 ext4 工具；不要把手拷二进制当通用支持，应修内核/工具链根因。
- #1700 | 问：`adb shell` 无法申请 `su`，终端 app 却可。 | 解决：COMPLETED；报告者称改为不使用 kprobe 的手工集成后解决，未有官方通用补丁。 | 教训：shell root 要单独授权/测试；kprobe 集成问题需验证 hook 路径，不能泛化为所有 adb 不支持。

## 701–735

- #1702 | 问：热管理模块改 `/system/vendor` 后，重启只剩模块文件、原文件仿佛丢失。 | 解决：NOT_PLANNED；维护者明确不支持自解压 `/vendor` 文件，模块应把 vendor 内容放到 `system/vendor`，由 KSU 安装时处理符号链接；需修改模块。 | 教训：模块 ZIP 必须使用 KSU 目录布局，不在运行时自行解压/覆盖 vendor；目录 overlay 与单文件替换的行为不同。
- #1705 | 问：项目发布 1.0 前的支持策略公告。 | 解决：COMPLETED；官方原则上只接收 bug、外部 feature 要走 PR；以后只处理 GKI，1.0 放弃 non-GKI，旧设备建议停在 0.9.5。 | 教训：Skill/模块文档必须把 GKI 支持边界写清，non-GKI 只作为历史/自维护分支，不许承诺官方修复。
- #1710 | 问：简单 hosts 模块后 ADB 消失，删 modules 镜像才恢复。 | 解决：关联 PR #1713（`ksud: refine get_tmp_path`）已合并；维护者指出低 Android 版本需调整 legacy 临时目录 `/sbin`。 | 教训：模块安装期临时路径依赖 Android 版本；低版本设备要用对应 ksud 修复，不要用 hosts overlay 掩盖 init/USB 服务问题。
- #1715 | 问：请求帮助移除 Samsung S9+ 内核安全机制以编 KSU。 | 解决：NOT_PLANNED，维护者指向 #1705 支持政策。 | 教训：不把绕过厂商安全机制作为 KSU 模块/集成目标；只有有能力维护的开源内核改动才应自行承担。
- #1716 | 问：自编 KernelSU-debug.apk 显示不支持，官方 APK 正常。 | 解决：NOT_PLANNED；自编 Manager 必须让内核中的签名大小/哈希匹配，维护者给出 `ksud debug get-sign` 获取方式。 | 教训：定制 Manager 必须同步 APK 签名、`KSU_EXPECTED_SIZE` 与 hash；不能只重编 APK 而不更新内核验证数据。
- #1718 | 问：模块注入 `/system` 的 app 文件在位却不工作，ClassLoader 看不到。 | 解决：维护者让关闭 Settings 中 “umount by default”（COMPLETED）。 | 教训：需要系统/普通 app 可见模块文件时，要按 app 或全局关闭模块自动 umount，并在真实 ClassLoader/PackageManager 里验证。
- #1719 | 问：v0.9.4 下 Frida spawn 直接崩溃。 | 解决：NOT_PLANNED；提问者重新刷镜像后恢复，承认自己操作有误并要求关闭。 | 教训：安全工具/注入异常先重新刷已知正确镜像、移除模块和复现；单次刷机状态不能推出 KSU 回归。
- #1720 | 问：4.4 编译 `sucompat.c` 报 `real_regs` 未声明。 | 解决：COMPLETED，无评论、PR 或修复确认，未证实关闭。 | 教训：旧内核寄存器宏/API 差异必须针对架构和内核版本做条件适配；不要从关闭状态复制未知补丁。
- #1723 | 问：未授 root 的完整性检查 app 出现 UID/Play Integrity 异常，怀疑被当 root。 | 解决：维护者建议重启，报告者称仍存在，后续仅有系统/Play 日志，无确定修复；COMPLETED 未证实。 | 教训：授权状态、Android UID、Play 服务/Keystore 需要分别检查；模块不要以“重启即可”替代可验证回归。
- #1724 | 问：模块 `sepolicy.rule` 在非 GKI 设备上仍导致 camera server 异常。 | 解决：NOT_PLANNED；维护者指向官方 sepolicy 文档并称只接 5.10+，报告者后来确认是文件权限/所有权问题。 | 教训：`sepolicy.rule` 之外还要校验文件 owner/mode/label；不要为调试把 SELinux 设 permissive 作为最终方案。
- #1725 | 问：模块卸载失败，手删目录后重启又出现。 | 解决：维护者明确删除 `/data/adb/ksu/modules.img` 即可（COMPLETED）。 | 教训：镜像是模块状态的来源，删普通目录不能持久卸载；删除镜像会清空全部模块，先备份并告知用户。
- #1727 | 问：OnePlus 12/Termux 在给 root 后安装包报 `/dev/pts` permission denied。 | 解决：COMPLETED，但评论仅持续报告 devpts SELinux 问题和不安全的 permissive workaround，无本条已验证修复/关联 PR。 | 教训：终端模块必须测试 `/dev/pts` 标签、owner 和 package manager；绝不能把 `setenforce 0` 写成解决方式。
- #1730 | 问：模块挂载发生在开机动画后，bootanimation 模块失效。 | 解决：NOT_PLANNED，无维护者回复/时序变更。 | 教训：KSU 模块 overlay 不保证早于所有 init/bootanimation 读取；需要更早时序的资源替换不可假定可用。
- #1731 | 问：`chcon` 不能把文件标成 `u:object_r:rootfs:s0`。 | 解决：NOT_PLANNED；维护者说明 `/system` 只读。 | 教训：模块不要直接重标只读 system 文件；使用 module overlay 和 `sepolicy.rule`，把可写数据放 `/data`。
- #1736 | 问：请求完整卸载 KSU、清理 ksud/模块并恢复原厂镜像。 | 解决：COMPLETED，无评论、PR 或产物证据，未证实关闭。 | 教训：完整卸载应使用实际可用的官方 restore/原厂刷机流程；不能假定这个 Issue 已交付一键清理。
- #1739 | 问：Samsung boot 镜像无法修补。 | 解决：维护者明确应先解压 LZ4 镜像（COMPLETED）。 | 教训：输入镜像必须先从压缩容器解出原始 img；修补后按 Samsung 合适流程封装/刷写。
- #1740 | 问：按 AOSP Bazel 命令编 6.1 Image 后刷入不开机。 | 解决：NOT_PLANNED；维护者要求先搜 issue，报告者换命令仍不行，未有 KSU 根因或修复。 | 教训：GKI 构建不仅是一个 Image 路径，必须匹配 dist 产物、config、AVB 和设备刷法；不要拿任意 bazel-out Image 覆盖 AK3。
- #1743 | 问：请求应用内支持 LZ4 Image 修补。 | 解决：NOT_PLANNED；评论说明先解包后修补，Odin/Heimdall 可刷未重新压 LZ4 的 boot.img，或打 tar 包。 | 教训：Samsung 流程优先验证刷机工具支持的未压缩 img/tar；不要重压为不兼容 LZ4。
- #1747 | 问：银行 app 提示检测到 Magisk/root。 | 解决：NOT_PLANNED，评论指向 #1705，未提供规避。 | 教训：同所有金融检测项：不开发隐藏/绕过功能，把安全检测当目标 app 的边界。
- #1759 | 问：最新 release 编译脚本报 `clang-android.sh` 缺失、`MODULE_IMPORT_NS` 等错误。 | 解决：NOT_PLANNED；维护者要求先检查自身 4.14 源码为何绕过 guard，后续社区给 #1972 建议，未有官方兼容承诺。 | 教训：先验证内核树、toolchain、集成 commit 和 guard 条件；老内核编译错误不能拿主线 release 直接套补丁。
- #1766 | 问：请求手机自动救砖功能。 | 解决：COMPLETED，无评论、PR 或实现证据，未证实关闭。 | 教训：root 核心/模块不能承诺自动救砖；发布前应提供可验证的 boot、recovery、data 备份和人工恢复手册。
- #1767 | 问：请求自动给手机救砖。 | 解决：NOT_PLANNED，无技术方案。 | 教训：同 #1766，救砖属于设备/刷机流程，不是通用模块职责。
- #1768 | 问：再次询问 Android 6.1 编译后不开机。 | 解决：NOT_PLANNED；维护者指出 CI 代码公开且可启动，要求检查自己的构建差异。 | 教训：对比官方 CI 的完整配置、commit、dist 和签名，而不是只比较一条 Bazel 命令。
- #1770 | 问：Android-x86 用 GCC 编译 allowlist mutex 报 initializer non-constant。 | 解决：NOT_PLANNED；维护者认为不是 KSU，建议换编译器。 | 教训：内核构建器/语言标准要与目标内核匹配；模块不应把 host GCC 异常当运行时 KSU 问题。
- #1771 | 问：4.4 构建 `throne_tracker.c` 时 `full_name_hash` 参数数量不匹配。 | 解决：关联 PR #1773（`kernel: fix throne_tracker uncompile-able on 4.4 kernel`）已合并，报告者确认合并后有效。 | 教训：旧内核 API（此处 `full_name_hash`）须加版本兼容分支；把此类修复纳入 4.4 编译 CI。
- #1776 | 问：题干只有“1”。 | 解决：NOT_PLANNED；维护者问与 KernelSU 有何关系。 | 教训：无问题、环境、日志的条目不能形成任何模块实现依据。
- #1777 | 问：请求更易管理 sparse image 的自动缩小/手动 resize。 | 解决：NOT_PLANNED；维护者引用 #1705，且最小化功能已存在但未承诺新自动策略。 | 教训：用现有 Minimize Image/`ksud module shrink`；自动收缩会与动态写入冲突，不能设计为默认无条件行为。
- #1778 | 问：请求 Manager 内置模块仓库、更新通知和审核。 | 解决：NOT_PLANNED；维护者引用 #1705，讨论建议 MRepo/MMRL 等第三方管理器。 | 教训：模块仓库是独立供应链/审核问题；模块作者应遵守可信第三方仓库规范，不依赖 KSU Manager 自带市场。
- #1783 | 问：BlissOS 安装后 Manager 报未安装，Live boot 却正常。 | 解决：关联 PR #2041（`kernel: Relax dentry_path_raw compare in core_hook`）已合并；但后续仍有人在不同 BlissOS release 复现，不能宣称所有发行版已解决。 | 教训：x86/BlissOS 环境的路径比较必须兼容其挂载布局；修复后仍要按具体 ISO/Live 与 installed 状态回归。
- #1784 | 问：Termux root 后 `pm` 失败。 | 解决：关联 PR #1801（`Convert devpts domain to ksu_file`）已合并；讨论仍有不同 GKI/non-GKI 报告，需按发布版本验证。 | 教训：模块调用 `pm`/`am` 时要测试 devpts SELinux domain；不能只靠非 GKI 集成指南补丁或 permissive workaround。
- #1787 | 问：Root Explorer Global Namespace 设置不生效。 | 解决：NOT_PLANNED；维护者确认内核未实现该能力、UI 已禁用，并建议 `su -M`。 | 教训：不要在模块 profile 中依赖未实现的 global namespace；需要 global mount 时使用实际支持的 `su -M` 并检测能力。
- #1791 | 问：msm-4.4 编译 `MODULE_IMPORT_NS` 失败。 | 解决：COMPLETED；社区说明 non-GKI 支持已移除，需用旧版或自行 revert，另指向 #1972；无官方新修复。 | 教训：旧内核维护要锁定兼容 KSU 版本或维护 fork，不能期待 1.0 主线继续适配。
- #1793 | 问：升级 1.0 后开机核心频率满载。 | 解决：NOT_PLANNED；维护者判为模块问题，要求自行排查。 | 教训：性能异常先禁用全部模块、逐个二分并采集频率/trace；root 核心版本不是唯一变量。
- #1797 | 问：Vivo 新机修补 init_boot 后仍显示未安装，Magisk 可 root。 | 解决：NOT_PLANNED，维护者指向 #1289；评论称 Vivo seccomp 有额外限制，无官方 LKM 修复。 | 教训：厂商 seccomp/安全策略可能阻止 LKM；模块安装器必须明确设备限制并保留原厂恢复方案。
- #1800 | 问：1.0 刷后 Manager 显示未激活，但模块和 root 正常（Meizu 20 Pro）。 | 解决：关联 PR #1832（`throne_tracker: skip iterate if failed to open dir`）已合并；问题源于 Meizu `/data/app/default_cow` 被 SELinux 拒绝，遍历提前停止。 | 教训：Manager 发现逻辑遇单目录无权限时应跳过继续扫描；模块/ROM 集成要在严格 SELinux 下测试 `/data/app` 的异常条目。

# KernelSU closed-issue ledger — JSONL 185–368

口径：每条均阅读题干、`stateReason`、关联 PR 和存档中的最后 25 条评论。`COMPLETED` 本身不等于已修复；没有可核对的修复回复、提交或确认时，明确标为“未证实关闭”。

## 185–236

- #547 | 问：已装 KernelSU 的设备，怎样让 PC 的 `adb shell` 获得 root。 | 解决：维护者明确要求在超级用户页授权 `com.android.shell`；后续补充了读取受限文件时先在 `adb shell` 内 `su -c cp` 到可拉取目录的做法（COMPLETED）。 | 教训：模块调试应区分 host-side `adb pull` 权限与 device-side root shell；先给 shell UID 授权，再在设备端完成特权文件操作。
- #551 | 问：WSA 中把 Manager 预装为 `/system/priv-app` 后无法启动。 | 解决：NOT_PLANNED；维护者明确说 Manager 不应做 system APK，建议开机后用 `pm` 安装为普通用户 app，并解释预装会破坏首次启动/模块流程。 | 教训：不要把 Manager 打进 `system`；若 ROM 需自动部署，做 post-boot 的用户态安装，而非改模块或内核绕过。
- #552 | 问：从旧版升级 KernelSU 是否必须先恢复原厂 boot。 | 解决：维护者回复“直接刷就行”（COMPLETED）。 | 教训：升级包设计应允许直接覆盖目标 boot；仍需严格匹配设备、Android/KMI 与分区。
- #554 | 问：低版本内核编译时出现 `sockptr_t`、`__randomize_layout` 错误。 | 解决：NOT_PLANNED，仅被转到 #555，未给出修复。 | 教训：老内核移植错误必须按内核 API/补丁版本逐项适配，不能把关闭状态当兼容性承诺。
- #555 | 问：同一低版本 Nova2Plus 内核编译失败。 | 解决：NOT_PLANNED；维护者认为“与 KernelSU 无关”，无补丁。 | 教训：先复现最小内核配置并隔离原树/工具链错误；模块作者不能假设任意旧内核可编。
- #556 | 问：未使用 overlay/module 时，`try_umount` 和 allowlist 检查有额外开销。 | 解决：COMPLETED 的讨论建议缓存 post-fs-data 后稳定的 `should_umount` 结果，但无关联 PR/落地确认，属未证实实现。 | 教训：模块挂载状态应可被缓存/精确声明；性能优化先避免无模块时走全量卸载路径。
- #559 | 问：WSA 中把 Manager 放在 `/system/app` 后 SuperUser/`su` 不工作。 | 解决：NOT_PLANNED；维护者明确不支持 system/priv-app 安装，建议 boot 后 `pm` 安装用户 app。 | 教训：不要依赖 system-app 身份取得 KSU 管理权限；自动安装应保留正常 APK/签名/首次启动流程。
- #566 | 问：安装 false-encryption 模块后重启清空 app 数据。 | 解决：NOT_PLANNED；维护者答“no log no fix”，无诊断包即未修。 | 教训：涉及加密、`/data`、fstab 的模块必须先备份并收集完整日志；高风险模块不能只按 Magisk 行为假设可移植。
- #568 | 问：如何向只读的 `/system/etc/security/cacerts` 安装系统根证书。 | 解决：评论给出模块 overlay 目录 `system/etc/security/cacerts` 加证书文件的方案（COMPLETED），没有 maintainer 验证回复。 | 教训：系统文件修改优先用模块 `system/` overlay，不要 remount `/system`；证书命名/格式和目标系统可见性须实机验证。
- #571 | 问：安装 KernelSU 后 PUBG/Standoff 等游戏不能运行。 | 解决：NOT_PLANNED；维护者称与 KernelSU 无关，提问者反称换内核后恢复，未形成可复现修复。 | 教训：游戏兼容问题需收集内核、ROM、模块、检测日志，不能把单一更换内核经验写成通用模块方案。
- #573 | 问：提交 Samsung Galaxy A50 手工集成的设备内核。 | 解决：GitHub Actions 自动建立 PR #574（COMPLETED）。 | 教训：设备条目要提供可访问仓库、设备名和维护者身份，并让自动化/PR 承接发布记录。
- #577 | 问：Manager 给 app 开 root 时 JNI `setAppProfile` 因 null 崩溃。 | 解决：COMPLETED，但无评论、PR 或确认，未证实关闭。 | 教训：模块/管理器调用 profile API 必须防空对象、校验 native 返回值并保留 crash log。
- #578 | 问：建议把 KernelSU Kconfig 从 `drivers/` 移到 `security/`。 | 解决：NOT_PLANNED，未见维护者理由或 PR。 | 教训：目录/菜单重组不是模块兼容性接口；不要依赖源码路径来判断运行时能力。
- #580 | 问：非主用户 app 授权后不能 root，且同包不同用户会互相授权。 | 解决：COMPLETED，但无修复评论/PR，未证实关闭。 | 教训：模块与辅助 app 要用完整 Android 用户/UID 语义测试，不能只按包名或主用户 appId 管理授权。
- #583 | 问：#580 后同一 app 在主用户与非主用户的 root 不能共存。 | 解决：维护者让测试提交 `8af5a90`；提问者随后说旧 bug 消失却出现新 bug，故最终关闭不等于完全修复。 | 教训：多用户授权修复必须做双向回归：两个用户同包、分别授权/撤销、重启后都验证。
- #587 | 问：非主用户 app 重启后丢失 root。 | 解决：COMPLETED，但唯一后续评论仍报告其它设备复现，未见修复证据。 | 教训：授权持久化测试必须覆盖 secondary user 的重启、升级和包 UID 变化。
- #588 | 问：发布的 AnyKernel3 zip 对 `init_boot` 的处理是否有误。 | 解决：维护者准备更新 AK3；原建议者随后确认现有 `block=boot`/直接检查 `init_boot_a` 的方案合理，撤回改 `block=auto` 建议（COMPLETED）。 | 教训：刷机脚本不可机械套用分区策略；先确认 kernel 真正所在 boot/init_boot 分区，再做兼容分支。
- #592 | 问：custom profile 开启后失 root，groups/capabilities 会重复加入。 | 解决：维护者说明 `su` 需要足够 capability，并给出 `9ae0126 Add minimal capabilities for su`（COMPLETED）。 | 教训：最小 capability profile 也必须覆盖 `su` 的实际需求；配置编辑器要去重并回归测试保存多次。
- #598 | 问：custom profile 勾选全部 groups 后 root group 重复。 | 解决：COMPLETED，无评论/PR，未证实关闭。 | 教训：模块配置 UI 的集合字段应按 value 去重、保存后重载验证。
- #601 | 问：Android 13 kernel 的 patch level 被 CI 标错。 | 解决：COMPLETED，无可见修复证据。 | 教训：构建/发布元数据应从实际分支/KMI 生成并加 CI 断言；本条不能证明具体修复方式。
- #602 | 问：英文资源把 `Umount` 拼成 `Umoun`。 | 解决：关联 PR #605 “Update English translation” 已合并（COMPLETED）。 | 教训：UI 文案小改动也应由可追溯 PR 承接，并随模块/Manager 版本发布。
- #606 | 问：Manager 0.6 不能识别旧 Linux 内核的手工移植 KernelSU。 | 解决：NOT_PLANNED；维护者要求内核和 Manager 版本匹配。 | 教训：模块作者/ROM 集成者应检测并声明 KSU UAPI 与 Manager/ksud 版本，而不是混用旧手工移植与新版 Manager。
- #607 | 问：升到 v0.6.0 后 ZygiskOnKernelSU + LSPosed 安装失败。 | 解决：NOT_PLANNED；维护者给出具体兼容要求：ZygiskOnKernelSU 使用 debug 版。 | 教训：第三方注入框架属于独立兼容层，模块文档必须写清 release/debug 构建要求与支持矩阵。
- #610 | 问：能否用 CVE-2023-20963 等漏洞给手机 root。 | 解决：NOT_PLANNED，无回应。 | 教训：模块/KernelSU 开发不应把公开漏洞利用当安装或授权路径；仅使用解锁 bootloader 与受支持的内核集成流程。
- #612 | 问：保留 root 升级系统后 20 个授权只剩 9 个。 | 解决：NOT_PLANNED；维护者因无日志拒绝修，后续虽补充 bugreport 但未见结论。 | 教训：升级后授权丢失要同时保存升级前后 allowlist/profile、包 UID 与完整 KSU bugreport。
- #613 | 问：缺少 Linux 5.15.41 的镜像/AK3 包。 | 解决：NOT_PLANNED；该版本已 deprecated，建议刷同 KMI 的其它版本。 | 教训：模块发布与安装器应按 KMI/兼容 ABI 选包，不要只锁死精确旧 patchlevel。
- #615 | 问：`setup.sh` 下载失败，浏览器保存后又因 CRLF/`setup.py` 名称而报 shell 错。 | 解决：COMPLETED，但无评论/PR，未证实关闭。 | 教训：构建文档须明确脚本是 `.sh`、用 Unix LF、保留可执行方式，并区分网络/DNS 问题与脚本语法问题。
- #617 | 问：AdAway 有 root 仍不能同步 hosts，且授权似会消失。 | 解决：评论建议 systemless hosts 模块并指向 #355，但提问者称无效；没有维护者确认的最终修复，未证实关闭。 | 教训：hosts 类模块需同时验证 overlay 是否挂载、AdAway 写入目标和授权持久化，不能只给一个 zip 即宣称解决。
- #619 | 问：希望像 Magisk App 一样提供模块更新检查。 | 解决：NOT_PLANNED，维护者指向 #469，另一评论标为 duplicate。 | 教训：功能请求应先找已有 issue/协议；模块可实现自己的 `updateJson`，不要依赖重复的 Manager 功能请求。
- #620 | 问：S22 Ultra 自编 KSU 内核启动 Manager 后冻结重启。 | 解决：评论指向 #263 的 Samsung 构建建议，提问者确认“all works right now”（COMPLETED）。 | 教训：设备内核集成应复用已验证的厂商专项配置/指南，并让报告者确认修复后再关闭。
- #621 | 问：非 GKI 小米 MIX2S 升 10981 后 `su`、授权和模块都消失。 | 解决：COMPLETED，但评论只含 bugreport、旧版本链接和另一个 bootloop，未见维护者诊断；未证实关闭。 | 教训：内核升级前导出模块/授权状态，并先在同一非 GKI 移植分支验证持久化与启动。
- #623 | 问：ChromeOS/Android container 是否能打 kernel patch root。 | 解决：NOT_PLANNED；维护者称未使用过，可自行尝试，评论指向 #637。 | 教训：容器/虚拟化 Android 要按实际宿主内核构建，不能把普通 Android boot 镜像流程直接套入。
- #624 | 问：小米 12 Android 12 刷 Android 12 5.10.168 镜像无法开机。 | 解决：维护者先给兼容镜像链接，后建议先升级系统，并说明 5.10.66 已被 Google 弃用（COMPLETED）。 | 教训：刷入前匹配 Android release、KMI 和厂商系统版本；对已弃用内核先升级系统而非反复降级包。
- #625 | 问：内核已开 OverlayFS，Manager 仍称不支持，模块不能运行。 | 解决：NOT_PLANNED；维护者仅让检索既有 issue，提问者称已按 #263 仍失败，未有明确结论。 | 教训：模块安装器要输出实际 OverlayFS capability/挂载失败原因；配置已开不等于运行时可用。
- #627 | 问：希望 App 内建 GitHub release 检查/下载更新器。 | 解决：COMPLETED，但无评论/PR，未证实关闭。 | 教训：不要据此假定内置更新 API 存在；模块更新应遵循实际 Manager/ksud 支持的 metadata。
- #628 | 问：银行 app 按 `me.weishu.kernelsu` 包名检测，能否改 Manager 包名。 | 解决：NOT_PLANNED；维护者建议不用时卸载 Manager，并确认 root 与模块核心不依赖 Manager；社区改包建议未获官方背书。 | 教训：不要通过重签/改包/二进制替换规避检测；模块功能应独立于 Manager 是否安装，且遵守目标 app 的安全策略。
- #632 | 问：提交 Poco F4/K40S Pixel Experience 设备条目。 | 解决：NOT_PLANNED，维护者转到 #633。 | 教训：设备提交通道出错时按维护者指定的新 issue/PR 流程重提，勿把旧关闭单当已收录。
- #633 | 问：同一设备条目的自动提交。 | 解决：NOT_PLANNED；机器人故障，维护者要求另开新 issue。 | 教训：自动化失败要保留 action 链接并重开可解析的提交，不能假定设备支持已发布。
- #635 | 问：希望备份/恢复 root 授权、模块开关和版本配置。 | 解决：NOT_PLANNED；维护者无此需求，欢迎 PR。 | 教训：备份功能应由需求方实现并明确敏感授权数据的加密、版本兼容和恢复冲突策略。
- #636 | 问：ChromeOS 显示未安装，vmlinux 格式不同如何操作。 | 解决：NOT_PLANNED；评论建议自行编译 `common-android12-5.10-arcvm-gki-base`，维护者指向 #637。 | 教训：vmlinux/ARCVM 路径要从源码重编和验证，不要试图用通用 AnyKernel 直接修补未知容器内核。
- #637 | 问：ChromeOS ARCVM 构建时被 `-Werror=unused-function` 阻断。 | 解决：COMPLETED 的讨论后来有用户称 ChromeOS 已能 root/装模块，但没有针对最初 warning 的 maintainer 补丁/PR，且最后评论仍报告新 ARCVM 版本“Manager 未安装”；未证实官方修复。 | 教训：先让无 KSU 的目标内核可启动，再最小集成 KSU；把 ARCVM、ARC++、CPU 架构和 defconfig 当独立测试矩阵。
- #638 | 问：如何让 overlayfs 的 `/system` 可读写。 | 解决：NOT_PLANNED；维护者建议用模块修改系统文件，若要直接 R/W 可用 Magisk overlayfs 但有取舍，并指向模块文档。 | 教训：模块应通过 systemless overlay 实现目标，不承诺直接 remount `/system`；高风险替代方案须说明基带/兼容风险。
- #642 | 问：旧工具链不支持 C99 `for (int i=...)` 导致编译失败。 | 解决：评论给出改为循环前声明 `int i; for (i=...)`（COMPLETED）。 | 教训：维护旧内核兼容补丁时避免假定 C99；把声明风格和编译器版本纳入 CI。
- #644 | 问：提交 Poco F4/K40S 设备内核。 | 解决：GitHub Actions 自动建立 PR #650（COMPLETED）。 | 教训：设备登记需以自动 PR/合并记录为准，不只看 issue 状态。
- #645 | 问：更新后 system app 不能 root；回退会让所有 system app 都 root。 | 解决：维护者解释 system apps 共用 UID 1000，按 UID 授权时一旦授权即全体生效；无通用细粒度修复证据。 | 教训：不要把 UID 1000 当普通单 app 授权对象；模块/系统 app 设计应避免依赖对共享 system UID 的隔离授权。
- #647 | 问：老 GCC 把 `ERR_PTR` 参数类型告警升级为错误。 | 解决：评论明确改用 `PTR_ERR`（COMPLETED）。 | 教训：内核错误指针 API 必须方向正确；旧工具链下把 warning-as-error 编入移植验证。
- #652 | 问：Root Explorer 改为 Global namespace 后仍只能看自己的 `/data/data`。 | 解决：NOT_PLANNED；维护者无法复现，后来发现特定 App 版本差异，要求提供可复现步骤后重开。 | 教训：namespace 模块/配置问题必须固定 app 版本、Android 版本和验证命令；不要把 GUI 观察当唯一证据。
- #653 | 问：提交 Sony Tama 设备条目。 | 解决：自动机器人无法创建 PR，提示手工 PR；COMPLETED 但未显示合并结果。 | 教训：设备支持应检查实际 PR 是否合并，机器人提示不是发布证明。
- #654 | 问：同一 Sony Tama 设备条目重提。 | 解决：同样只得到“手工 PR”提示（COMPLETED），未证实收录。 | 教训：修正 issue 模板后仍应跟踪 PR；不要重复提交而不补齐自动化需要的字段。
- #655 | 问：root 文件管理器无法把 `/system` remount 为 R/W。 | 解决：NOT_PLANNED；维护者明确不支持这一历史做法，欢迎提出合适 PR。 | 教训：模块必须使用 systemless overlay，不能以 `/system` 直接 R/W 作为兼容前提。
- #657 | 问：#563 的性能优化令老内核出现未定义 hook/`__maybe_unused` 等编译问题。 | 解决：NOT_PLANNED；维护者称更新文档已有解决方式，未给直接补丁。 | 教训：手工移植必须逐版阅读更新集成文档、补齐所有 hook 定义/Makefile，不可只 cherry-pick 部分优化。
- #664 | 问：在 4.14 上手开“给 kworker 安装 init keyring” workaround 后加载 allowlist Oops。 | 解决：NOT_PLANNED；维护者要求加日志定位，因无反馈关闭；后来用户称 0.6.6 不可复现、可能被 `a83390b` 修复，仍非确定证据。 | 教训：不要未经验证启用内核 workaround；保留 call trace、最小补丁与版本 bisect，避免把“可能修好”当发布保证。

## 237–260

- #668 | 问：ChromeOS ARCVM 中部分调用 `/system/bin/sh` 的已授权 app 和 `adb shell` 仍不能用 root。 | 解决：NOT_PLANNED；讨论转为 Brunch/ChromeOS 编译配置，维护者要求移到 Discussions 并锁定、称仍想解决请另开可聚焦 issue，未证明 root 问题已修。 | 教训：ARCVM/ChromeOS 应单列内核、框架、Android 容器与构建配置的复现矩阵；不要把跑题的构建讨论当 root 修复。
- #669 | 问：旧内核手工集成链接时缺 `ksu_vfs_read_hook`、`ksu_execveat_hook`、`ksu_input_hook`。 | 解决：NOT_PLANNED；社区先建议关 KPROBES/看手工指南，维护者实际要求 `cd KernelSU && git pull`、确认最新完整代码与变量/hook 都存在；无报告者最终成功确认。 | 教训：手工移植须同步完整 drivers/Kconfig/Makefile/符号与当前指南；`-fPIC` 不能修复未定义的 KSU 符号。
- #671 | 问：担心 `uname`/内核版本里的 `KernelSU` 字符串被检测。 | 解决：NOT_PLANNED；维护者说官方 KernelSU 不改该字符串，推断是下游内核维护者所加。 | 教训：模块文档不要把下游 kernel branding 归因于官方 KSU；先检查实际 kernel tree/构建脚本。
- #672 | 问：安装模块报 `no space left on device`。 | 解决：维护者索取模块并给测试 Actions 构建，报告者确认修复了大模块安装（COMPLETED）；无关联 PR 可核对。 | 教训：安装器容量 bug 要用原始 zip/实际大小复现，并由报告者验证候选构建后再关闭。
- #673 | 问：GCC 4.9 等旧工具链编译 KSU 报 C99 循环/类型错误。 | 解决：NOT_PLANNED；维护者仅要求仔细读清晰的日志，未给补丁。 | 教训：老内核需采用兼容工具链或自行适配源码；warning/error 是实际集成/工具链信号，不能靠关闭 issue 消除。
- #680 | 问：小米 12S Pro 装 KSU 和多个模块后 120W 充电反复断连。 | 解决：NOT_PLANNED，无评论、日志或修复证据。 | 教训：硬件回归先以无 KSU/无模块基线和逐模块二分隔离，收集电源日志后才可归因。
- #683 | 问：Redmi K40G 4.14.186 从 KSU 0.6.0 手工升级至 0.6.1 后屏幕闪烁。 | 解决：NOT_PLANNED；维护者要求 clean build、日志、revert/bisect，并提醒手工集成文档已变；报告者缩小到 `bd8434f` 后仍无正式修复。 | 教训：集成架构变更后须按最新版指南重做、清理输出并禁用模块测试，再用 commit bisect 定位。
- #684 | 问：设备伪装模块导致基带丢失；安全模式正常、卸载模块后仍异常。 | 解决：NOT_PLANNED；维护者判为模块问题，建议删 `/data/adb/ksu`，报告者随后称已解决。 | 教训：设备伪装类模块风险很高；用安全模式/最小状态隔离，并把清理 KSU 模块状态作为最后的可恢复手段。
- #685 | 问：加密 shell 脚本模块在 KSU 的 `sh` 下不能执行、而 Magisk 可以。 | 解决：NOT_PLANNED；维护者以加密方式未知、Magisk 专有环境且无日志为由未支持。 | 教训：模块应提供可审计、可移植的明文 POSIX shell，或明确解释器/运行时依赖；不可假定 Magisk 特性存在于 KSU。
- #688 | 问：4.19 手工集成后启用部分 KSU SELinux 规则即卡启动。 | 解决：NOT_PLANNED；维护者指出未见完整 KSU 修改、文档已更新，要求 pstore 和逐提交 revert；后续有人称本地编译可开机而 GHA 构建卡死，未有可证实官方修复。 | 教训：不要任意打开 SELinux policy 注入规则；严格按当前集成指南，保留 pstore 并用本地/GHA 和 commit bisect 隔离差异。
- #690 | 问：没有厂商源码的非 GKI MTK 设备能否像 Magisk 一样直接 patch `boot.img`。 | 解决：NOT_PLANNED；维护者明确 GKI 无需厂商源码，非 GKI 无源码不可能，建议用 Magisk。 | 教训：KSU 内核集成的前提是 GKI 或可改的非 GKI 内核源码；模块不能把 boot-image patch 当普适安装路径。
- #693 | 问：超级用户授权页一直转圈、模块页又提示不支持 OverlayFS。 | 解决：NOT_PLANNED；维护者只要求搜索既有 #263；后来讨论核对 `CONFIG_KSU`、drivers Makefile 和编译日志，未有可验证结论。 | 教训：遇到 Manager/OverlayFS 双故障先确认 KSU 实际参与编译、Kconfig/Makefile 与运行时能力，不能只凭 APK 页面判断。
- #697 | 问：三星 A33 刷官方预编译 GKI 镜像后 MicroSD 不工作。 | 解决：评论归因三星专有 sdFAT 驱动不在 Google common kernel；报告者接受需自编内核（COMPLETED），不是 KSU 模块修复。 | 教训：预编译通用内核不能保证厂商专有硬件；模块发布/测试应以设备原始内核驱动栈为兼容基线。
- #700 | 问：能否像 Magisk 一样把 Manager 改随机名以躲避银行 app 检测。 | 解决：NOT_PLANNED，维护者直接标为 #628 重复。 | 教训：不要把改包名/隐藏管理器当 KSU 模块功能或检测规避方案；遵守目标 app 的安全策略。
- #701 | 问：能否给 Manager APK 加 `armeabi-v7a` 支持。 | 解决：NOT_PLANNED；维护者明确拒绝，称该 ABI 已不再支持。 | 教训：模块/管理器须先校验设备 ABI；不能假定 arm32-binder64 的 arm64 内核意味着 Manager 可运行 ARM32 native 库。
- #705 | 问：如何改 issue 模板以得到足够的 bug 信息。 | 解决：关联 PR #715（已合并）更新 feature-request 模板；维护者强调 bug 最关键是 Manager 生成的 bugreport，必要时可自动关闭无效单。 | 教训：模块问题报告模板应要求可导出的完整 bugreport、版本和复现步骤，而不是以截图/口述代替可分析证据。
- #706 | 问：提交 `c0066b6` 后 4.14 编译因 `init_task` 未声明失败。 | 解决：报告者称加入 `#include <linux/init_task.h>` 可编过；COMPLETED 但无维护者回复、关联 PR 或确认，未证实官方修复。 | 教训：移植兼容补丁要核实目标内核头文件/API，而不能把单设备 include workaround 当上游通用方案。
- #709 | 问：提交 `dfc2a86` 后链接缺 `task_lock`/`task_unlock`。 | 解决：NOT_PLANNED；唯一回复指向相同的 #708，报告者的加 `linux/sched/task.h` 自救未获维护者验证。 | 教训：先查关联/重复 issue 并按完整兼容补丁应用；仅补 include 可能掩盖旧内核导出符号差异。
- #713 | 问：能否支持 Magisk `overlay.d`，从 AnyKernel/recovery 注入早期 `init.rc`、二进制和 helper module。 | 解决：NOT_PLANNED；维护者说明 KSU 不改 ramdisk、任意 rc 注入跨内核且复杂，因无人开发关闭；并解释 recovery 写 `/data/adb/modules` 会被系统启动后的 KSU ext4 挂载覆盖，Android 内可尝试 `ksud module install <ZIP>`，但 recovery 安装从未受支持。 | 教训：KSU 模块应走正常 zip/Android 安装与 `post-fs-data` 生命周期，不依赖 Magisk `overlay.d`、early-init 或 recovery 直写模块目录；早期 init 能力须另做受支持设计。
- #714 | 问：Exynos 4.19 为让 KSU 启动要启哪些内核配置。 | 解决：NOT_PLANNED；维护者确认原内核可启动后，要求按 non-GKI 指南且不要用 kprobe，报告者确认 backport 路径需关 KPROBES。 | 教训：非 GKI/厂商内核要选定 kprobe 或 backport 集成路径；不要混用，配置应以当前官方指南为准。
- #719 | 问：ANXCamera Magisk 模块在 KSU 下安装成功却崩溃。 | 解决：NOT_PLANNED；维护者先让关默认卸载模块、授 root 和上传 bugreport，再由 `dlopen`/SELinux 日志定位缺 `/vendor/lib64/libmialgoengine.so`，判为模块问题，未给 KSU 修复。 | 教训：移植 Magisk 模块须验证每个 vendor 库、挂载路径、SELinux context 和 linker namespace；用 `adb shell` + `su` 抓可复现日志。
- #721 | 问：装过约 350MB 大模块后再装其它模块会失败。 | 解决：维护者定位 ext4 镜像块大小把 4096 误当 1K，要求测试新版本；报告者最终清掉 `/data/adb/ksu`、重启后成功，未见关联 PR 证明永久修复。 | 教训：模块安装器必须正确读取文件系统 block size、处理扩容/遗留状态；大模块回归测试应覆盖连续安装、清理和重启。
- #723 | 问：Manager 刷模块时 `set -x` 的调试输出不能按 Magisk 方式保存。 | 解决：评论给出 `exec 2>/sdcard/Download/$MODID-debug.log; set -x` 或 `exec 2>&1` 的 workaround；后者会显示到 Manager，未证明达成所求日志行为。 | 教训：模块安装脚本要显式重定向 stdout/stderr/xtrace，并在真机确认 Manager 实际采集哪一路输出。
- #726 | 问：模块的 `system/vendor` 被移到根 `vendor`，且 `/system/vendor` 指向 `/vendor`，是否为打包 bug。 | 解决：维护者确认这是 OverlayFS 对独立 vendor 分区的预期处理：保留 `$MODPATH/system/vendor` 会遮住真实目录，故移为 `$MODPATH/vendor`；无行为变更承诺。 | 教训：模块不能假定 Magisk 式固定目录；应处理 `$MODPATH/vendor`/其它独立分区，并用实际挂载结构测试脚本与文件路径。

## 261–297

- #727 | 问：即使对 app 卸载全部模块，app 仍检测 root（同时使用 Zygisk on KernelSU）。 | 解决：NOT_PLANNED；回复称无法复现、全局 umount 不是万能隐藏，判为 Zygisk 模块问题。 | 教训：模块卸载视图不等于完整 root/注入痕迹隐藏；应按具体 Zygisk/模块层单独复现，不能承诺检测规避。
- #728 | 问：安装模块后不返回、切换底部导航再回模块页会重复安装已选 zip。 | 解决：COMPLETED，但无评论、PR 或报告者确认，未证实具体修复。 | 教训：Manager/安装器流程要在任务完成后清空 picker 状态、禁止重复提交；模块作者也应使重装操作幂等。
- #729 | 问：K60e Android 14 刷 5.10.136 后 Wi‑Fi 不能开。 | 解决：NOT_PLANNED；题干只有求助，无日志/评论/修复证据。 | 教训：设备内核回归必须给原厂与目标内核、无线驱动/固件和日志；不能从一个版本号推断 KSU 兼容性。
- #730 | 问：Manager v0.6.2 提示内核版本不够，五位数版本号是什么意思。 | 解决：NOT_PLANNED；没有可用日志或维护者技术诊断，未证实关闭。 | 教训：发布模块前应把 Manager、内核 KSU version 与构建来源一起记录；仅描述“版本不够”无法定位兼容矩阵。
- #731 | 问：连续装 4–5 个模块后分区损坏、重启模块全失，日志为 `resource busy`。 | 解决：NOT_PLANNED；维护者要求安装日志和 bugreport，未收到证据。 | 教训：连续安装要串行化并在失败时保留旧镜像/事务状态；报告必须附每次安装日志和完整 bugreport。
- #733 | 问：在 ARM64 Ubuntu 上如何编译 userspace Rust `ksud`，交叉编译报 `Exec format error`。 | 解决：NOT_PLANNED；社区指 `ksud.yml`，维护者要求参照 CI；未给最终命令。 | 教训：交叉构建要使用与 host 架构匹配的 NDK prebuilt（题干误用了 `linux-x86_64` clang）并复用 CI 的 target/环境设置。
- #735 | 问：指定旧 tag（如 v0.5.2）运行 setup 脚本后 KSU 未参与内核编译。 | 解决：NOT_PLANNED；维护者称与项目无关，无调试或补丁。 | 教训：固定旧 tag 时应自行核验 `CONFIG_KSU`、drivers 链接、Kconfig/Makefile 和构建日志；脚本成功输出不等于目标已编入。
- #736 | 问：v0.6.0 的 `su` 命令参数解析异常。 | 解决：维护者直接给出修复提交 `df9cf61`（COMPLETED）。 | 教训：模块调用 `su` 应按目标 KSU 版本回归参数解析；遇到 CLI 行为差异优先锁定已修提交/版本。
- #738 | 问：能否从 `adb shell` 安装/卸载模块。 | 解决：COMPLETED；提问者称已找到方法，但存档未保留实际命令或维护者答复，不能据此确认 API。 | 教训：CLI 安装接口必须以当前官方文档/`ksud` help 为准，不要从关闭状态或转述猜命令。
- #739 | 问：自编 Manager 报“signature is not right”，怎样把 APK 签名加入 boot/kernel。 | 解决：维护者指向 kernel Makefile 第 35 行，提问者致谢（COMPLETED）。 | 教训：自编 Manager 的签名标识必须与编入内核的预期签名一致；改 APK 签名后需连同 kernel build config 一起重建。
- #740 | 问：能否像 Magisk 一样把模块卸载脚本延后到下次开机，以避免当前会话状态不一致。 | 解决：维护者拒绝：完全卸载 Magisk/KSU 后下次开机可能无法运行 `uninstall.sh`，因此须立即执行；要求模块设计成系统已启动后也可安全卸载。 | 教训：`uninstall.sh` 必须幂等、立即安全，清理模块外资源时不能依赖“下次开机一定会执行”。
- #742 | 问：怎样安装 BusyBox；尝试 Magisk 模块后卡开机或无效。 | 解决：NOT_PLANNED，无评论或可核对建议。 | 教训：不要把任意 Magisk BusyBox 模块当 KSU 兼容；先确认 KSU 自带/所需命令和模块的安装脚本假设。
- #743 | 问：同名 BusyBox 安装问题。 | 解决：COMPLETED，但题干和评论均无实质内容，未证实关闭。 | 教训：空白关闭单不能作为 BusyBox 模块制作依据；以实际二进制、ABI 与脚本依赖测试为准。
- #744 | 问：请求上传适配 v0.6.2 的新 WSA 5.15.104.1 内核。 | 解决：维护者指 Actions artifact 并确认已上传，报告者确认（COMPLETED）。 | 教训：WSA/预编译内核与 Manager/KSU 版本应一起发布且给出可追溯 artifact，模块测试应锁定该组合。
- #745 | 问：自编 boot 和 Manager、改过 signature id 后仍显示 OverlayFS 不可用、授权页一直加载。 | 解决：NOT_PLANNED；维护者强调一个内核只接受一个签名，要求检查 build config/源码；报告者后来称已找到问题但未披露具体原因。 | 教训：自定义 Manager 要全链路校验单一签名、APK 打包 native 库与 kernel config；“签名一致”的口头判断不足以替代日志。
- #746 | 问：模块 `customize.sh` 配 `SKIPUNZIP=1` 后解包文件重启即消失。 | 解决：报告者将写入目录从 `modules` 改为 `modules_update` 后自称解决（COMPLETED）。 | 教训：安装阶段的 `$MODPATH` 应指 KSU 的 staging `modules_update`，而不是已挂载的运行时 `modules`；自定义安装脚本要在重启后验证镜像内容。
- #751 | 问：Magisk 模块目录中的 `.replace` 和 KSU `REPLACE` 为何行为不同、同目录文件不出现。 | 解决：维护者说明 KSU 永不创建 `.replace`，功能目标相同但实现不相同（COMPLETED）。 | 教训：移植时不要用 Magisk sentinel 文件判断 KSU 行为；按 KSU 的 `REPLACE` 合约测试实际覆盖结果。
- #752 | 问：自编 Manager 缺少 `libksud.so`，显示 OverlayFS 不可用。 | 解决：NOT_PLANNED；维护者仅要求看 CI 构建脚本，未有报告者最终确认。 | 教训：Manager 打包必须复用 CI 的 native-library/ABI 步骤，APK 能生成不代表 `libksud.so` 已被正确纳入。
- #756 | 问：三星 S23 Ultra 刷预编译 KSU 后耳机无声。 | 解决：维护者指 #697，建议改用 Samsung 官方源码自编内核；无直接修复确认。 | 教训：通用 GKI 缺厂商音频/存储等驱动时，模块无从补救；必须以厂商内核源码构建测试。
- #758 | 问：连续安装多个模块后页面只显示第一个，重启后才全显示。 | 解决：维护者建议下拉刷新，报告者承认即可（COMPLETED）。 | 教训：模块列表 UI 与实际安装状态要区分；自动化/用户排错先刷新并用 CLI/重启验证，不要把缓存显示当安装失败。
- #759 | 问：可否对单个 app 只卸载部分模块，而不是全部或不卸载。 | 解决：NOT_PLANNED；维护者认为隐藏是全有或全无，称可接受 PR，但无实现。 | 教训：当前模块设计不能依赖 per-module app unmount；若需此能力须自行提出完整实现和隐藏语义。
- #760 | 问：能否内建兼容 Zygisk 的 zygote 注入 API，以减少检测痕迹。 | 解决：NOT_PLANNED；维护者明确“不计划”。 | 教训：不要把 Zygisk 注入/隐藏能力列为 KSU 模块平台保证；第三方注入层须独立声明兼容和风险。
- #764 | 问：`android13-5.15.74_2023-02` 镜像可启动却不能给 app 授 root，旧镜像正常。 | 解决：维护者要求更新 Manager；报告者表示会试，但存档无最终确认。 | 教训：排错先升级/匹配 Manager 与内核版本，随后再判断内核回归；本条不能证明仅升级即可修复。
- #765 | 问：OEM 内核缺被 kprobe 的函数，建议 `EXPORT_SYMBOL_GPL` 让 kprobe 注册可见。 | 解决：维护者认为上游破坏性变化才是根因、GKI 的 kprobe 必须正常，且非法 symbol 注册本身不应 bootloop；未见文档/代码变更。 | 教训：kprobe 移植需以目标 kernel 的真实符号和 non-GKI 指南验证，不能把导出符号当通用修复或把 bootloop简单归咎 kprobe。
- #766 | 问：能否像 Magisk 一样备份/恢复原厂 boot 以方便 OTA。 | 解决：NOT_PLANNED；维护者称无个人需求，知道原理者应提 PR，评论给了外部工具路线。 | 教训：镜像备份/OTA 是独立高风险工作流；模块不要假定 Manager 负责，需明确 slot、校验和恢复策略。
- #767 | 问：能否给旧 app 提供可写 `/system` 等分区。 | 解决：NOT_PLANNED；维护者允许 draft PR 但定位为无 UI 的高级实验功能，手工创建镜像、文档警告、不保证工作且相关 issue 会直接关，优先模块。 | 教训：模块应优先 systemless overlay；若实验 System RW，必须显式 opt-in、无官方兼容承诺并做好可恢复设计。
- #768 | 问：已授权 `adb shell` 执行 `/proc/mounts` 失败，且 `/prism` 不能 remount R/W。 | 解决：NOT_PLANNED；维护者判定与 KSU 无关。 | 教训：分区挂载/命令路径问题要先核实命令、SELinux 和设备 fstab；不要把每个 root shell 失败自动归因模块框架。
- #769 | 问：`adb shell` 里找不到 `su`，Termux 却能用。 | 解决：评论指出须在 Manager 给 `com.android.shell` 授权（COMPLETED）。 | 教训：自动化调试前先授权 Android shell 包；电脑端 `adb` 不会继承 Termux 的 root 授权。
- #770 | 问：编 Android common kernel + KSU 时 ksymtab/symbol list 不一致。 | 解决：NOT_PLANNED；维护者称是内核构建问题、要求参考 CI，无修复证据。 | 教训：修改 KSU 后仍须按目标 common-kernel CI 的 symbol-list/LTO 流程全量构建；不能只复用旧命令判断归因。
- #775 | 问：把 `/data/adb/ksu/bin/` 加 PATH 后 `which busybox` 多一个 `/`，字符串比较失败。 | 解决：维护者认为多 `/` 不影响功能；报告者承认只影响不稳健的字符串比较（COMPLETED）。 | 教训：脚本应按可执行性/规范化路径而非 `which` 的字面字符串判断，容忍 POSIX 路径中的重复斜杠。
- #776 | 问：翻译代码的 CC BY-NC 与 GPLv3 许可是否兼容。 | 解决：维护者称非律师、若知道修复请 PR；未有法律结论或变更，COMPLETED 未证实实质解决。 | 教训：模块引入第三方代码/资源前要自行做许可证兼容审查和署名，不可把 issue 关闭当许可批准。
- #780 | 问：不再使用 KSU 时怎样卸载。 | 解决：维护者给出删除 `/data/adb/ksu*` 并刷回原厂 `boot.img`，并确认 `/data/adb/ksud` 也相关（COMPLETED）。 | 教训：卸载内核级 root 必须同时清理用户态状态和恢复原厂 boot；操作前备份、确认目标分区，模块作者不可假设单删 APK 即可卸载。
- #781 | 问：DriveDroid 不能访问外置 SD 根目录，而 Magisk 可以。 | 解决：NOT_PLANNED；维护者要求最新 KSU 与 bugreport，后续使用者仍报告最新版存在，未见诊断修复。 | 教训：外置存储/namespace 兼容问题要用当前版本、完整 bugreport 和设备路径复现；不能声称 KSU 与 Magisk 行为相同。
- #782 | 问：能否让 KSU 的 `su` 也暴露在 recovery 自定义路径（如 `/gearlock/bin/su`）。 | 解决：NOT_PLANNED；维护者以普通用户 overhead 为由拒绝，建议 recovery 自行放 su 并用 SUID。 | 教训：模块/恢复环境不能依赖 KSU 可配置 PATH；应自带受控的 recovery root 工具与权限方案。
- #784 | 问：刷模块偶发 error 16 后重启会清空旧模块，只剩新模块。 | 解决：NOT_PLANNED；维护者因不能复现且无日志关闭，欢迎附 bugreport 重开；后续同类描述仍未复现。 | 教训：安装更新必须原子化并保留旧 modules image；遇到 error 16 立即保存 bugreport/镜像，勿继续安装覆盖现场。
- #786 | 问：请求支持黑鲨 5 Pro；尝试 patch 官方 boot 后系统加载但进不了用户界面。 | 解决：NOT_PLANNED，无评论/修复证据。 | 教训：设备支持请求至少应给可复现源码、boot/分区细节和启动日志；无源码设备不能把 AnyKernel 试刷当 KSU 支持证明。
- #787 | 问：Mi 10 Ultra KSU version 10704 被新版 Manager 拒绝（要求至少 11701），旧 Manager 可用。 | 解决：维护者要求更新 KernelSU source，报告者确认完成（COMPLETED）。 | 教训：Manager 的最低内核版本是硬兼容门槛；构建模块/内核时要同步更新 KSU 源码，不能靠降级 Manager 长期规避。

## 298–334

- #795 | 问：能否让 `ksud` 挂载/卸载 KSU OverlayFS、临时改 system 并自动生成模块。 | 解决：NOT_PLANNED；仅有社区提到 debug 已有 mount，未见维护者回应或实现。 | 教训：模块不能依赖公开的 OverlayFS mount/unmount API 或运行时改 `/system`；应按安装期 systemless overlay 设计。
- #796 | 问：Android 9 自编内核下 Manager 不能读取 app 列表。 | 解决：NOT_PLANNED；社区称 KSU 最低 Android 10，SELinux permissive 时能加载说明需要自行适配 Android 9；无官方修复。 | 教训：模块/Manager 要声明最低 Android/SELinux 支持范围；旧系统兼容属于内核与政策适配，不能凭 root 已工作推断 UI/allowlist 都可用。
- #798 | 问：空的 `$MOD/system/vendor` 重启后使 `/system/vendor` 为空，怀疑 OverlayFS 文档/opaque 属性错误。 | 解决：维护者索要详细复现，报告者随后认为未弄清原因、可能不是所述问题；COMPLETED 未证实有修复。 | 教训：对分区 overlay 先做最小可复现和挂载/目录树检查；空目录也可能遮蔽 lowerdir，不能在未验证前把现象写成平台 bug。
- #800 | 问：三星 A33 5G 刷 KSU 后蓝牙通话/音频不工作。 | 解决：NOT_PLANNED，维护者直接关联 #697 的预编译 GKI 缺厂商驱动解释，未有本机修复。 | 教训：蓝牙等 OEM 硬件功能应由匹配的厂商内核保证；模块不能修复预编译 common kernel 缺失的驱动。
- #801 | 问：空白“thanks” issue。 | 解决：NOT_PLANNED；维护者只说搜索 issues，没有可提取问题或答案。 | 教训：空白/无症状单不能作为模块兼容案例；问题报告必须写清操作、版本和日志。
- #804 | 问：自编 KSU Manager 虽可 root 却不能读写 `/data/data`，官方/其它 app 正常。 | 解决：NOT_PLANNED；社区根据构建 CI 推测漏编 `ksud`，无报告者确认。 | 教训：自编 Manager 不仅要改签名，必须把 `ksud` 等 native 组件按 CI 打入并测试 data namespace；不能用 APK 能启动代替功能验证。
- #806 | 问：授权 app 每次重启后 root profile 均丢失。 | 解决：NOT_PLANNED；维护者判为已有问题，指向 #436、#419、#276，未在本单给出最终方案。 | 教训：授权持久化丢失不是设计预期；应追踪对应内核/allowlist 持久化 issue，记录 encrypted data、UID 和重启日志，而非重复新建模糊单。
- #807 | 问：开发 KSU 内核时怎样避免每次全量 GKI 编译。 | 解决：社区建议应用对应内核版本的 `.github/patches`，并以 `CCACHE="/usr/bin/ccache" LTO=thin BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh` 构建，提问者致谢（COMPLETED）。 | 教训：模块/内核开发可用 ccache 与匹配 patch 加速，但缓存不替代干净构建/刷机回归。
- #810 | 问：旧内核保存 allowlist 报 `create file failed: -126`，修改 keyring 判断仍无效。 | 解决：NOT_PLANNED；维护者只指向 #436，未在本单验证修复。 | 教训：allowlist 写入失败要按既有旧内核/keyring 兼容路径排查，保留准确 errno 与版本；不要盲改版本比较条件。
- #813 | 问：NetHunter chroot 的 dbus 因 capability drop `operation not permitted` 无法启动。 | 解决：NOT_PLANNED；维护者不认定为 KSU 问题，报告者后来发现 KSU root 的 IAB/capability 全置位，清空 `capsh --iab=""` 可绕过，未获官方补丁。 | 教训：chroot 服务并非“capability 越多越好”；模块启动脚本要测试 capability/ambient/bounding 集合并给受限环境 workaround。
- #815 | 问：模块安装日志不自动滚动、音量键菜单不能正常交互，能否增强 Manager。 | 解决：COMPLETED，但无评论、PR 或上线证据，未证实实现。 | 教训：安装脚本不要依赖音量键或快速刷屏的交互菜单；提供明确默认值、文本提示和非交互路径。
- #817 | 问：手工造的模块镜像含无 SELinux label 文件会 post-fs-data 重启并删掉 `.img`。 | 解决：维护者复核后解释这是保护/回滚机制：unlabeled `/system/etc` overlay 会阻止系统启动；正常 Manager 或 `ksud module install` 会自动设 context，手装必须自己处理。 | 教训：手工模块必须在 staging 镜像里正确 `chcon`，尤其 system/vendor overlay 文件；不要绕过受支持安装器再期待启动时自动修标签。
- #820 | 问：Pixel 6 刷 `android13-5.10.157` 后 Manager 提示 KSU 内核版本低于 11071。 | 解决：NOT_PLANNED；回复称按字面更新内核，后续相似自编 QGKI 报告无本单解决。 | 教训：刷对 Linux/KMI 镜像不代表含足够新的 KSU；构建发布须核验编入的 KSU version，而非只看 kernel release 名称。
- #823 | 问：GKI GitHub workflow 是否应让 `CONFIG_KSU` 成为可选而非强制编入。 | 解决：NOT_PLANNED；维护者称“不重要”，未改 workflow。 | 教训：不要依赖官方 CI 提供 KSU 可选开关；分发自定义内核时由自己的 defconfig/构建矩阵明确是否启用。
- #824 | 问：4.9 手工/kprobe 集成后模块开关灰、重启无效，且 MTP/ADB 异常。 | 解决：COMPLETED；社区建议应用特定 4.9 patch/提交，但报告者称已有对应修改仍未解决，未见维护者最终确认。 | 教训：旧内核“su 正常”不代表 ksud/module 生命周期完整；需逐项验证 hook、SELinux、模块镜像和 USB，并以可复现 patch 集合为准。
- #828 | 问：每次重启已授权 root 都重置，KPROBES 已关闭。 | 解决：NOT_PLANNED；维护者只要求先搜索 issue，未给具体修复。 | 教训：重复的 allowlist 持久化 bug 应先关联旧单并附 bugreport；关闭 kprobe 本身不能证明存储/SELinux 路径正确。
- #830 | 问：安装 ZygiskOnKernelSU 后分区变只读，想继续 R/W。 | 解决：NOT_PLANNED；维护者明确 `Won't fix`，并有评论指 Google anti-write protection。 | 教训：KSU 不提供传统 system R/W；模块要用 overlay/systemless 文件替换，不应要求注入框架保持真实分区可写。
- #834 | 问：刷 KSU version 低于 168 的内核会令部分设置（指纹/人脸/密码）闪退。 | 解决：NOT_PLANNED；无评论、日志或修复证据。 | 教训：版本回归至少要提供 crash log、精确 KSU/kernel/ROM 组合；不能把“低于某数”当通用模块兼容结论。
- #836 | 问：注册 Manager 时对未分配用户页 `copy_from_user` 的可观察差异可能被用作 KSU 探测，建议先检查页分配。 | 解决：COMPLETED，但无维护者回复、关联 PR 或修复确认，未证实落地。 | 教训：涉及 Manager 注册的模块/工具不能依赖内核 copy-from-user 的页副作用；安全检测改动要有代码审计和回归证据。
- #838 | 问：WSA 安装含 233MB APK 的模块后系统无法重启。 | 解决：报告者后来确认并非大小，而是 RRO overlay APK 的 SELinux context；在 `vendor/overlay` 用 `chcon u:object_r:vendor_overlay_file:s0 overlay-webview-packages.apk` 后定位（COMPLETED）。 | 教训：大模块失败先区分容量与 label；vendor overlay APK 必须使用正确的 `vendor_overlay_file` context。
- #839 | 问：请求 Android 14 `android14-5.15` GKI 构建 CI。 | 解决：关联 PR #1057 “Add Android 14 kernel ci” 已合并（COMPLETED）；讨论还澄清 Android 系统版本不必等于 kernel branch。 | 教训：模块/内核选包应依据实际 KMI/kernel branch，不按 ROM 的 Android marketing version 猜；新分支应有独立 CI。
- #840 | 问：为何 `v0.6.5` (11182) 被撤下，旧 0.6.2 内核搭配它失效。 | 解决：维护者说明 0.6.5 在 4.x 内核有致命问题，已修并发布 0.6.6（COMPLETED）。 | 教训：旧内核用户必须关注 release 撤回/热修信息；不要固定使用已撤标签。
- #843 | 问：msm-4.9、关 KPROBES 后，提交 `7bdb885` 编译缺 `get_fs`/`set_fs`/`pagefault_*`。 | 解决：维护者指向 #841；COMPLETED 但本单未保留修补内容或确认。 | 教训：4.9 兼容错误须按关联 issue 的完整版本适配，不能只根据关闭状态复制零散 include/符号替换。
- #845 | 问：non-GKI + Clang 15 编 v0.6.6，`USER_DS` 未声明。 | 解决：报告者发现 main 分支已有修复 PR，维护者称等反馈后再发版本；COMPLETED，但无本单提交号。 | 教训：编译兼容性修复先用明确的上游提交/后续 release 验证，不要长期固定在有已知缺陷的 tag。
- #846 | 问：app 提权成功时是否可按 app 弹 Toast。 | 解决：NOT_PLANNED；维护者因无人愿开发关闭。 | 教训：模块不可依赖 Manager 提权成功通知 UI；应用自身应按 `su` 返回值处理失败/成功。
- #847 | 问：提交 Redmi Note 12 Pro/Pro+ MIUI 14 设备内核。 | 解决：GitHub Actions 自动创建 PR #848（COMPLETED），无合并记录。 | 教训：设备/模块兼容条目以实际 PR 合并和可访问源码为准，自动建 PR 不等于已支持。
- #849 | 问：提交 Xiaomi Mi 8 Pro/Explorer Edition 设备内核。 | 解决：机器人自动创建 PR #850（COMPLETED），无合并记录。 | 教训：同样应跟踪后续评审/合并，不能把 issue 完成状态当发布承诺。
- #851 | 问：装 Zygisk、LSPosed、USNF 后 X-plore/AdAway 等 root app 不能改文件。 | 解决：NOT_PLANNED；维护者明确 KSU 不支持 `/system` R/W，建议用模块修改。 | 教训：需要替换 hosts/system 文件的功能应做模块 overlay；不要让 app 直接 remount system 成为模块前提。
- #852 | 问：安装显示 `mount failed`/`resource busy`、更新卸载重启后不生效。 | 解决：NOT_PLANNED；维护者先指出日志显示 YouTube 模块实际装成，后要求保存安装日志+bugreport并升级 0.6.6；报告者后来称 0.7.6 仍遇到，未见最终修复。 | 教训：不要只看单行 mount warning；比较安装前后 bugreport、保存 Manager 安装日志并先升级，再判断 modules image 事务失败。
- #853 | 问：KSU 是否兼容 NetHunter、是否要替代 Magisk。 | 解决：NOT_PLANNED；维护者称非 NetHunter 用户，不作保证，要求自行尝试。 | 教训：第三方 chroot 生态没有官方兼容承诺；模块须明确所测 NetHunter/ROM/内核组合。
- #854 | 问：能否使 root 授权跨重启保留。 | 解决：NOT_PLANNED；维护者明确这是 bug、应搜索既有 issue，不是正常功能请求。 | 教训：授权重置应按 bug/旧内核适配处理，而不是设计为每次模块重启后手动授权。
- #855 | 问：更新模块时镜像似只增不减，空间小会爆存储。 | 解决：维护者说明 resize 会 trim 但需余量，社区给 `resize2fs -M /data/adb/ksu/modules_update.img` workaround；无永久实现证据。 | 教训：大模块更新要预算旧镜像、staging 和临时镜像空间；手工 shrink 可能影响运行时在模块目录创建/改文件，须谨慎。
- #858 | 问：模块全不能加载、`e2fsck` exit code 8。 | 解决：维护者建议删 `/data/adb/ksu`、重启后重装全部模块，报告者确认有效；后来另一人失败时给测试 APK+要日志（COMPLETED）。 | 教训：损坏 modules state 可通过重建恢复，但这是破坏性操作；先备份模块/日志，重装后逐一验证。
- #859 | 问：NetHunter terminal 进 chroot 报 `su: Untracked pid` 并崩溃。 | 解决：维护者要求 bugreport、建议试关 KPROBE；报告者试后看到 `Required key not available`，判为 NetHunter 问题并自行关闭。 | 教训：Magisk 正常不等于 KSU 必有 bug；chroot 故障需区分 KSU hook、内核 key/LSM 与上游 NetHunter，保留完整日志。
- #861 | 问：CI 镜像缺 AndroidRelease/KmiGeneration version strings。 | 解决：维护者确认最新 CI 已修；评论补充在删 `.repo` 前以 `setlocalversion --save-scmversion` 保留版本信息的实现思路（COMPLETED）。 | 教训：模块/内核 artifact 的版本字符串必须在清理源码元数据前固化，并在 CI 中断言。
- #867 | 问：TWRP 下 `./ksud module list` 只返回空列表。 | 解决：NOT_PLANNED；维护者明确 recovery 不受支持；社区给临时救援法：手动挂 `modules.img` 到 `/data/adb/modules`、必要时设 `sys.boot_completed=1` 后用 ksud，结束后卸载。 | 教训：recovery 管理模块不是官方 API；救援前要备份并理解 ext4 挂载状态，日常模块流程不要依赖它。
- #868 | 问：模块 `service.sh` 开机似未执行，手动 echo thermal/swappiness 才生效。 | 解决：维护者从日志证明多个 `service.sh` 已被执行；报告者最终确认是内核接口问题，只能用户层手改。 | 教训：先用 ksud 日志确认脚本是否启动，再检查目标 sysfs/proc 节点是否被驱动/其它服务覆盖；执行不等于效果持久。

## 335–368

- #869 | 问：Huawei Nova 2s 在 Treble ROM 装首个模块后，后续重启会强制重启或关机，EMUI 正常。 | 解决：NOT_PLANNED；维护者称无法理解描述，未给诊断/修复。 | 教训：设备重启问题需给准确触发步骤、pstore/dmesg、ROM 与模块矩阵；“Treble/EMUI 不同”本身不足以定位模块 bug。
- #872 | 问：Huawei 4.9 集成 KSU 后 `__GFP_ZERO` 未定义，强行定义或 include 后会 kernel panic。 | 解决：NOT_PLANNED；社区给华为教程/关闭干扰选项和 `_GFP_ZERO` workaround，但报告者仍称 panic，未有官方可验证解决。 | 教训：极旧厂商内核 API 适配必须逐项验证并保留 boot panic 日志；不要把其他 Kirin 设备的 patch 直接当通用方案。
- #874 | 问：从 Magisk 切 KSU 时旧 `/data/adb/modules` 被静默删除，希望改名备份。 | 解决：NOT_PLANNED；维护者不做兼容代码，表示会在文档加警告、欢迎 PR。 | 教训：迁移到 KSU 前必须人工备份 Magisk 模块目录和数据；安装器不能假设两个模块存储格式可安全共存。
- #875 | 问：modules.img/modules_update.img/update_tmp.img 三份占空间，能否自定义 reserve size/一键 trim。 | 解决：NOT_PLANNED；维护者称临时文件会删除，缩小镜像会令运行期在 `/data/adb/modules` 创建/修改文件失败，不为旧小容量设备增加复杂度。 | 教训：模块应声明安装/更新峰值空间并避免大临时文件；不可盲目 shrink KSU 镜像，否则运行时写入模块会失败。
- #878 | 问：DualShock 4 能配对连接但无输入，多个 KSU kernel 复现。 | 解决：NOT_PLANNED；维护者要求先确认归因，报告者随后确认与 KSU 无关、指向 ROM source issue。 | 教训：外设回归必须和同一 ROM/内核的 KSU 开关做对照；不要以“装过 KSU”替代因果证明。
- #879 | 问：三星 S22 Ultra 不能连续安装模块、偶尔重启后只能装一个。 | 解决：维护者要求更新 v0.6.6，给修复提交 `5f2566e`；报告者称在官方群协助下已修好（COMPLETED）。 | 教训：模块安装故障先升级到含已知修复的版本并提交 bugreport；与维护者共同复现的日志是有效修复证据。
- #880 | 问：B 站 app 无故被授予超级用户，关闭后又出现。 | 解决：NOT_PLANNED；维护者索要 bugreport，因无反馈关闭。 | 教训：意外授权属于安全事件：立即撤销、记录 package/UID/时间并提交 bugreport；无证据不能判定为 KSU 漏洞或模块行为。
- #881 | 问：4.14 编译 SELinux `sepolicy.c` 时结构体字段/指针类型不匹配。 | 解决：NOT_PLANNED；维护者只要求搜索既有 issue，未给本单补丁。 | 教训：SELinux policydb 结构差异必须按目标内核版本使用对应兼容代码/旧 issue，不可直接套 main 分支实现。
- #882 | 问：Release 页面同时显示 v0.6.6 与后发的 v0.6.4，哪个最新。 | 解决：维护者明确 v0.6.6（COMPLETED）。 | 教训：模块/内核依赖应按语义版本和维护者公告选取，不能仅看 GitHub “Latest” 标签时间。
- #883 | 问：小米 13 刷 `android13-5.15.104_2023-06` 后息屏解锁指纹延迟。 | 解决：NOT_PLANNED；维护者建议换其它版本，并称有人报告 Google 6 月版本有问题，未有确认修复。 | 教训：预编译 kernel 回归应测试相邻 release/原厂基线；模块不应承诺修复底层指纹时序。
- #885 | 问：ColorOS 13.1 更新后刷旧 KSU kernel，支付宝崩溃且 SIM 随机掉线。 | 解决：NOT_PLANNED；维护者要求先确认是否真由 KSU 导致，未有后续证据。 | 教训：系统 OTA 后的兼容故障要分离 ROM、kernel、模块和目标 app 版本，先做无 KSU 对照。
- #886 | 问：把 Manager 作为三星 ROM preload app 后显示 KSU 未安装，手工安装正常。 | 解决：NOT_PLANNED；维护者指向 #559，即 system/preload 安装不受支持。 | 教训：Manager 需走受支持的普通用户 APK 安装/路径与签名流程；不要把它预装为 system app 后期待管理/模块功能正常。
- #887 | 问：自编 QGKI 显示 ksud 工作、已授权却不能 root/模块无效。 | 解决：NOT_PLANNED；日志显示 `execve_kp`/`input_handle_event_kp` 为 `-2`，维护者建议 kprobe 异常时改手工集成，未见报告者确认。 | 教训：kprobe hook 注册失败必须视为内核集成失败；切换到完整 manual integration 并验证每个 hook/模块生命周期，不能只看 Manager “工作中”。
- #888 | 问：自编 Manager 因内核签名限制显示未安装，怎样用于学习通信。 | 解决：NOT_PLANNED；维护者只要求先搜索既有 issue。 | 教训：自编 Manager 与 boot 必须同步设置受信任签名；不要以放宽官方预编译镜像安全限制作为学习方案。
- #890 | 问：TWRP 删除模块数据后，模块仍工作却不出现在 Manager 列表。 | 解决：COMPLETED；维护者要求重新启用 logcat、进入模块页后再给 bugreport，未有最终诊断/确认。 | 教训：手动 recovery 清理会使数据库/镜像状态脱节；先恢复日志能力并比对实际模块目录与 Manager 视图。
- #892 | 问：题干仅为“1”。 | 解决：NOT_PLANNED，无评论或事实。 | 教训：无上下文 issue 不能产生模块开发结论；自动化支持流程应拒绝无效输入。
- #894 | 问：提交 Redmi K40 (alioth) 设备内核。 | 解决：机器人自动创建 PR #895（COMPLETED），无合并证据。 | 教训：设备条目必须继续检查 PR 审核/合并和后续 release，而不是只登记 issue。
- #896 | 问：请求 Manager 随机 package name 以避 app 检测。 | 解决：NOT_PLANNED，维护者标为 #628 重复。 | 教训：不要把随机包名/检测规避写入模块需求；遵守安全策略并保持可审计的包签名/身份。
- #897 | 问：OnePlus 6 4.9 手工集成后模块释放文件不存在、卸载后仍残留。 | 解决：NOT_PLANNED；维护者判定所有 ksud 相关未执行，社区指 #824/提交 `646d0c8` 的 4.9 适配，报告者确认应用后正常。 | 教训：模块安装/卸载全失效通常是旧内核 ksud/SELinux 集成不完整，不是单模块脚本问题；用已验证的完整兼容 commit 回归。
- #899 | 问：担心第三方 app 能调用 KSU 给自身授权，建议出自定义 key 版本。 | 解决：NOT_PLANNED；维护者否定前提，并称需要可自行修改。 | 教训：不要基于未证实的权限模型恐慌改 key；定制信任根需理解 Manager 签名校验并自行维护安全更新。
- #901 | 问：能否支持 Linux 3.18.138。 | 解决：NOT_PLANNED；维护者称理论可行，实际须读源码、修编译错误、测试。 | 教训：3.x 级移植没有现成支持承诺；需独立维护 API/SELinux/hook backport 和全量硬件回归。
- #902 | 问：请求支持 ARM（32 位）架构。 | 解决：NOT_PLANNED，无评论或实施证据。 | 教训：不要假设 ARM32 ABI 已受 KSU/Manager 支持；模块发布必须列出实际支持 ABI。
- #903 | 问：仅称 `android12-5.10-lts` 内核已更新。 | 解决：NOT_PLANNED；维护者要求说清具体诉求。 | 教训：版本更新报告要说明希望新增 CI、镜像还是修复，并给 branch/commit/KMI；裸链接不能变成可执行任务。
- #904 | 问：Bazel GKI 构建加入 KSU 后 `savedefconfig` 与 `gki_defconfig` 因 `CONFIG_OVERLAY_FS` 不一致而失败。 | 解决：维护者给出修复提交 `5e893e3`（COMPLETED）；后续讨论 Bazel 启动问题未形成额外证据。 | 教训：Kconfig 新依赖必须同步 canonical defconfig，Bazel 的 config-diff 校验会阻止“仅本地启用”漂移。
- #905 | 问：提交 OnePlus 6/6T（无 retrofit dynamic partitions）设备内核。 | 解决：自动创建 PR #906（COMPLETED），未见合并记录。 | 教训：设备声明要注明分区约束，并以实际 PR/release 验证支持状态。
- #907 | 问：重复提交同一 OnePlus 6/6T 设备内核。 | 解决：自动创建 PR #908（COMPLETED），无合并证据。 | 教训：重复自动 PR 仍需去重并跟踪合并，不能仅凭 issue 状态宣称设备已正式支持。
- #909 | 问：P20 Lite 原厂 EMUI 9.1 中 Manager app 列表无限加载、模块称无 OverlayFS，GSI 正常。 | 解决：社区指 EMUI9 适配提交，把 SELinux 设为始终 permissive 后报告者称解决；不是维护者正式安全方案。 | 教训：旧 EMUI 的 SELinux 适配是根因线索，但 permissive 会显著降级安全；模块作者必须优先做正确 policy 适配并明确风险。
- #910 | 问：Redmi Note 4 4.9 手工集成后，装任意模块即卡死重启、模块消失。 | 解决：NOT_PLANNED；社区最终定位旧 kernel 的 ext4/overlayfs 兼容，报告者采用 OnePlus sdm845 ext4 driver；后续建议特别回退 `ext4: make dioread_nolock the default` 等，非官方通用补丁。 | 教训：旧 4.9 模块挂载故障常在 ext4/overlayfs backport；须对自己的 tree 做最小 diff/bisect，不能无选择搬整套文件系统驱动。
- #912 | 问：按官方命令自编显示 KSU version 16 过低。 | 解决：NOT_PLANNED；日志显示 `KSU_GIT_VERSION not defined`，维护者要求确保内核树 `KernelSU` 目录中 git 可输出仓库信息；讨论指出 symlink/Bazel 下 `.git` 可见性是关键。 | 教训：构建 KSU 时要保留可访问的 Git 元数据（最好 submodule/完整 clone）并验证 `git rev-list --count HEAD`，否则会编入默认 version 16。
- #915 | 问：数据加密的 ROM 上授权重启即重置，解密 data 则正常。 | 解决：NOT_PLANNED；维护者称是 kernel source 问题、需内核开发者按既有 issue 修，未给本单 patch。 | 教训：加密 data 的 allowlist 持久化依赖内核适配；模块作者要测试 FBE/metadata 挂载时序，不可假定解密 data 的结果可迁移。
- #916 | 问：点击更新前能否弹 changelog、确认/取消对话框。 | 解决：维护者初称欢迎 PR，后给提交 `81bbb31`（COMPLETED）；评论提醒长 Markdown 对话框高度仍要处理。 | 教训：模块更新 UX 应在执行前展示可滚动 changelog 和确认；还要测试超长 Markdown/小屏可用性。
- #918 | 问：x86_64 ChromeOS/FydeOS 5.4 KSU 构建成功却自动重启，workflow 是否适用。 | 解决：NOT_PLANNED；维护者说先看官方编译教程、kprobe 不行可手工集成，并确认 README 支持 x86_64；无成功回报。 | 教训：x86_64 支持不等于 mainline `defconfig` 可启动；应使用目标 ChromeOS config、按官方手工集成路径和设备启动日志验证。
- #922 | 问：Moto Edge S30 关 KPROBES 后手工改源码仍显示“不支持”，kprobe 时不能启动。 | 解决：NOT_PLANNED；维护者先要求确认关 KPROBES/给 bugreport，后从日志判断 KSU 根本没参与编译，要求代码、build log、Image 与官方 Manager/logcat/dmesg；因报告者后续无反馈关闭。 | 教训：手工移植验收必须证明 KSU object 被编译/链接、官方 Manager 签名匹配，并同时保存 build log、电脑抓 logcat 和 dmesg；UI “不支持”不能单独定位。
- #928 | 问：KernelSU 能否作为独立 LKM 发布，以在厂商定制 GKI 上复用 KMI。 | 解决：关联 PR #1254 “Build KernelSU as LKM” 已合并（COMPLETED）；维护者解释旧方案受未导出符号、`kallsyms_lookup_name`/kprobe 限制、GKI KCFI 和 SELinux 结构访问制约，但有源码时仍可直接集成重编内核。 | 教训：LKM 是可用路线但不是无条件替代源码集成；模块开发应考虑符号导出、CFI、OEM GKI 裁剪和 API 稳定性，并保留直接集成方案。

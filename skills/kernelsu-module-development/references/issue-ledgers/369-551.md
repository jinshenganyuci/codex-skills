# KernelSU 关闭 Issue 学习账本（JSONL 第 369–551 行）

方法：逐条读取标题、正文、`stateReason`、关联 PR 与末尾最多 25 条评论。只有关联 PR、维护者回复或报告者复现确认才称为“已解决”；仅显示 `COMPLETED` 而没有这些证据，一律写作“未证实关闭”。社区绕过方案不等于上游通用修复。

- #929 | 问了什么：希望支持多模块系统、把启动阶段解耦。 | 怎么解决/为何关闭：`COMPLETED`，无关联 PR、无评论，未证实关闭。 | 可复用模块开发教训：模块树自动出现不等于已挂载；启动阶段与挂载后端必须是独立且明确的契约。
- #930 | 问了什么：刷入模块时 ASCII 标志文字显示错位。 | 怎么解决/为何关闭：评论只定位到字体和行首空白问题，没有确认补丁；`COMPLETED` 但未证实修复。 | 可复用模块开发教训：安装器输出应使用纯文本、避免依赖终端字体和前导空白。
- #931 | 问了什么：`ptrace`/硬件断点后出现 `do_execveat_common su found` 日志。 | 怎么解决/为何关闭：`NOT_PLANNED`，无解决证据。 | 可复用模块开发教训：KSU 的 `su found` 仅是拦截日志，不能当作崩溃或调试器问题的根因。
- #933 | 问了什么：请求隐藏 KernelSU Manager。 | 怎么解决/为何关闭：维护者明确建议卸载而非增加隐藏功能，需求被拒绝。 | 可复用模块开发教训：模块不能承诺隐藏 Manager；应把安全边界建立在系统锁屏和最小授权上。
- #941 | 问了什么：4.4.88 内核启用 kprobe/手动集成后仍不能正常授权 root。 | 怎么解决/为何关闭：讨论指向 SELinux 与旧内核适配，维护者只要求查文档；没有上游确认的修复。 | 可复用模块开发教训：旧内核先审计 AVC、密钥环和兼容补丁，不能把所有 `su` 失败归因于模块。
- #942 | 问了什么：4.14 上安装完成、开启超级用户后仍找不到 `su`。 | 怎么解决/为何关闭：仅被引导阅读文档，无确认修复。 | 可复用模块开发教训：非 GKI 集成必须逐项验证 hook、SELinux 与数据目录访问。
- #943 | 问了什么：为 4.4/4.9/4.14/4.19/5.4 非 GKI 内核建立警告与分流。 | 怎么解决/为何关闭：该条关闭是维护/支持策略：文档、已知问题和 Discussions 是入口，不是单一 bug 修复。 | 可复用模块开发教训：模块必须声明内核前置能力，非 GKI 故障应附完整日志和集成方式。
- #944 | 问了什么：寻求 4.19 Snapdragon 内核编译帮助。 | 怎么解决/为何关闭：维护者不提供通用内核编译教学，未给出修复。 | 可复用模块开发教训：模块作者应自备可复现的内核构建链，不把设备内核维护转嫁给模块安装器。
- #945 | 问了什么：首次安装时 `modules_update.img` 未被处理。 | 怎么解决/为何关闭：讨论说明首次启动可能尚无 `ksud` 或数据尚未可用；可先打包/复制 `ksud`，后续才考虑内嵌，未见通用合入确认。 | 可复用模块开发教训：首次启动、数据解密和守护进程引导要单独设计；暂存模块可能需要第二次启动。
- #946 | 问了什么：重启后 `load_allow_list` 打开文件失败（-126）。 | 怎么解决/为何关闭：仅指向 #436，没有本条可核实的修复。 | 可复用模块开发教训：授权持久化依赖内核在正确时序访问 `.allowlist`，不要只测试运行时授权。
- #951 | 问了什么：请求模块安装支持“不重启”标志。 | 怎么解决/为何关闭：维护者以会破坏安全模式为由拒绝。 | 可复用模块开发教训：模块变更应遵守重启和安全模式语义，不能假设热加载总是安全。
- #957 | 问了什么：以 `/system/bin/su` 开头的普通文件名被误拦截。 | 怎么解决/为何关闭：关联且已合并 PR #958（“Copy one extra byte…”）确认修正边界匹配。 | 可复用模块开发教训：路径/令牌匹配必须检查完整边界，绝不能用仅前缀的 `su` 判断。
- #959 | 问了什么：手动集成出现 `modpost` section mismatch。 | 怎么解决/为何关闭：只被转至 #943 的非 GKI 分流，没有单独解决证据。 | 可复用模块开发教训：手动集成的链接/段警告要在目标内核配置和源码版本中处理。
- #961 | 问了什么：应用 root 权限重启后不保存。 | 怎么解决/为何关闭：仅转至 #943，没有本条修复。 | 可复用模块开发教训：授权状态丢失先检查内核能否读写 `/data/adb/ksu/.allowlist`。
- #962 | 问了什么：更新 11238 后 NFC 丢失。 | 怎么解决/为何关闭：报告者称清缓存并重启后恢复；这是设备侧经验，不是上游补丁。 | 可复用模块开发教训：排查模块问题时先隔离缓存、模块与系统更新，且标注临时恢复法的局限。
- #963 | 问了什么：KernelSU 未写入 `.allowlist`。 | 怎么解决/为何关闭：仅指向 #943，无确认修复。 | 可复用模块开发教训：模块和授权功能都应检测数据目录可达性与 SELinux 上下文。
- #977 | 问了什么：为何不能用 C 的 `stdio.h` 编译内核代码。 | 怎么解决/为何关闭：非 GKI 分流，没有单独补丁。 | 可复用模块开发教训：内核侧只能使用内核头文件和内核 API，不能把用户态 C 库带入内核构建。
- #981 | 问了什么：请求用内核命令行彻底禁用 KSU。 | 怎么解决/为何关闭：维护者指出已有提交 `2d9aebf`，且控制机制迁至 bootconfig，因此不再增加命令行方案。 | 可复用模块开发教训：新增开关前先确认现有 bootconfig/启动配置机制，避免并行控制面。
- #982 | 问了什么：安装模块报 `Permission denied (os error 13)`。 | 怎么解决/为何关闭：仅转至 #943，无本条已证实修复。 | 可复用模块开发教训：安装权限错误优先收集 AVC、挂载和数据目录日志。
- #983 | 问了什么：如何判断自己应使用哪个 KMI 镜像。 | 怎么解决/为何关闭：维护者说明名称已表明非 GKI，Manager 正则可能误判，须向内核维护者核实。 | 可复用模块开发教训：不能只依赖 App“支持”提示选镜像；必须验证实际 KMI、ABI 和启动镜像布局。
- #986 | 问了什么：请求把授权界面字符串拆分以便翻译。 | 怎么解决/为何关闭：因会造成大规模重译而拒绝。 | 可复用模块开发教训：Manager/模块的 UI 字符串应保持稳定语义，减少无必要拆分。
- #987 | 问了什么：WPS 在授权 root 后崩溃。 | 怎么解决/为何关闭：维护者判断与 KernelSU 无关。 | 可复用模块开发教训：应用崩溃须以可复现日志区分应用问题、模块冲突和 KSU 核心问题。
- #990 | 问了什么：userspace `fstack-protector` 相关担忧。 | 怎么解决/为何关闭：维护者解释正常执行路径少会触及，报告者认为现象奇怪后关闭；无代码修复证据。 | 可复用模块开发教训：对工具链保护机制的担忧要以实际调用路径和崩溃栈验证，不能据关闭状态宣称已修复。
- #991 | 问了什么：Singpass 检测到 KSU。 | 怎么解决/为何关闭：临时删除 `/data/adb/ksu/modules*.img` 并重启可规避但会破坏模块；讨论称 v0.6.9 对内核 >=5.10 有修复。 | 可复用模块开发教训：模块镜像挂载痕迹可被检测；隐藏策略必须版本门控，并测试卸载后模块是否仍可用。
- #993 | 问了什么：Nethunter 工具问题。 | 怎么解决/为何关闭：维护者判定与 KernelSU 无关。 | 可复用模块开发教训：第三方发行版/工具链问题不要伪装成模块框架 bug。
- #994 | 问了什么：请求加入 guamp 设备。 | 怎么解决/为何关闭：GitHub Action 自动创建 PR #995，设备登记流程有证据。 | 可复用模块开发教训：设备支持应走可审查的源码仓库和自动 PR 流程。
- #997 | 问了什么：HDFC/ICICI 银行 App 检测 root。 | 怎么解决/为何关闭：指向 #991；评论提到 ZygiskNext/Shamiko，但无上游确认。 | 可复用模块开发教训：反检测模块是社区兼容方案，不应当作 KSU 核心保证。
- #998 | 问了什么：Manager 要求 KernelSU 版本至少 11071。 | 怎么解决/为何关闭：需刷入更新的 KSU 内核，Manager 本身不能弥补旧内核。 | 可复用模块开发教训：模块/Manager 与内核 ABI 有最低版本约束，发布时必须做版本检查。
- #1001 | 问了什么：自编译内核启动后驱动全部失效。 | 怎么解决/为何关闭：维护者判定非 KSU 问题，无修复。 | 可复用模块开发教训：替换内核后需单独验证 vendor 驱动、DTB 和模块 ABI。
- #1002 | 问了什么：显示工作中，但模块重启后消失、数量为 0。 | 怎么解决/为何关闭：没有日志或确认结论。 | 可复用模块开发教训：模块报告必须同时提供安装日志、`modules*.img` 状态和启动日志。
- #1005 | 问了什么：模块更新日志过长时更新按钮被遮挡。 | 怎么解决/为何关闭：`COMPLETED`，无 PR、无评论，未证实关闭。 | 可复用模块开发教训：模块 changelog 需要限制高度、滚动容器和可见操作按钮。
- #1006 | 问了什么：模块更新对话框高度异常。 | 怎么解决/为何关闭：明确重复 #1005，未提供独立修复证据。 | 可复用模块开发教训：UI 重复问题应绑定同一回归用例，而不是复制结论。
- #1007 | 问了什么：希望完善 App Profile。 | 怎么解决/为何关闭：评论给出提交 `9b29468`，可作为实现变更证据。 | 可复用模块开发教训：Profile 功能要以具体提交和权限持久化测试为准。
- #1010 | 问了什么：Raspberry Pi 上 AK3 无法拆分非标准 FAT boot 分区。 | 怎么解决/为何关闭：建议自行编译/集成内核，AK3 的标准 Android boot-image 假设不适用。 | 可复用模块开发教训：发布模块/刷机包前先验证分区格式和镜像布局，不能假设所有设备都是标准 boot 分区。
- #1011 | 问了什么：请求加入 Xiaomi 5X。 | 怎么解决/为何关闭：Action 自动创建 PR #1012。 | 可复用模块开发教训：设备元数据登记应可追踪到 PR。
- #1013 | 问了什么：请求加入 Xiaomi 6。 | 怎么解决/为何关闭：Action 自动创建 PR #1014。 | 可复用模块开发教训：同上，维护设备清单要保留来源和审核记录。
- #1016 | 问了什么：建议 Releases 只保留最新 sublevel。 | 怎么解决/为何关闭：维护者认为旧精确版本可能用于兼容和回退，未采纳。 | 可复用模块开发教训：模块/镜像发布不应过早删除兼容版本，回退链是故障恢复资产。
- #1018 | 问了什么：再次请求加入 Xiaomi 6。 | 怎么解决/为何关闭：Action 自动创建 PR #1019。 | 可复用模块开发教训：自动化登记也应去重并记录具体设备分支。
- #1020 | 问了什么：再次请求加入 Xiaomi 5X。 | 怎么解决/为何关闭：Action 自动创建 PR #1021。 | 可复用模块开发教训：同一设备多来源时，模块应明确维护者和兼容分支。
- #1022 | 问了什么：询问安全漏洞披露渠道。 | 怎么解决/为何关闭：维护者增加 SECURITY 路由、GitHub Advisory/邮件渠道。 | 可复用模块开发教训：模块应公布负责任披露方式，不在公开 issue 暴露漏洞细节。
- #1026 | 问了什么：Superuser 权限重启后重置。 | 怎么解决/为何关闭：仅指向 #943，无单独修复。 | 可复用模块开发教训：持久化问题必须同时审计 init、加密时序和 `.allowlist` I/O。
- #1028 | 问了什么：长 changelog 导致不能更新模块。 | 怎么解决/为何关闭：重复 #1005，评论称“也许下个稳定版解决”，不是修复证明。 | 可复用模块开发教训：不要把版本猜测写成已修复；要保留 UI 回归证据。
- #1030 | 问了什么：运行 `setup.sh` 时出现空 pathspec。 | 怎么解决/为何关闭：删除残留的 `KernelSU` 目录后重跑；不要手改 `KSU_GIT_VERSION`。 | 可复用模块开发教训：安装脚本应处理旧工作树，版本信息应由 Git/子模块生成。
- #1031 | 问了什么：请求为不支持的 stock kernel 提供通用可刷注入器。 | 怎么解决/为何关闭：非 GKI 请求被拒绝，GKI 路线指向 #928。 | 可复用模块开发教训：不要承诺“万能注入器”；内核集成方式必须受版本和设备边界约束。
- #1033 | 问了什么：手动集成出现一系列 undefined reference。 | 怎么解决/为何关闭：给出文档/配置方向，但后续无验证成功。 | 可复用模块开发教训：手动 hook 必须匹配当前源码和 Kconfig；未复现成功前不能记录为修复。
- #1035 | 问了什么：未指定 boot 槽位刷内核可能损坏系统。 | 怎么解决/为何关闭：讨论涉及 Magisk、分区、格式化和 slot，未见上游修复。 | 可复用模块开发教训：刷机流程必须明确 boot/init_boot、A/B 槽、AVB 与恢复路径，并先备份原镜像。
- #1036 | 问了什么：出现 `Skipping insecure file: Try again`。 | 怎么解决/为何关闭：维护者判定无关。 | 可复用模块开发教训：安装器安全文件检查失败要先定位调用者和文件元数据。
- #1037 | 问了什么：CI 更新后 Manager 不识别已安装 KSU。 | 怎么解决/为何关闭：应使用 main 分支 CI 产物，而非 PR 产物。 | 可复用模块开发教训：内核与 Manager 构建必须来自同一有效版本线。
- #1038 | 问了什么：Galaxy S23 刷 KSU 后不开机。 | 怎么解决/为何关闭：仅要求搜索既有 issue，无确认方案。 | 可复用模块开发教训：设备启动问题至少应提供精确镜像、补丁级别和 pstore。
- #1039 | 问了什么：ColorOS 14 5.15.94 无法用 AnyKernel3。 | 怎么解决/为何关闭：无日志、无确认修复；仅有第三方构建建议。 | 可复用模块开发教训：第三方包建议须标为设备特定，不能替代可复现的正式修复。
- #1040 | 问了什么：请求内置 Zygisk/Shamiko 开关。 | 怎么解决/为何关闭：维护者拒绝，且请求者未证明“主流需求”。 | 可复用模块开发教训：不要把第三方 root/隐藏栈硬编码进核心；保持模块化边界。
- #1041 | 问了什么：振动模块失效。 | 怎么解决/为何关闭：模板/证据不足，未证实解决。 | 可复用模块开发教训：模块故障报告要附模块内容、目标路径和日志。
- #1044 | 问了什么：翻译问题。 | 怎么解决/为何关闭：维护者说明该文本有意不翻译，需求未采纳。 | 可复用模块开发教训：有技术含义或协议名的文本应保持可辨识的原文。
- #1045 | 问了什么：模块在 Manager 中消失。 | 怎么解决/为何关闭：定位到“默认卸载模块”与 Pixel Launcher/Profile 影响，关闭对应设置/配置后可见。 | 可复用模块开发教训：默认 unmount 会让选定 App 看不到模块文件；要测试 launcher、工作资料和 Profile。
- #1046 | 问了什么：请求加入 Tab S7 Wi‑Fi。 | 怎么解决/为何关闭：Action 自动创建 PR #1047。 | 可复用模块开发教训：设备支持的来源、型号和维护仓库都要可追溯。
- #1048 | 问了什么：希望 kprobe 不关闭时仍可手动集成。 | 怎么解决/为何关闭：根因是 `do_execveat_common` kprobe 符号失败；建议查 `/proc/kallsyms`、仅手动补 `fs/exec.c`，后来有导出 `__do_execve_file`/#668 绕过，但 App `su` 仍有问题。 | 可复用模块开发教训：混合集成要按实际可 hook 符号裁剪；这是部分方案，不是通用修复。
- #1049 | 问了什么：模块突然消失。 | 怎么解决/为何关闭：与 #1045 同类，初始日志未构成复现证据。 | 可复用模块开发教训：先排除 Profile/default-unmount，再判断模块是否真的丢失。
- #1050 | 问了什么：4.4 内核安装模块时冻结。 | 怎么解决/为何关闭：维护者给出 `ksu_allowxperm` 和两条额外 SELinux 规则，报告者确认可用。 | 可复用模块开发教训：旧设备应使用最小、定向的 SELinux 规则，不能以全局 permissive 代替。
- #1051 | 问了什么：ColorOS 14 刷 android12-5.10.168 镜像无限重启。 | 怎么解决/为何关闭：有人通过清数据、CI 或 LKM 成功，但仍有设备冻结/bootloop；没有通用修复。 | 可复用模块开发教训：启动镜像兼容性必须按设备和补丁级别验证，破坏性清数据只能是明确标注的最后手段。
- #1052 | 问了什么：无法 root，Profile 重启后消失。 | 怎么解决/为何关闭：维护者建议先禁用所有模块以诊断；无报告者确认结果。 | 可复用模块开发教训：把核心 root/持久化故障与第三方模块冲突分层排查。
- #1054 | 问了什么：模块列表消失。 | 怎么解决/为何关闭：重复 #1045。 | 可复用模块开发教训：默认卸载策略应有清晰 UI 和 per-app 可见性测试。
- #1056 | 问了什么：Manager 显示无模块。 | 怎么解决/为何关闭：删除大范围 KSU/Magisk 状态一度看似恢复，维护者怀疑 `magisk.db`，后续又有人出问题；这是不安全且未证实的绕过。 | 可复用模块开发教训：不要建议广泛删除 `/data/adb` 状态；先备份并定位符号链接、数据库和镜像。
- #1061 | 问了什么：4.9 内核重启到 recovery 后 bootloop。 | 怎么解决/为何关闭：无评论，`NOT_PLANNED`，未证实关闭。 | 可复用模块开发教训：恢复模式与正常系统的挂载/SELinux 路径应分别测试。
- #1062 | 问了什么：Itsme 检测到 root。 | 怎么解决/为何关闭：讨论称 v0.6.9 对内核 >=5.10 有修复；4.19 可尝试 `ksu_unmount_injector`，但可能破坏模块功能。 | 可复用模块开发教训：隐藏行为需要版本门控；每次 unmount 后都应验证模块是否仍提供功能。
- #1063 | 问了什么：0.6.9 中游戏自动获 root。 | 怎么解决/为何关闭：建议测试 CI 内核/提交 `b183004`，无确认结果。 | 可复用模块开发教训：授权异常要以 allowlist、UID 映射和测试提交定位，不能宣称已修复。
- #1066 | 问了什么：如何把 `/system` 挂为读写。 | 怎么解决/为何关闭：只建议搜索已关闭的 rw issue，无直接方案。 | 可复用模块开发教训：系统覆盖优先采用模块 overlay，不假设可安全重挂真实 `/system`。
- #1067 | 问了什么：模块被卸载。 | 怎么解决/为何关闭：建议最新 CI 与 #1045，无报告者确认。 | 可复用模块开发教训：先记录 App Profile 和 unmount 开关，再判断模块加载失败。
- #1068 | 问了什么：Android 14 Pixel 8 不能用 AK3 刷入。 | 怎么解决/为何关闭：发现 AK3 缺 arm64 二进制，KernelSU AK3 提交 `1abdfc5` 已加入；安装成功，但另一个 bootloop 未解决。 | 可复用模块开发教训：安装包必须携带目标 ABI 工具；“刷入成功”与“系统稳定”是两个验收项。
- #1069 | 问了什么：请求加入 Xiaomi 11T Pro。 | 怎么解决/为何关闭：Action 自动创建 PR #1070。 | 可复用模块开发教训：设备清单变更走自动 PR 并保留版本来源。
- #1072 | 问了什么：KSU 工作但相机、USB、电量异常。 | 怎么解决/为何关闭：维护者建议移除 `/data/adb/ksu`，没有结果回报。 | 可复用模块开发教训：硬件回归先做无模块/无 KSU 状态的对照，避免直接归因核心。
- #1073 | 问了什么：Nafath App 在模块安装后崩溃。 | 怎么解决/为何关闭：指向 #1062；报告者称 MIUI+模块状态下 Magisk 也复现，删除模块镜像才恢复，无上游确认修复。 | 可复用模块开发教训：先用安全模式/逐模块二分定位，不把应用检测都归咎于 KSU。
- #1074 | 问了什么：Pixel 8 刷后 Wi‑Fi/蓝牙缺失。 | 怎么解决/为何关闭：定位为 vendor 模块 `bcmdhd4398`/依赖 ABI 问题；测试镜像获用户确认 Wi‑Fi、BT 和 KSU 可用。 | 可复用模块开发教训：替换内核必须维持 vendor 模块符号 ABI 和依赖链。
- #1077 | 问了什么：请求支持 Pixel 8 Pro。 | 怎么解决/为何关闭：建议测试 CI/release，没有明确报告者确认。 | 可复用模块开发教训：新机支持应先以可回退的测试镜像验证，而非仅凭设备名。
- #1080 | 问了什么：请求 Manager 内置日志查看器。 | 怎么解决/为何关闭：维护者拒绝，建议使用专业文件管理/查看器。 | 可复用模块开发教训：核心 Manager 应聚焦授权/模块状态，复杂日志消费可交给通用工具。
- #1081 | 问了什么：OxygenOS 14 支持问题。 | 怎么解决/为何关闭：转至 #1051，没有独立修复。 | 可复用模块开发教训：相同启动症状必须按机型、分区和镜像版本区分，避免错误复用方案。
- #1083 | 问了什么：USB tether/ADB 失效。 | 怎么解决/为何关闭：报告者确认 android12-5.10-lts 5.10.199 可修复，属设备/内核特定结果。 | 可复用模块开发教训：版本回归要记录精确 kernel release，不泛化为所有设备的 KSU 修复。
- #1084 | 问了什么：特定网络下在线更新模块时崩溃。 | 怎么解决/为何关闭：报告者通过 Clash 代理恢复，怀疑异常 API 响应；是网络绕过，不是确认代码修复。 | 可复用模块开发教训：模块源客户端应校验非预期响应，并把网络异常与安装逻辑分离。
- #1085 | 问了什么：请求加入 Redmi Note 8。 | 怎么解决/为何关闭：Action 自动创建 PR #1086。 | 可复用模块开发教训：同类设备登记可自动化，但仍要验证维护仓库。
- #1087 | 问了什么：默认 root profile/template 如何使用。 | 怎么解决/为何关闭：Settings 的“create template”会创建后再改 ID；提问者转问默认 profile 后无维护者跟进，结论仅部分明确。 | 可复用模块开发教训：模块 Profile 文档要区分“模板创建”和“默认策略”。
- #1089 | 问了什么：root 可被检测。 | 怎么解决/为何关闭：维护者要求英文、日志并指向 #1062，无本条修复。 | 可复用模块开发教训：反检测报告必须给 App、版本、最小复现和无模块对照。
- #1090 | 问了什么：请求加入 Galaxy A70。 | 怎么解决/为何关闭：Action 自动创建 PR #1091。 | 可复用模块开发教训：非 GKI 设备也要记录准确内核树和维护状态。
- #1092 | 问了什么：Windows Defender 报 KSU APK 木马。 | 怎么解决/为何关闭：维护者给出源码/GitHub Action 来源，缺可操作代码细节无法修复。 | 可复用模块开发教训：发布模块应提供可复现构建与校验值，区分误报和真实供应链风险。
- #1093 | 问了什么：请求加入 Redmi 6/6A。 | 怎么解决/为何关闭：Action 无法建 PR，要求提交手工内容/PR；未证明已集成。 | 可复用模块开发教训：自动化失败时应有明确人工贡献路径。
- #1095 | 问了什么：Redmi Note 12 Turbo 上 Shamiko/Zygisk 后 Chrome 网页空白。 | 怎么解决/为何关闭：关闭“默认卸载模块”后恢复。 | 可复用模块开发教训：依赖系统覆盖的模块应声明与 default-unmount 的兼容要求。
- #1098 | 问了什么：银行 App 把 KSU 视作恶意软件。 | 怎么解决/为何关闭：被作为重复且不可操作问题关闭。 | 可复用模块开发教训：不要把无日志的广义“银行检测”作为可修复 bug；应拆成具体检测面。
- #1099 | 问了什么：在 `/sdcard` 绑定目录。 | 怎么解决/为何关闭：维护者判定无关并转 Discussions。 | 可复用模块开发教训：存储挂载需求需与 KSU 模块安装边界分开设计。
- #1100 | 问了什么：手动集成 undefined reference。 | 怎么解决/为何关闭：只转 #943 的非 GKI 分流，无本条解决。 | 可复用模块开发教训：手动集成应锁定源码版本和所需导出符号。
- #1101 | 问了什么：模块无法覆盖 `/apex` 文件。 | 怎么解决/为何关闭：维护者明确 KSU 不处理 APEX mount，作者需自行挂载，且无计划支持。 | 可复用模块开发教训：`system/` overlay 不涵盖 APEX；模块必须显式实现并测试自己的 APEX 挂载逻辑。
- #1103 | 问了什么：如何从内核树移除 KSU。 | 怎么解决/为何关闭：社区建议回退集成提交/查看历史，未有维护者确认的统一步骤。 | 可复用模块开发教训：集成 KSU 时应保留干净提交边界，确保可逆。
- #1104 | 问了什么：Android 14 bootloop。 | 怎么解决/为何关闭：转至 #1051，无独立方案。 | 可复用模块开发教训：bootloop 处理需要精确镜像和安全补丁对齐。
- #1106 | 问了什么：非 GKI 安装模块导致 ext4 崩溃。 | 怎么解决/为何关闭：维护者判断为内核问题。 | 可复用模块开发教训：loop/ext4/overlay 稳定性是内核前置条件，模块不可替其兜底。
- #1107 | 问了什么：土耳其语翻译。 | 怎么解决/为何关闭：转至 #1108，没有本条直接结果。 | 可复用模块开发教训：翻译变更应集中到专门 issue/平台，避免分散状态。
- #1110 | 问了什么：Manager 安装按钮缺失。 | 怎么解决/为何关闭：只有含糊的复选框评论，无验证结论。 | 可复用模块开发教训：UI bug 报告应提供截图、屏幕尺寸、版本和可复现步骤。
- #1111 | 问了什么：自编译的非官方 Manager 中模块消失。 | 怎么解决/为何关闭：维护者不支持非官方构建，建议对照官方 GitHub Action。 | 可复用模块开发教训：自定义 Manager 必须先与官方签名、版本和 userspace 产物做差异对比。
- #1112 | 问了什么：请求 Multi-User Mode。 | 怎么解决/为何关闭：维护者认为白名单模型下 root 默认不存在，功能诉求被拒；后续仍有人指出跨用户管理空白。 | 可复用模块开发教训：多用户授权不能仅看 UID 白名单，还要设计用户隔离、可见性和撤销语义。
- #1113 | 问了什么：Exynos LOS 手动集成后 `su` 不工作。 | 怎么解决/为何关闭：建议按当前源码重新手动集成；报告者称 `su`/模块大多恢复但 Profile 仍有问题。 | 可复用模块开发教训：部分成功必须拆分验收：root、模块、Profile 持久化各自验证。
- #1117 | 问了什么：Pixel 8 Pro 手工修补镜像无法启动。 | 怎么解决/为何关闭：使用较新的 2023-10 内核并以 `magiskboot` 修补后，报告者确认可启动。 | 可复用模块开发教训：修补流程必须与安全补丁和目标镜像版本匹配。
- #1118 | 问了什么：请求供 ARCVM 使用的 Android 13 5.10.188。 | 怎么解决/为何关闭：维护者缺硬件/测试条件，要求提交 PR。 | 可复用模块开发教训：新环境支持需要贡献者提供可测试构建，而不应凭空发布镜像。
- #1124 | 问了什么：McDonald’s App 仍检测 KSU，LSPosed 绕过失败。 | 怎么解决/为何关闭：维护者判定无关，无修复。 | 可复用模块开发教训：第三方注入栈失败不证明 KSU 核心 bug。
- #1132 | 问了什么：ColorOS 14 卡启动动画。 | 怎么解决/为何关闭：建议尝试更高 patch level（2023-10），有人有阶段性成功，但仍有擦除数据/slot/冻结问题；不是通用修复。 | 可复用模块开发教训：把版本建议、破坏性步骤和残余风险分开记录。
- #1136 | 问了什么：刷新 App Profile 模板页后 Manager 崩溃。 | 怎么解决/为何关闭：删除 `/data/adb/ksu/profile/templates` 可恢复；根因是在线模板 ID 与文件名不一致。 | 可复用模块开发教训：模块/模板缓存应校验 ID 与文件名映射，并提供可恢复的缓存重建路径。
- #1139 | 问了什么：请求加入 Realme C25/Narzo50A。 | 怎么解决/为何关闭：Action 自动创建 PR #1140。 | 可复用模块开发教训：设备声明要与实际可维护内核仓库绑定。
- #1142 | 问了什么：Chalo App 检测 root。 | 怎么解决/为何关闭：最终定位到 Magisk OverlayFS RW 模块；安全模式/无模块可用。 | 可复用模块开发教训：检测问题必须先逐个禁用模块，避免误判 KSU 核心。
- #1147 | 问了什么：如何为不同用户授权 KSU。 | 怎么解决/为何关闭：维护者称 MIUI 主用户可为双开 App 授权，但原报告信息不足，无通用解法。 | 可复用模块开发教训：多用户场景必须按 OEM、工作资料和克隆 App 分别测试。
- #1148 | 问了什么：自编 6.1.25 内核无法启动。 | 怎么解决/为何关闭：按 GitHub Action 构建配方操作后，报告者确认可启动。 | 可复用模块开发教训：优先复用可验证 CI 配方，再改动本地构建参数。
- #1152 | 问了什么：请求 android12-5.10.189 构建。 | 怎么解决/为何关闭：维护者仅回复提交 `0db7aa5`，issue 未说明变更和用户验证，未证实关闭。 | 可复用模块开发教训：镜像发布请求应附 KMI/Android 分支和可验证的产物链接。
- #1155 | 问了什么：Redmi Note 13 刷最新内核包后首屏无限重启。 | 怎么解决/为何关闭：维护者建议改用 `magiskboot` 方式，报告者未确认结果，`NOT_PLANNED`。 | 可复用模块开发教训：bootloop 先更换受支持的镜像修补法并保存原 boot，不能只换包反复刷。
- #1157 | 问了什么：多用户中的 App 授权 root 无效。 | 怎么解决/为何关闭：维护者称 MIUI 双开可正常授权，要求补充复现，没有通用解法。 | 可复用模块开发教训：多用户问题要提供 userId、克隆方式、UID 和 allowlist 证据。
- #1158 | 问了什么：是否有通用隐藏特定文件/目录方案以对抗检测。 | 怎么解决/为何关闭：维护者认为“非原厂系统检测”与 KernelSU 无直接关系，未采纳。 | 可复用模块开发教训：不要承诺通用文件隐藏；检测面远超单一模块路径。
- #1162 | 问了什么：ColorOS 13 Orange State 循环、不能入系统。 | 怎么解决/为何关闭：社区建议先以 Magisk 进入系统取日志，维护者建议 `magiskboot` 修补；无确认成功。 | 可复用模块开发教训：不可启动时要先建立可取 pstore/原厂镜像的恢复链。
- #1165 | 问了什么：安装模块后工作资料 Intune 检测 root。 | 怎么解决/为何关闭：评论指出 4.14 没有模块 unmount 支持，维护者索取内核源码并指向 #1062；无最终修复。 | 可复用模块开发教训：反检测能力取决于内核版本和 unmount 支持，模块要声明该限制。
- #1166 | 问了什么：HyperOS 14 上 Fake Location 获 root 后仍不能使用。 | 怎么解决/为何关闭：维护者不认为是内核问题，另有评论称 Magisk 也会重启；无 KSU 修复。 | 可复用模块开发教训：跨 root 方案都复现时，应转向 App/OEM 兼容性排查。
- #1170 | 问了什么：请求内置 custom resetprop hiding。 | 怎么解决/为何关闭：评论说明 KSU 不修改 props，建议第三方隐藏模块；没有上游实现。 | 可复用模块开发教训：属性修改/隐藏应由独立模块负责，并明确其全局副作用。
- #1172 | 问了什么：小米 Mi 8 屏幕指纹版集成 KSU 时 `apk_sign.c` 的 `IS_ERR` 警告被当作错误。 | 怎么解决/为何关闭：讨论确认官方源码适配 GCC，Proton-Clang 需额外修改；未见本条构建成功确认。 | 可复用模块开发教训：内核/模块构建工具链必须与 OEM 源码预期一致，不能默认任意 Clang 可用。
- #1173 | 问了什么：Android OTA/跨 Android 版本后如何保留 KSU。 | 怎么解决/为何关闭：维护者说明 Android 版本与内核版本不同；新系统先让对应内核可用，再重新集成/刷入 KSU。 | 可复用模块开发教训：OTA 后将 KSU/模块视为需重新验证的内核依赖，而不是永久保留的 App 状态。
- #1177 | 问了什么：G99 设备该如何低风险安装 KSU、如何从 Magisk 切换。 | 怎么解决/为何关闭：讨论解释 Magisk 改 ramdisk、KSU 改 kernel；通用 GKI 未必兼容，应找本机开源内核；三星首次刷 boot 常需清数据。 | 可复用模块开发教训：不要把任何 GKI 镜像视为无风险；先确认设备标准、原厂源与可恢复路径。
- #1178 | 问了什么：Galaxy A54 刷 KSU 后显示 RAM 从 8GB 变 6GB。 | 怎么解决/为何关闭：维护者认为不像 KSU，建议用三星官方内核源码加 KSU 验证；无修复。 | 可复用模块开发教训：内存容量异常可能来自设备内核/GKI，需含和不含 KSU 的同源构建对照。
- #1180 | 问了什么：ChromeOS M117+ 无法用 KernelSU/`sudo`。 | 怎么解决/为何关闭：维护者明确无关，社区给外部 ARCVM 方案链接。 | 可复用模块开发教训：ChromeOS/容器平台不能直接套用 Android KSU 支持结论。
- #1182 | 问了什么：OnePlus 11 OxygenOS 14 用 GKI/AK3 启动即进 recovery。 | 怎么解决/为何关闭：维护者建议 `magiskboot`；报告者确认修补干净 `init_boot.img` 后获得 root。 | 可复用模块开发教训：先确认设备用 `boot` 还是 `init_boot`，再用匹配的修补流程。
- #1183 | 问了什么：请求加入 MI 8 SE。 | 怎么解决/为何关闭：Action 自动创建 PR #1184。 | 可复用模块开发教训：设备集成要用维护者仓库和自动审核记录。
- #1185 | 问了什么：授权/卸载模块后 bootloop，安全模式也进不去，栈指向 `ksu_prune_allowlist`。 | 怎么解决/为何关闭：删除数据目录对一位用户有效；维护者提交 `fe7f509` 并明确说现场已丢失、无法保证根因，属尝试修复。 | 可复用模块开发教训：allowlist 清理必须防御失效 UID/卸载竞态；安全模式不能依赖可能已崩的清理路径。
- #1187 | 问了什么：Motorola 5.4 上 overlayfs 挂载失败，怀疑 FeliCa 限制。 | 怎么解决/为何关闭：报告者后来发现是“默认卸载模块”及模块与 NFC 冲突，并非提出的内核补丁问题。 | 可复用模块开发教训：先切换 unmount 设置和逐模块隔离，避免以破坏 NFC 的内核改动作为方案。
- #1190 | 问了什么：锁定 bootloader 的 Snapdragon Samsung 无法安装 KSU。 | 怎么解决/为何关闭：设备/运营商 bootloader 无法解锁，非 KSU 可修复问题。 | 可复用模块开发教训：KSU 安装前置条件是可解锁并可刷写的 boot 链；SIM 解锁不等于 bootloader 解锁。
- #1191 | 问了什么：畸形 `module.prop`（ID 行含 TAB）让模块被放到错误目录并影响其他模块。 | 怎么解决/为何关闭：`COMPLETED`，无评论、无 PR，未证实修复。 | 可复用模块开发教训：模块 ID 必须严格校验、拒绝空白/控制字符，并保证安装失败不删除已有模块。
- #1193 | 问了什么：请求构建 aarch32 Manager/支持 32 位设备。 | 怎么解决/为何关闭：维护者明确无计划：内核数据结构、ksud、resetprop、busybox 都需 32 位适配；社区仅给实验性补丁。 | 可复用模块开发教训：模块目标 ABI 必须显式限制；32 位 userspace 与 64 位内核混用需额外指针大小审计。
- #1195 | 问了什么：Pixel 6 Pro 12 月更新后，5.10.177 修补镜像启动失败。 | 怎么解决/为何关闭：用户确认 android13-5.10.189 可用；维护者强调 KSU 镜像安全补丁级别不得低于 stock。 | 可复用模块开发教训：选择内核镜像要比较安全补丁级别，不只是内核小版本。
- #1204 | 问了什么：空白/不完整的反馈。 | 怎么解决/为何关闭：维护者要求按模板补全，未提供任何技术解决。 | 可复用模块开发教训：模块 bug 报告必须含操作、版本、日志和预期；否则不可诊断。
- #1205 | 问了什么：ColorOS 14 刷 KSU boot 后不能启动。 | 怎么解决/为何关闭：维护者因未说明镜像/刷写步骤而判定无效反馈，无修复。 | 可复用模块开发教训：启动报告必须写明具体 Image、分区、工具和命令。
- #1206 | 问了什么：OnePlus 12 ColorOS 14 刷 6.1.25/AK3 变砖。 | 怎么解决/为何关闭：维护者建议最新 CI+`magiskboot`；一名用户称自修补后 `fastboot -w` 可启动，但其他人仍失败/假死。 | 可复用模块开发教训：`fastboot -w` 会清数据，只能作为有备份的设备特定绕过，绝不能宣称通用修复。
- #1207 | 问了什么：Pixel 7 Pro 修补 `boot.img` 后 bootloop。 | 怎么解决/为何关闭：报告者确认按 #1195 使用 5.10.189 后解决。 | 可复用模块开发教训：相同安全补丁门槛适用于多个 Pixel，但仍需以具体机型验证。
- #1210 | 问了什么：自编 6.1.25 虽工作但显示版本 16/过低。 | 怎么解决/为何关闭：报告者改用 `tools/bazel run --config=fast //common:kernel_aarch64_dist -- --dist_dir=dist` 后确认成功。 | 可复用模块开发教训：Bazel 产物/版本戳要走正确的 run+dist 流程，不能只看编译是否成功。
- #1211 | 问了什么：启用 SIM PIN 后重启崩到 recovery。 | 怎么解决/为何关闭：维护者先问是否装模块并认为不像 KSU，未有后续结论。 | 可复用模块开发教训：安全功能回归先做无模块和 stock kernel 对照。
- #1212 | 问了什么：请求 android12-5.10.198 构建。 | 怎么解决/为何关闭：维护者提供 GitHub Action 构建链接 `7158924415`，但无用户确认。 | 可复用模块开发教训：发布镜像时应让请求者验证启动、模块和硬件，而非只交付产物。
- #1213 | 问了什么：请求将 BusyBox 从 1.34.1 升至 1.36.1。 | 怎么解决/为何关闭：关联 PR #1214“Update busybox to 1.36.1”已合并。 | 可复用模块开发教训：模块 shell 依赖应固定并记录 BusyBox 版本，升级要有兼容测试。
- #1215 | 问了什么：文档手动编译命令与 GitHub CI 差异大。 | 怎么解决/为何关闭：回复明确手动编译应遵官方文档，不要复制 CI；6.1 的追问无最终命令。 | 可复用模块开发教训：CI 含缓存、stamp、发布参数；模块文档应提供单独的可复现本地命令。
- #1217 | 问了什么：Android 14 6.1.25 自编后 Mi 14 卡第二屏，release Image 却能启动。 | 怎么解决/为何关闭：讨论未提供维护者修复或成功验证，未证实关闭。 | 可复用模块开发教训：同分支构建仍可能因 config、LTO、dist 产物不同而不兼容，需做二进制/配置差分。
- #1218 | 问了什么：SM-A546E 装 KSU 后 8GB RAM 显示为 6GB。 | 怎么解决/为何关闭：无评论，`NOT_PLANNED`，无结论。 | 可复用模块开发教训：没有启动期 kmsg 和同源对照时，不能把容量差异归咎于模块。
- #1219 | 问了什么：Pixel 7 12 月补丁后没有可用 5.10-a13 镜像。 | 怎么解决/为何关闭：评论确认 5.10.189+`magiskboot` 可用，维护者称 Android Image Kitchen 已不推荐（`534ac88`）。 | 可复用模块开发教训：将修补工具纳入兼容矩阵，优先维护推荐工具链。
- #1220 | 问了什么：安装 Pixelify 模块时报空间不足且功能不完整。 | 怎么解决/为何关闭：无评论、无 PR，`COMPLETED` 但未证实关闭。 | 可复用模块开发教训：大型模块要预检解压大小、动态下载量和 modules.img 容量。
- #1224 | 问了什么：土耳其健康 App 检测 root。 | 怎么解决/为何关闭：社区提供 unmount injector 但报告者称无效，维护者转 #1062；无确认修复。 | 可复用模块开发教训：隐藏模块失败也应保留功能/版本/ROM 矩阵，不能只给下载包。
- #1227 | 问了什么：多用户同一 App 都授权后卸载会 bootloop，删 `.allowlist` 可恢复。 | 怎么解决/为何关闭：维护者要求复现后的 allowlist，未记录修复结果。 | 可复用模块开发教训：允许同包多用户时，删除/卸载路径必须按 userId 清理并防止残留 UID。
- #1228 | 问了什么：请求加入 Samsung Galaxy A12s。 | 怎么解决/为何关闭：Action 自动创建 PR #1229。 | 可复用模块开发教训：登记设备前应验证源码仓库仍可构建。
- #1234 | 问了什么：Docker/redroid Android 12 是否能用 KSU。 | 怎么解决/为何关闭：维护者称理论可行但需自行尝试；容器共享 host kernel，评论建议自行编译宿主内核，无验证结果。 | 可复用模块开发教训：容器 Android 的 KSU 边界在宿主 kernel/namespace，不是容器 APK 层。
- #1236 | 问了什么：Galaxy A54 RAM 显示减少。 | 怎么解决/为何关闭：维护者认为类似 Samsung GKI 问题，建议官方源码；报告者称第三方 v0.7.5 内核已解决。 | 可复用模块开发教训：把 OEM/GKI 缺陷与 KSU 模块分开，记录已验证的设备内核版本。
- #1238 | 问了什么：请求 Manager 内置 App Lock/生物识别。 | 怎么解决/为何关闭：维护者拒绝，认为系统锁屏才是最终防线，root App 理论上可做同等操作。 | 可复用模块开发教训：安全设计不能依赖单一 Manager 密码；应依赖系统设备安全和最小权限。
- #1242 | 问了什么：升级 KSU 后 BusyBox 没升级。 | 怎么解决/为何关闭：维护者给出删除 `/data/adb/ksu/bin` 以重新生成的恢复方法。 | 可复用模块开发教训：内置工具更新应有版本检查与原子重建/回滚机制。
- #1243 | 问了什么：KSU 在 LXC/chroot/pivot_root 中是否会泄漏权限。 | 怎么解决/为何关闭：维护者说明 KSU 不处理 namespace、结果未定义，并建议检查被拦截的 `faccessat`/`newfstatat`/`execveat`；社区未得到通用支持结论。 | 可复用模块开发教训：不要假设容器 namespace 天然隔离 KSU；模块必须明确不支持/测试容器边界。
- #1248 | 问了什么：5.10 GKI 刷 AK3 后有 root 但不支持 overlayfs，且无源码。 | 怎么解决/为何关闭：只要求日志，无解决方案。 | 可复用模块开发教训：overlayfs 是内核能力；没有源码/配置就不能靠 Manager 或模块补齐。
- #1249 | 问了什么：选择允许 root 的 App 重启后不保存。 | 怎么解决/为何关闭：重复 #436/#943；社区给出将版本阈值 4.10 临时改为 4.15 的 4.14 hack，但未在 Samsung 上验证。 | 可复用模块开发教训：旧内核兼容补丁要按 init_keyring/加密行为验证，社区 hack 不应默认合入。
- #1252 | 问了什么：自 `b99701d` 后所有模块安装报“Failed to extract assets”。 | 怎么解决/为何关闭：维护者给 CI 构建 `7422996484`，报告者确认“现在可以用了”。 | 可复用模块开发教训：安装器资产提取回归需同时覆盖 Manager 和 `ksud module install` CLI。
- #1261 | 问了什么：模块 `REMOVE` 与 `system/product` 同用时出现 `/product/product`。 | 怎么解决/为何关闭：评论精确定位 installer 的 `mv` 在目标目录已存在时嵌套，提出替代 copy/remove/symlink；维护者只转 #2929，本条未见合入证明。 | 可复用模块开发教训：模块路径重写必须处理已存在目录、`REMOVE`/`REPLACE` 组合和幂等性。
- #1265 | 问了什么：Exynos 4.9 自编 KSU 模块界面与目录状态不一致。 | 怎么解决/为何关闭：报告者称合并 `sticpaper/android_kernel_xiaomi_msm8998-ksu@646d0c8` 后修复。 | 可复用模块开发教训：老内核需补齐 KSU 所需兼容提交，并以 CLI、目录和 Manager 三层验收。
- #1271 | 问了什么：`modules_update.img` 重启后不处理，模块变成空壳。 | 怎么解决/为何关闭：报告者称“Fixed with this patch. My bad.”，但 issue 未给可审查补丁细节。 | 可复用模块开发教训：更新镜像处理必须保留权限/上下文，并以重启后真实模块文件验证。
- #1275 | 问了什么：ColorOS 上 Manager 启动闪退。 | 怎么解决/为何关闭：定位到字体模块与默认卸载模块；删字体模块恢复，维护者建议使用字体模块时关闭 default-unmount。 | 可复用模块开发教训：系统字体/资源覆盖模块需明确 Manager 可见性例外或关闭默认 unmount。
- #1289 | 问了什么：Vivo 安全策略让 `ksud` 无法从 `/data/adb/ksud` 启动。 | 怎么解决/为何关闭：维护者不跟进；后续社区分析 OEM `vr.ko` 的反提权检测，有人自编内核/私有绕过成功，但无通用公开修复。 | 可复用模块开发教训：OEM 内核加固属于设备维护工程，模块不能承诺跨 Vivo 机型绕过。
- #1292 | 问了什么：将 KSU APK 预装进 AOSP 后显示“未安装”。 | 怎么解决/为何关闭：维护者只指向 #559 的评论，本条没有可核实结果。 | 可复用模块开发教训：预装 Manager 要验证签名、安装路径、包可见性和 KSU 期望签名绑定。
- #1294 | 问了什么：请求 android14-5.10.x 镜像支持。 | 怎么解决/为何关闭：无评论，`NOT_PLANNED`，无方案。 | 可复用模块开发教训：镜像支持要以实际 GKI 分支/构建产物为准，不能按“5.10.x”泛化。
- #1295 | 问了什么：请求每个模块只对指定 App/进程生效。 | 怎么解决/为何关闭：维护者说明有 root 的模块可改属性/注入 zygote，无法可靠限制作用域，指向 #759。 | 可复用模块开发教训：模块权限是全局能力；所谓 per-app scope 只能是可见性/挂载策略，不能当安全沙箱。
- #1296 | 问了什么：安装任何模块报 `mount failed: Invalid argument`。 | 怎么解决/为何关闭：维护者称“不是 bug”，未给修复。 | 可复用模块开发教训：挂载错误必须结合内核 overlayfs/参数限制诊断，不能假定 Manager 可修。
- #1303 | 问了什么：请求关闭自动检查更新。 | 怎么解决/为何关闭：`COMPLETED`，仅有支持评论，无 PR/维护者/报告者验证，未证实关闭。 | 可复用模块开发教训：设置类功能应有明确实现证据和持久化/离线测试。
- #1304 | 问了什么：自定义 Manager 使用系统签名会崩溃/无权限。 | 怎么解决/为何关闭：`ksud debug get-sign` 显示证书 1196B，超过 1024B；报告者改 `apk_sign.c` 的 `CERT_MAX_LENGTH` 后解决签名长度，但又暴露 `/data/adb` 不可访问，须按 #943 修内核。 | 可复用模块开发教训：自定义 Manager 的证书大小、hash、内核常量和 allowlist I/O 都必须配套更新。
- #1310 | 问了什么：手动集成时 `ksu_vfs_read_hook`/`ksu_execveat_hook` 未定义。 | 怎么解决/为何关闭：维护者判定为内核问题，无 KSU 侧补丁。 | 可复用模块开发教训：每个 hook 的声明、实现、调用点和配置必须来自同一集成版本。
- #1313 | 问了什么：KernelSU 是否支持 Ubuntu/x86_64 内核。 | 怎么解决/为何关闭：维护者明确“不支持 Linux PC”。 | 可复用模块开发教训：KSU 模块/Manager 的支持目标是 Android 内核栈，不能直接移植到桌面 Linux。
- #1318 | 问了什么：请求检测 Magisk 残留 `/data/adb/modules` 符号链接。 | 怎么解决/为何关闭：无评论、无实现证据。 | 可复用模块开发教训：安装前应主动检测并提示 Magisk 残留链接，避免模块重启后“消失”。
- #1320 | 问了什么：modules.img 是否固定大小、动态下载过大会怎样。 | 怎么解决/为何关闭：维护者明确大小为 zip 解压后大小 + 256MB；安装过程中动态下载过大将失败。 | 可复用模块开发教训：模块应预先声明最大落盘量，避免安装后下载大量文件。
- #1323 | 问了什么：在线游戏卡顿（无设备、日志等信息）。 | 怎么解决/为何关闭：无评论、无诊断。 | 可复用模块开发教训：性能问题至少需要设备、内核、模块清单和对照测试。
- #1324 | 问了什么：建议文档增加 `fastboot -w`，称 Pixel 7a 只有该命令后能启动。 | 怎么解决/为何关闭：关联 PR #1254 标题为 Build KernelSU as LKM，无法从本条证明文档已改；维护者仅反问是否试过 `magiskboot`。 | 可复用模块开发教训：`fastboot -w` 会擦除数据，不能作为默认安装说明，先用非破坏性修补/恢复流程。
- #1326 | 问了什么：`magiskboot` 报 ASN.1 DER tag “unexpected”是否为错误。 | 怎么解决/为何关闭：维护者确认不是错误，应报给 Magisk；报告者确认修补结果可用。 | 可复用模块开发教训：区分打包工具的 warning 与失败，仍需通过启动和验签验证输出镜像。
- #1327 | 问了什么：WSA 下 root 也无法访问 App data。 | 怎么解决/为何关闭：维护者指导用 `su -mm`/ADB 检查；报告者完整卸载重装 WSA 后自行恢复。 | 可复用模块开发教训：容器/WSA 权限异常先做干净环境复现，避免把残留状态误判为 KSU 权限模型。
- #1328 | 问了什么：Payback App 检测 root。 | 怎么解决/为何关闭：Shamiko、冻结/隐藏 Manager、改 ROM 等社区办法结果不一致，最终有人换 ROM/Magisk；无上游通用修复。 | 可复用模块开发教训：反检测兼容不能承诺；应提供无模块对照和版本矩阵，避免诱导用户降级安全性。
- #1331 | 问了什么：5.4 内核在 system 上挂 overlayfs 失败。 | 怎么解决/为何关闭：维护者给出测试提交/CI，用户确认测试构建修复；后续定位为 overlay 参数过长，内核 256 限制提高到 4096 后恢复。 | 可复用模块开发教训：大量模块会拉长 `lowerdir`，安装器要考虑内核参数长度/分批或兼容补丁。
- #1338 | 问了什么：无法禁用或卸载 bootanimation 模块。 | 怎么解决/为何关闭：维护者给 CI 后因无反馈关闭；后续仍有人复现，社区仅给 `rm -rf /data/adb/modules/<name>` 手工恢复。 | 可复用模块开发教训：卸载必须有幂等 CLI/Manager 测试；手删只能是备份后的故障恢复，不是确认修复。
- #1339 | 问了什么：`$MODPATH/system` 未从 modules_update 移到 modules。 | 怎么解决/为何关闭：无评论、无结论。 | 可复用模块开发教训：模块更新转换应覆盖目录移动、符号链接和低版本内核场景。
- #1341 | 问了什么：Tab S6 Lite Exynos 手动/kprobe 集成后 root 和模块均不可用。 | 怎么解决/为何关闭：维护者转 #943，报告者承认是无效 issue；无直接修复。 | 可复用模块开发教训：非 GKI 手动集成先按 #943 的前置条件完成自检。
- #1342 | 问了什么：集成 KSU 后偶发启动冻结，日志含 `ksud` 栈。 | 怎么解决/为何关闭：维护者要求完整 pstore，并建议检查 kprobe 集成；报告者后来看到 OOM，但无确定根因/修复。 | 可复用模块开发教训：不要从截取栈直接定因；启动冻结需完整 pstore、内存和 hook 配置。
- #1344 | 问了什么：请求加入 Galaxy Tab S6 Lite LTE。 | 怎么解决/为何关闭：Action 自动创建 PR #1345。 | 可复用模块开发教训：新设备应同时提交可维护源码与测试证据。
- #1346 | 问了什么：Tab S6 Lite 重启后 root、Profile 和 default-unmount 设置都重置。 | 怎么解决/为何关闭：报告者确认 4.14 缺少所需 init_keyring；构建时启用 pre-4.10 支持或升级到较新 4.14 可解决。 | 可复用模块开发教训：旧内核版本号不足以代表能力，需检查 init_keyring 等具体 backport。
- #1348 | 问了什么：自定义 backend 的 zip 在 KSU Manager 中不会按预期刷入。 | 怎么解决/为何关闭：维护者说明 KSU 有意替换 backend，不支持在 TWRP 安装模块；讨论未改变该策略。 | 可复用模块开发教训：面向 KSU 的模块必须使用受支持的 installer/`customize.sh`，不能依赖 Magisk 的自定义 update-binary 兼容性。
- #1350 | 问了什么：KSU 是否/如何可用于 syscall tracing。 | 怎么解决/为何关闭：维护者称仍在计划，推荐外部项目 stackplz；无 KSU 模块实现。 | 可复用模块开发教训：不要把“内核级 root”误解为现成 syscall tracing API；需要独立内核工具与安全设计。
- #1354 | 问了什么：HTTP Toolkit 经 ADB 挂入 CA 后 App 看不到挂载，如何兼容 KSU。 | 怎么解决/为何关闭：维护者解释默认会对 App unmount 修改；可在 App Profile 调整，或卸 KSU overlay 后再挂/重挂，并参考 `0c52f24` 进入目标进程 mount namespace。 | 可复用模块开发教训：外部工具的 mount 必须与 KSU overlay/unmount 时序协调；全局关闭隐藏会影响金融 App，应让用户知情。
- #1355 | 问了什么：无意义的“2/3”内容。 | 怎么解决/为何关闭：`COMPLETED`，无任何技术证据，未证实关闭。 | 可复用模块开发教训：无最小复现的信息不能形成模块知识或修复结论。
- #1357 | 问了什么：Yono SBI/Moviehub 检测 root，Yono 崩溃。 | 怎么解决/为何关闭：报告者称刷 Shamiko 后 Yono 崩溃缓解，维护者转 #1328；对所有 App 无通用确认。 | 可复用模块开发教训：第三方隐藏模块的单 App 成功必须标为设备/版本特定。
- #1358 | 问了什么：请求给 Manager 加口令/生物识别保护。 | 怎么解决/为何关闭：维护者转 #1238，沿用“系统锁屏才是最终防线”的拒绝结论。 | 可复用模块开发教训：模块安全不应建立在可被 root 环境绕过的单 App 二次口令上。

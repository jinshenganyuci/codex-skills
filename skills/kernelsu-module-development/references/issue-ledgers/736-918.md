# KernelSU closed-issue ledger — JSONL 736–918

口径：每条均阅读题干、`stateReason`、关联 PR 和存档中的最后 25 条评论。`COMPLETED` 本身不等于已修复；没有可核对的修复回复、提交或确认时，明确标为“未证实关闭”。

## 736–782

- #1806 | 问：begonia（4.14）在 Android 14/VoltageOS 上不受支持，如何启用 KSU/Zygisk。 | 解决：NOT_PLANNED；维护者说明低于 5.10 的旧基线不再支持，只能自行回退 `898e9d4` 后构建。 | 教训：模块不能弥补未支持的内核基线；先验收内核/KMI，再谈模块兼容。
- #1809 | 问：Windows 的 Manager 构建工作流中，`ksud-x86_64-pc-windows-gnu` 不能给外置 LKM 打补丁。 | 解决：维护者要求传入 `--kmi <KMI>`，并建议用 WSL；属于工具参数/环境问题。 | 教训：离线修补工具必须显式传递 KMI，并在文档中限定受支持的主机环境。
- #1813 | 问：`/system/bin/su` 不存在，终端无法取得 root。 | 解决：讨论指向终端包装方式、应用架构及可能漏接 32 位 hook，未有官方确认修复，属未证实关闭。 | 教训：模块测试要分别覆盖原生 shell、Termux 包装、32/64 位进程，不能只验证 Manager 授权页。
- #1818 | 问：5.10.177 是否已不再支持。 | 解决：NOT_PLANNED；维护者转向 KMI 安装文档，未恢复旧版本支持。 | 教训：发布包/模块应以 KMI 和实际支持矩阵选型，勿承诺已弃用的精确内核小版本。
- #1822 | 问：请求改进小米系统分身等多用户下的授权管理。 | 解决：NOT_PLANNED，仅指向 #1705，未给可验证的实现或替代方案。 | 教训：多用户模块必须把 userId/UID、授权隔离和重启持久化作为独立测试矩阵。
- #1825 | 问：AirMusic 被拒绝 root。 | 解决：NOT_PLANNED；维护者明确认为与 KernelSU 无关。 | 教训：模块兼容报告先排除目标 app 自身限制，并提供最小复现与日志。
- #1826 | 问：Android 11 安装 Manager 报 `INSTALL_FAILED_NO_MATCHING_ABIS`。 | 解决：NOT_PLANNED；维护者说明 Manager 不支持 32 位设备。 | 教训：模块/辅助 APK 要在发布前声明 ABI，安装器应尽早给出 32/64 位不兼容提示。
- #1827 | 问：请求导入/导出授权与设置。 | 解决：NOT_PLANNED，仅指向 #1705，未证实已有功能或修复。 | 教训：若模块保存配置，应自行提供可审计的备份/恢复格式，不假定 Manager 有全局迁移 API。
- #1828 | 问：请求随机化 Manager 包名以回避银行 app 检测。 | 解决：NOT_PLANNED；仅有社区讨论，无官方实现或确认。 | 教训：不要把改包/伪装写成模块能力或安全承诺；功能应在正常安装状态下工作。
- #1833 | 问：遇到 `EACCES`。 | 解决：NOT_PLANNED，仅链接 #1705，未有诊断或修复证据。 | 教训：权限错误必须附命令、SELinux 上下文、挂载和日志，不能从关闭状态推断解决。
- #1836 | 问：Moto G54 的 GKI 镜像可用而 LKM 显示“未安装”。 | 解决：COMPLETED 但零评论、零 PR，未证实关闭。 | 教训：LKM 安装失败应保留 boot 日志、KMI、模块加载状态和 Manager 搜索日志。
- #1838 | 问：为何部分具 root 能力的 app 不弹窗请求 `su` 授权。 | 解决：维护者明确拒绝被动/自动授权模型，认为预先、主动授权才安全。 | 教训：模块绝不能静默扩大 root；把授权设计成显式 allowlist 和最小权限。
- #1839 | 问：锁定 bootloader 能否使用 KSU LKM。 | 解决：维护者说明除非存在提权漏洞，通常不可行。 | 教训：模块安装流程只能以已解锁/可刷写的受支持设备为前提，不能依赖绕过 boot 链信任。
- #1840 | 问：怎样从 GKI 模式方便切到 LKM，KMI 小版本是否有关。 | 解决：需按官方方式修补/刷写 `boot` 或 `init_boot`；KMI 不按内核 sublevel 判断。 | 教训：安装脚本应检测真实分区与 KMI，不应把 GKI/LKM 切换当成纯 Manager 内开关。
- #1845 | 问：旧非 GKI 内核用 clang-17 编译失败。 | 解决：NOT_PLANNED，指向 #1705/#1972，未给官方补丁。 | 教训：旧内核移植需维护独立补丁集和工具链 CI；不要把主线支持外的错误归为模块问题。
- #1852 | 问：安装时 `create temp dir failed`。 | 解决：维护者确认 Manager 无法访问 `/sdcard/Download` 导致失败。 | 教训：模块安装器应使用可写的私有/临时目录，并在路径权限错误时给出可操作提示。
- #1854 | 问：请求 QGKI 支持。 | 解决：NOT_PLANNED，仅有提问，无实现或官方结论。 | 教训：厂商内核分支不能因名称相近就假设 ABI 兼容；先做 KMI/符号验证。
- #1855 | 问：发送日志界面出现重复“保存日志”。 | 解决：维护者给出修复提交 `51337ee`。 | 教训：模块 WebUI/Manager 文案变更也应有可追溯提交和回归截图。
- #1856 | 问：魅族 20 Pro LKM 在 0.9.5/1.0.0 不工作、0.9.4 正常。 | 解决：报告者称 1.0.1 后重新修补可用；无关联 PR，属用户确认而非通用官方修复。 | 教训：升级后应重新修补并记录设备/KMI；单机回归不能推广为全部 LKM 兼容结论。
- #1857 | 问：官网/Release 找不到 `ksud` 二进制。 | 解决：讨论说明可从 CI/Manager APK 的 `libksud.so` 获取使用线索，未涉及缺陷修复。 | 教训：模块工具链应固定可信 release/CI 来源与校验值，不要猜测 APK 内部资源布局。
- #1862 | 问：能否在 Magisk root 下直接 `insmod kernelsu.ko`。 | 解决：NOT_PLANNED；维护者明确不支持该加载方式。 | 教训：不要把另一套 root 环境当 KSU LKM 加载器；使用官方修补/安装路径。
- #1871 | 问：自编内核可启动却不支持 KSU。 | 解决：NOT_PLANNED，讨论归入 #1705 的旧/非 GKI 支持边界，未有具体补丁。 | 教训：内核集成要核对 Kconfig、hook、版本和 Manager 协议，不把“能启动”当集成成功。
- #1874 | 问：刷入内核后 Manager 不工作。 | 解决：NOT_PLANNED，指向 #1705；无经验证的解决步骤。 | 教训：刷机报告应同时给出驱动版本、Manager 版本、KMI 和完整 bugreport。
- #1876 | 问：`umount debug_ramdisk` 反复失败。 | 解决：NOT_PLANNED，归入旧/非 GKI 问题，未证实修复。 | 教训：模块 mount/umount 脚本要处理不支持的挂载点并输出可诊断错误。
- #1877 | 问：非 GKI 设备的模块在 Manager 中无法安装。 | 解决：报告者称外部提交 `646d0c8` 可绕过；不是维护者确认的上游方案。 | 教训：不要把第三方补丁当官方兼容层；模块需声明对 legacy fork 的额外风险。
- #1882 | 问：Redmi Note 13 5G 升级后 ProxyDroid 等异常。 | 解决：仅有用户回退版本的经验，未有上游根因或修复，未证实关闭。 | 教训：模块兼容回归应逐项 bisect KSU、内核、ROM 和模块版本，避免以回退替代诊断。
- #1897 | 问：非 GKI 小米 9 使用 LSPosed 后 RootList 不显示。 | 解决：NOT_PLANNED 且零评论，未证实关闭。 | 教训：注入框架与 root 列表交互需最小化复现；无日志的关闭不可作为兼容结论。
- #1917 | 问：编译出现 `current_sid` 重定义。 | 解决：NOT_PLANNED，归入 #1705 类旧内核适配，未有官方补丁。 | 教训：SELinux 接口冲突必须按目标内核头文件做条件编译，不能机械套用新树代码。
- #1929 | 问：小米系统管家突然检测到 root。 | 解决：报告者重新刷写后现象消失，但无确定根因或官方修复。 | 教训：不要保证“隐藏 root”；排查应隔离 boot 状态、模块、Manager 与系统安全扫描版本。
- #1932 | 问：还原镜像后无法 OTA。 | 解决：COMPLETED 但仅有追问，无解决证据，未证实关闭。 | 教训：OTA 流程应先验证恢复原厂分区、槽位和 AVB，保留可回刷的原始镜像。
- #1939 | 问：请求提供不含 KSU 的“干净”内核镜像。 | 解决：NOT_PLANNED 且零评论，未证实关闭。 | 教训：模块项目不应依赖维护者提供任意设备的定制内核；用设备官方镜像做基线。
- #1940 | 问：请求 Manager 签名密钥。 | 解决：NOT_PLANNED 且零评论，未证实关闭。 | 教训：绝不索取、分发或复用发布签名密钥；模块以独立签名和可验证发布链交付。
- #1952 | 问：刷入后存储显示异常/巨大容量。 | 解决：社区建议最小化 sparse 镜像，但提醒可能破坏模块；无维护者确认，未证实修复。 | 教训：模块应避免假设 sparse/loop 行为无副作用，并实测存储统计、TRIM 与大模块升级。
- #1954 | 问：11944 无法安装字体模块。 | 解决：COMPLETED 但零评论，未证实关闭。 | 教训：字体类模块需记录安装器日志、overlay 路径和 Android/厂商差异，不能把状态当兼容证明。
- #1961 | 问：Pixel 5a Android 14 上 Manager 显示获取 root 失败。 | 解决：COMPLETED 但零评论，未证实关闭。 | 教训：root 自检失败要先区分 Manager、`su`、内核驱动及 SELinux，附完整日志再定因。
- #1967 | 问：键盘精灵等 app 不能取得 root。 | 解决：NOT_PLANNED；维护者确认 32 位 app 不受支持。 | 教训：模块依赖 app 要标注 64 位前提，不能以 32 位兼容作为默认要求。
- #1970 | 问：能否用 DSU 提取 boot，作为 LKM 安装替代流程。 | 解决：维护者建议临时 `fastboot boot`，再做 LKM 安装或 `dd` 回原始 boot；DSU 方案未采纳。 | 教训：救援/安装文档应优先写受支持的可回退步骤，不引入未经维护的旁路流程。
- #1972 | 问：非 GKI 手工集成时出现缺少类型说明的编译错误。 | 解决：题干给出 v0.9.5/回退相关提交的 workaround，但无评论确认，不能当官方修复。 | 教训：legacy 适配应将版本钉死和变更说明写入构建脚本，避免自动追最新主线。
- #1973 | 问：非 GKI 编译出现 `ksu_vfs_read_hook` 等未定义符号。 | 解决：题干给出关闭 KPROBES 或补手工 hook 的方向，但零评论，未证实。 | 教训：hook 模式必须二选一且确保所有符号/Makefile 同步，禁止半套集成。
- #1986 | 问：银行 app 开始检测 root。 | 解决：NOT_PLANNED 且零评论，未证实关闭。 | 教训：模块不得承诺绕过金融 app 风控；保持功能、风险说明和设备状态检测分离。
- #1992 | 问：提交 Redmi 9（lancelot）设备支持。 | 解决：GitHub Actions 报告创建 PR #1993，但关联字段未证明合并。 | 教训：设备支持需以实际合并/发布为准，自动建 PR 只是提交流程的起点。
- #1996 | 问：Nintendo Switch 等非 GKI 自定义构建失败。 | 解决：NOT_PLANNED；维护者说明 1.0+ 不再支持非 GKI，最后官方线为 v0.9.5。 | 教训：模块/内核工具必须明确 legacy 截止版本，禁止让旧设备自动升级到不兼容 Manager。
- #2000 | 问：卸载模块弹窗一直不消失。 | 解决：COMPLETED 但只指向 #1705，无修复证据。 | 教训：UI 卡住要记录操作、线程/日志与版本；不能以关闭状态推断已修好。
- #2004 | 问：LKM 的 `kernelsu.ko` 如何读取/提取内核 ramdisk，以实现类似 `overlay.d`。 | 解决：维护者仅指向 `userspace/ksud/src/boot_patch.rs`，没有解决运行时提取问题。 | 教训：模块应使用文档化的 systemless 路径；内核修补能力与运行时模块逻辑不能混为一谈。
- #2005 | 问：提交 Moto G7（river）自定义内核。 | 解决：Actions 报告创建 PR #2006，但无关联关闭 PR/合并证据。 | 教训：设备清单类模块依赖必须核验上游实际收录，不能只看机器人回复。
- #2011 | 问：升级 Manager 到 11960 后所有模块消失。 | 解决：报告者称降级恢复，未有上游修复或根因，未证实关闭。 | 教训：模块升级器要支持导出状态和回退；版本回退是排查线索，不是通用修复。
- #2019 | 问：部分 HyperOS 包没有 `init_boot.img`，如何安装。 | 解决：讨论结论是该设备应修补 `boot`，而非硬找 `init_boot`。 | 教训：安装器必须根据固件真实分区布局选择目标，不能假定所有设备都有 `init_boot`。

## 783–825

- #2026 | 问：安装模块时 Manager 崩溃。 | 解决：DUPLICATE；评论猜测可关闭隐藏应用/私密空间功能，但未给出规范 issue 或确认修复。 | 教训：模块安装故障要隔离 ROM 的私密空间/多用户特性，不能把社区猜测写成依赖。
- #2027 | 问：安装、卸载、启停模块时报 `Failed to attach loop`。 | 解决：NOT_PLANNED；维护者仅推测可能有 root app 占用 loop 设备，未证实。 | 教训：模块安装器应检查 loop 设备占用并输出竞争进程/挂载证据，避免破坏性抢占。
- #2032 | 问：构建时 `su` 目标与 `system/extras/su` 重复定义。 | 解决：仅有外部社区提交 `65b757d`，无官方确认。 | 教训：内核/ROM 集成要检测同名 build target，采用显式命名空间或条件构建。
- #2037 | 问：能否把 Magisk 模块自动转换为 KSU 模块。 | 解决：NOT_PLANNED；维护者说明 KSU 不能模拟 Magisk 内部特性，并建议检查默认卸载模块开关。 | 教训：模块要按 KSU 生命周期、挂载和工具接口原生开发，不能承诺通用 Magisk 转换。
- #2042 | 问：使用 KSU 后 Kernel Flasher 无法工作。 | 解决：NOT_PLANNED；维护者判断是 Kernel Flasher 的问题。 | 教训：模块/工具兼容问题应先在目标 app 维护渠道复现，不混淆责任边界。
- #2044 | 问：LXC/Waydroid 环境无法安装模块。 | 解决：提问者在继续调查时自行关闭，未有修复。 | 教训：容器 Android 的挂载、loop、SELinux 与真机不同，必须单列支持矩阵。
- #2045 | 问：0.9.3 后按键精灵无法正确获 root。 | 解决：NOT_PLANNED；维护者确认 32 位 app 不受支持，且与 #1813 重复。 | 教训：模块的 companion app/自动化脚本应保证 64 位，不应依赖 legacy 32 位可执行文件。
- #2061 | 问：空白/格式错误的设备添加请求。 | 解决：Actions 无法创建 PR，COMPLETED 不代表收录。 | 教训：自动化输入要做必填字段校验；模块设备目录的提交需可解析仓库、设备和维护者信息。
- #2064 | 问：WebUI API 的 `spawn` 方法无效。 | 解决：评论指向 #1395，称预期在 1.0.7 修复，但没有本 issue 的 PR/验证，未证实。 | 教训：模块 WebUI 应给异步命令提供版本检测、错误回传和降级路径。
- #2067 | 问：文档称 LKM 可临时卸载，实际 `rmmod` 会重启。 | 解决：NOT_PLANNED；维护者确认这是 TODO，尚未实现。 | 教训：模块文档只能声明已实现能力；涉及卸载内核模块的操作必须提供安全回退。
- #2077 | 问：没有 Manager 时如何使用 KSU/让 ADB shell 默认 root。 | 解决：维护者建议仅调试内核启用 `CONFIG_KSU_DEBUG`，并明确非 debug 默认 root 有风险。 | 教训：开发模块的调试开关不得进入生产配置；默认最小授权并保留显式授权入口。
- #2080 | 问：ARCVM 卡在模块 setup。 | 解决：NOT_PLANNED；因无 ARCVM 维护者承接而关闭。 | 教训：虚拟化目标需要专属维护/测试资源；不应把其失败当普通模块安装问题。
- #2088 | 问：如何备份模块，恢复 sparse 文件大小后怎样恢复设置。 | 解决：维护者说明安装任意模块即可恢复 sparse-file 设置。 | 教训：模块备份/恢复流程应写明镜像重建触发条件，并先验证现有模块可见性。
- #2090 | 问：如何编辑 `build.prop`。 | 解决：维护者要求改用模块的 `system.prop`，而非直接改 `build.prop`。 | 教训：系统属性应通过模块声明式文件注入，禁止直接 remount/改只读系统分区。
- #2091 | 问：AVD 中手工修补 `ramdisk.img` 的 LKM 不工作。 | 解决：NOT_PLANNED；维护者认为 AVD LKM 不受支持/可能不符合 GKI。 | 教训：模拟器与真机的内核模块 ABI 不可假定一致；先确认其 GKI/KMI 和模块包同步。
- #2095 | 问：Samsung M14 在新版本后无法给部分 app 完整 root。 | 解决：NOT_PLANNED；维护者定位到 v0.9.2 后 32 位 app 不支持，建议使用 64 位 app；手工 hook 说法未证实。 | 教训：模块兼容排查先查 ABI，再考虑 hook/SELinux，不要用未验证的内核补丁掩盖应用位数问题。
- #2101 | 问：v0.9.5 不支持 Android 4.4 内核。 | 解决：社区提供构建脚本/提交后报告可用，但无官方 PR 证据。 | 教训：4.4 等旧内核只应作为社区移植实验，维护独立分支和完整启动回归。
- #2108 | 问：没有独立 vendor 分区时无法修改 `/vendor`。 | 解决：讨论归因于 ROM/system-as-root 布局，安装器行为被认为正确。 | 教训：模块 overlay 目标要以实际挂载/分区拓扑判断，不能只按目录名推断独立分区。
- #2116 | 问：再次提交 Redmi 9（lancelot）设备。 | 解决：Actions 报告创建 PR #2117，仍无关联字段证明合并。 | 教训：重复设备提交前先查询现有 PR/目录，发布前必须核验合并结果。
- #2119 | 问：内置 BusyBox 的 `getopt` 不能直接调用。 | 解决：维护者说明 KSU BusyBox applet 从不加入 `$PATH`，应显式 `busybox getopt`。 | 教训：模块脚本要使用绝对 BusyBox 路径或显式前缀，不能依赖隐式 PATH 注入。
- #2130 | 问：请求恢复非 GKI 的模块安装支持（`os error 38`）。 | 解决：NOT_PLANNED；官方明确停止非 GKI 支持，社区 fork 不是上游解决方案。 | 教训：模块发布应锁定官方 legacy 最后版本并拒绝自动升级，不把 fork 行为写作官方能力。
- #2131 | 问：任意模块更新时 Manager 崩溃。 | 解决：评论归因于过旧的非 GKI 驱动，建议重编为 v0.9.5；无官方确认闭环。 | 教训：Manager/ksud/内核驱动须按协议版本配套；模块更新失败先核验三者版本。
- #2137 | 问：仅贴出外部 android-x86 `init` 链接。 | 解决：NOT_PLANNED，评论无可复用诊断或修复。 | 教训：issue/模块报告必须包含问题、环境、复现和日志；外链不能替代最小证据。
- #2168 | 问：Android 8.1 是否支持，kprobe 集成后 `su` 不存在。 | 解决：报告者称改用手工集成而非 kprobe 后可用，属社区单机经验。 | 教训：旧 Android/内核集成必须验证 hook 机制；手工路径需维护可复现 patch，而非只靠口头结论。
- #2171 | 问：模块 action 按钮执行后，`module.prop` 描述变更却不自动刷新。 | 解决：关联 PR #2201“Fix not refreshing after action button was pressed”已合并。 | 教训：`action.sh` 改状态后应触发 UI reload；模块状态应写入可重新读取的 `module.prop`。
- #2173 | 问：索要“无极码”。 | 解决：NOT_PLANNED 且零评论，未证实关闭。 | 教训：模块项目不得索取/分发不明兑换码或凭据，保持发布物可验证。
- #2177 | 问：在模块脚本中嵌套 `ksu module install ZIP` 批量安装失败。 | 解决：维护者给出提交 `a953ae9`，但 issue 未说明具体改动。 | 教训：批量安装必须避免重入安装会话，并对每个 ZIP 的退出码/日志顺序处理。
- #2178 | 问：ICICI iMobile 突然检测 root，多个绕过栈无效。 | 解决：评论互相矛盾，且指出解锁 boot/环境指纹也会触发；无官方修复。 | 教训：不要为金融 app 承诺隐身；模块应避免扩大检测面，并将兼容性作为尽力而为。
- #2179 | 问：Redmi 8 非 GKI 在 1.0.2 无法安装模块并称有内存泄漏。 | 解决：NOT_PLANNED；维护者/社区均说明 1.0.2 不支持非 GKI。 | 教训：安装器应在 legacy 内核上拦截不兼容版本，给出降级/支持边界而非模糊 OS 错误。
- #2180 | 问：升级后不能刷入模块。 | 解决：NOT_PLANNED；与 #2179 重复，临时办法是降级 Manager，官方不再支持非 GKI。 | 教训：模块文档须把“临时降级”与“官方支持”分开标注，避免误导长期依赖。
- #2184 | 问：非 AArch64/x64 Linux 如何从 `libksud.so` 提取嵌入的 `.ko`。 | 解决：讨论结论是静态嵌入且无导出符号；离机修补应从 Release 下载 `.ko`，或在能运行 `ksud` 的环境完成 patch。 | 教训：不要依赖解析 APK 私有二进制；离线工具应消费公开 Release 工件和明确架构支持。
- #2185 | 问：刷 KSU 后 128GB 设备显示 2TB 存储。 | 解决：评论指向官方 FAQ 的巨大 1TB 文件说明，未显示具体代码修复。 | 教训：模块使用 sparse/loop 前应说明存储展示副作用，并提供无模块/卸载后的验证步骤。
- #2186 | 问：1.0.2 安装模块报 `OS error 38`。 | 解决：NOT_PLANNED；确定为非 GKI 支持在内核/用户态被移除，社区仅给旧版临时办法。 | 教训：模块安装失败提示要区分 FUSE/loop 故障和“不受支持的 legacy 内核”。
- #2200 | 问：1.0.1 内核搭配 1.0.2 Manager，`addon.sh` 能否正常用。 | 解决：NOT_PLANNED；维护者明确非 GKI 应留在 1.0.1 Manager，1.0.2 不可用。 | 教训：模块 API/Manager 兼容性应以内核线决定，禁止跨 major userspace 混搭。
- #2209 | 问：手工集成后编译报找不到 `drivers/kernelsu/Kconfig`。 | 解决：维护者直接指出缺失该路径，评论追问 symlink 是否存在。 | 教训：集成脚本要原子创建/验证源码链接、Kconfig 和 Makefile，再启动完整构建。
- #2210 | 问：没有浏览器时点击更新会使 Manager 崩溃。 | 解决：NOT_PLANNED；维护者建议手动更新，没有修复承诺。 | 教训：模块/Manager 的外链更新应先检测 intent handler，并提供下载文件的离线导入路径。
- #2214 | 问：`action.sh` 内无限 `while` 监听音量键会卡住执行页。 | 解决：COMPLETED 但无评论、PR 或确认，未证实关闭。 | 教训：`action.sh` 应短时、非交互、可取消；长驻输入循环应移到服务并有超时。
- #2218 | 问：Termux 的 LKM root 下 `pm uninstall` 报 Binder transaction 失败。 | 解决：报告者找到 `cmd package uninstall` 可用，且 `adb shell su -c pm` 正常；无上游修复。 | 教训：模块脚本操作包管理时优先用 Android `cmd package`，并跨终端/namespace 测试。
- #2221 | 问：请求增加 5.4 的 GKI（11 月 patch）。 | 解决：NOT_PLANNED；维护者明确 5.4 不是 GKI 2.0。 | 教训：选择预编译内核先确认 GKI generation，不能只按版本号命名。
- #2225 | 问：请求模块不使用 image/sparse 文件，以避免 1TB 显示、检测和 WebUI 不一致。 | 解决：维护者给出 `de29115`、`4ede20e` 两个提交；设计讨论仍提醒目录复制/更新路径复杂。 | 教训：无镜像/可选挂载实现必须同时覆盖安装、`modules_update`、可见性和 rollback，不只跳过创建镜像。
- #2234 | 问：重启后全部 root 授权被重置。 | 解决：维护者说明 v2.0 已通过不再给 kworker 装 keyring 解决。 | 教训：授权持久化需要跨重启实测；内核 keyring/workqueue 设计会影响 allowlist 生命周期。
- #2235 | 问：OnePlus 13 修补/临时启动后 Manager 始终显示未安装。 | 解决：用户称重装 Manager 有时可用、他人仍复现，未有维护者确认根因，未证实关闭。 | 教训：安装检测必须报告 Manager 搜索、APK 路径和分区状态；“重装 APK”仅是诊断线索。
- #2236 | 问：MediaTek Kernel Protect 在 Android 14/6.1 有时因运行时提权触发 panic。 | 解决：COMPLETED 但零评论/PR，未证实关闭。 | 教训：厂商安全扩展与 root hook 冲突要保留 panic/ramoops，模块不得在未知环境中强行提权。

## 826–870

- #2245 | 问：ColorOS 下 ES 文件管理器、Swift Backup 等部分 app 获 root 失败。 | 解决：维护者确认 KSU 不支持 32 位 app；Swift 的社区替代为给 XPL-EX 的 Intercept Shell 授权，非通用修复。 | 教训：模块兼容清单应标注 ABI 与进程模型，不能把第三方桥接 app 当官方 root 能力。
- #2250 | 问：模块 WebUI 操作后，模块页不自动刷新描述。 | 解决：评论明确称由 #2317 解决。 | 教训：WebUI 写入状态后应通知/刷新模块列表，避免依赖用户手动下拉。
- #2254 | 问：KSU BusyBox `udhcpc6` 触发 stack corruption。 | 解决：评论和维护者均确认已由 PR #2383 修复。 | 教训：随仓 BusyBox 应由可复现 CI 构建并做 applet 冒烟测试，避免手工二进制漂移。
- #2257 | 问：Manager 只有重装后才检测到 KSU。 | 解决：DUPLICATE；有“加深搜索深度”社区 workaround，但无关联 PR/官方确认，未证实根因。 | 教训：Manager 发现 APK 时应覆盖嵌套安装目录，并把搜索深度/命中路径写进诊断日志。
- #2274 | 问：`.ksurc` 中的 `PS1` 不生效。 | 解决：讨论归因为 `ash` 限制，建议在 `.ksurc` 启动 Termux `bash --rcfile`；报告者接受。 | 教训：模块 shell 脚本要按 `/system/bin/sh` 的 POSIX/ash 能力编写，Bash 特性须显式引入。
- #2276 | 问：安装 Viper/Audio Modification Library 后 Chromium 浏览器断网/标签异常。 | 解决：建议将所需内容全局挂载或关闭默认卸载模块；报告者确认有效。 | 教训：依赖全局可见系统文件的模块要声明挂载需求，并最小化全局挂载范围。
- #2285 | 问：银行 app 通过 Manager 包名/图标检测，要求隐藏或改名。 | 解决：DUPLICATE；只有社区绕过建议，无官方功能或验证。 | 教训：不要将伪装包名作为模块设计目标；保持模块功能独立于 Manager 是否安装。
- #2291 | 问：Samsung S22 最新固件修补后 bootloop。 | 解决：维护者说明 Samsung RKP/RDP 阻止内核加载模块，只能走 GKI；后续 vbmeta 建议属社区路径。 | 教训：Samsung 设备需先识别 Knox/RKP/RDP 与 GKI 约束；不要把 LKM 修补当通用方案。
- #2304 | 问：选择 `boot.img` 准备修补时 Manager 崩溃。 | 解决：COMPLETED 但零评论/PR，未证实关闭。 | 教训：镜像选择/patch UI 要做文件格式、权限和异常捕获测试，保存 crash/bugreport。
- #2312 | 问：6.1 中 `vfs_statx` hook 不工作，`su` 不可用而移到 `vfs_fstatat` 可用。 | 解决：NOT_PLANNED；评论只称缺提交并给外部实现，没有官方补丁。 | 教训：手工 hook 必须随目标内核 syscall/VFS 签名变化适配，不能复制旧版本位置。
- #2315 | 问：执行模块 action 后再点 Home 等按钮无响应。 | 解决：维护者给出提交 `de2100e`。 | 教训：action shell 生命周期要与 UI navigation 解耦，执行完必须释放 root shell/界面状态。
- #2322 | 问：仅要求“Please publish”。 | 解决：NOT_PLANNED；维护者没有采纳，未形成技术结论。 | 教训：模块需求需给出功能、受众、复现或设计动机，模糊请求不可转化为接口承诺。
- #2325 | 问：Pixel 7 Android 15 QPR2 Beta2 修补 `init_boot` 后无 root。 | 解决：维护者转到 #2650；本条后续多设备仍报告失败，未证实修复。 | 教训：Beta ROM/新分区布局必须单独验收 `boot`、`init_boot`、`vendor_init_boot`，不混同稳定版流程。
- #2326 | 问：Samsung Note 9 点击 KSU 即重启。 | 解决：评论怀疑仍开启 `CONFIG_UH`，维护者指向 #1705；没有完成验证。 | 教训：集成时需审计厂商反 root 配置，保留可回刷镜像后逐项禁用/验证。
- #2337 | 问：模块显示已刷入却不生效。 | 解决：COMPLETED 但零评论/PR，未证实关闭。 | 教训：模块安装成功与运行生效须分开验证：日志、挂载、属性/文件和目标行为都要检查。
- #2338 | 问：任何模块安装都报 `function not implemented (os error 38)`。 | 解决：DUPLICATE；维护者归入 #2130 的非 GKI 支持终止，旧 1.0.1 仅为临时回退。 | 教训：安装器要在不支持的内核上提供明确的 compatibility gate，而非暴露底层 errno。
- #2347 | 问：OnePlus OTA 后随机丢 root 授权/默认卸载设置。 | 解决：有一次 13341 构建测试通过，但后续 v2.1.1 仍复现且 #2922 尚未合并，未证实最终关闭。 | 教训：模块/Manager 必须在 A/B OTA 后校验 allowlist；备份 `.allowlist` 只能作应急，不应替代修复。
- #2350 | 问：提交 OnePlus 6 PixelExperience 内核设备支持。 | 解决：Actions 报告创建 PR #2351，未证明合并。 | 教训：设备条目只有合并和发布后才应被模块兼容表标为可用。
- #2352 | 问：切换槽位更新系统后掉 root。 | 解决：评论给出流程：还原原厂→安装更新但不重启→安装到未使用槽位→从系统更新页重启；OnePlus 情形需从更新页重启。 | 教训：模块/安装器 OTA 指南必须按槽位和 OEM 重启路径写明顺序，并准备重新 root 的回退方案。
- #2355 | 问：提交 Samsung Galaxy A05 设备支持。 | 解决：Actions 报告创建 PR #2356，未显示合并证据。 | 教训：不要把自动 PR 创建当设备已支持；引用设备清单前核验主线。
- #2357 | 问：Xiaomi 13 升 HyperOS 2 后刷镜像黑屏/bootloop。 | 解决：有人称 v1.0.3 GKI 成功，维护者称 GKI/LKM 都应可用，但后续仍有冲突报告，未证实通用修复。 | 教训：重大 ROM 升级后按模式、KMI、分区分别测试，单用户可启动不能替代兼容矩阵。
- #2368 | 问：root 后仍不能访问 `/data/user/0` 的 app 数据。 | 解决：先试 `su -mm` 无效，维护者随后称提交 `38027c9` 已修复。 | 教训：访问 app 数据要同时处理 mount namespace/SELinux；模块提供 root 时应明确 namespace 选项。
- #2369 | 问：初学者怎样本地编译 `libksud.so` 和 Manager APK。 | 解决：维护者仅建议先看 GitHub Actions，未给本地构建配方。 | 教训：模块工具链应以 CI 为可复现基线，并把本地构建依赖、目标 ABI 和产物路径写清。
- #2377 | 问：提交 Samsung Galaxy A04/A05 设备。 | 解决：Actions 无法创建 PR，要求检查内容或手工 PR，未证实收录。 | 教训：自动提交失败时应按模板修正字段/手工 PR，而非把 issue 关闭当完成。
- #2379 | 问：OnePlus 11R 更新后装模块，网络显示连接却无法访问。 | 解决：关闭默认卸载模块无效；报告者刷机/清理后无法复现并自行关闭，未有根因。 | 教训：网络模块/挂载冲突要抓路由、DNS、iptables、namespace 和模块二分，禁止以抹机作为结论。
- #2384 | 问：提交 Samsung S23 Snapdragon 设备。 | 解决：Actions 无法建 PR，要求检查内容或手工提交，未证实支持。 | 教训：模块兼容数据只消费已合并的设备源，不采信失败的机器人反馈。
- #2385 | 问：修补内核时报 `create temp dir failed`（Huawei）。 | 解决：维护者要求 bugreport 并给出 `f33b12c`；无报告者成功回执，提交是修复线索。 | 教训：针对 OEM 特殊权限先收集完整 bugreport；临时目录逻辑要错误可见且可回归验证。
- #2395 | 问：刷官方 6.6.30 AK3 后小米 15 Wi‑Fi/数据都不工作。 | 解决：维护者建议改用 LKM 模式。 | 教训：预编译 GKI 不能补齐 OEM 私有驱动；外设异常时优先切换受支持模式或自编设备内核。
- #2399 | 问：更新模块后 bootloop，所有内置救援方法均失败。 | 解决：提问者最终刷回 ROM boot 并清机；社区建议临时 recovery 删除 `/data/adb/ksu/`，非官方且有风险。 | 教训：模块必须提供 fail-safe/快速禁用；任何 recovery 删除前都要精确核对路径并保存可回刷镜像。
- #2402 | 问：OnePlus Ace 5 Pro LKM 后 Manager 重启又显示未安装。 | 解决：维护者要求正常/ADB bugreport；后来用户称最新 Actions Manager 不复现，属 CI 用户确认。 | 教训：Manager 检测问题要收集 root 前后两种日志，修复进入正式版前不要写成稳定兼容。
- #2403 | 问：Samsung S20 Snapdragon 手工非 GKI 集成后 bootloop。 | 解决：维护者指向 #1705；社区称已解决并提到禁 RKP/官方构建法，但无完整可审计方案。 | 教训：三星 legacy 移植需独立审计 Knox/RKP、构建脚本和启动链，避免照搬零散配置。
- #2404 | 问：S24 Ultra 刷 GKI 后 USB‑C 耳机失效。 | 解决：DUPLICATE；维护者说明 Samsung 私有 Type‑C 驱动不在 GKI，需用 Samsung 源码自编，LKM 也受 Knox 限制。 | 教训：模块不能替代厂商内核驱动；外设回归必须覆盖私有驱动和设备源码。
- #2405 | 问：Manager “关于”对话框可点击文字加粗失效。 | 解决：NOT_PLANNED；维护者本机正常，怀疑 Compose 组件差异，未给修复。 | 教训：模块/Manager UI 要跨系统字体/Compose 版本截图回归，不能由单机渲染推断普遍 bug。
- #2408 | 问：GPU 驱动更新触发 bootloop 时，自动救砖未按预期工作。 | 解决：NOT_PLANNED；评论认为自动救砖不适用于 GPU 驱动更新，未有修复。 | 教训：高风险内核/GPU 模块应预先备份并准备 fastboot/recovery，而非依赖通用自动救援。
- #2409 | 问：模块 action 完成后触发预测返回会无响应/崩溃。 | 解决：测试版被报告修好，维护者给出 `de2100e`。 | 教训：模块 action UI 要测试 Android 预测返回、取消和竞态，避免列表索引在 navigation 中失效。
- #2413 | 问：文档所说 LKM 临时禁用怎样操作。 | 解决：维护者明确“这是 TODO”，尚无指令。 | 教训：模块不能调用不存在的临时卸载接口；用显式授权/模块禁用替代危险的 `rmmod` 设想。
- #2416 | 问：电话/蓝牙等系统 app 无法更新 App Profile。 | 解决：内核为性能只支持 uid<2000 中的 uid=1000；关联 PR #2450 为“all apps 切换 unmount”提供针对性修复。 | 教训：模块 profile 要尊重 UID 优化边界，权限和挂载开关应拆成独立、最小范围的功能。
- #2417 | 问：Bazel 构建的 Android 13 内核显示 KSU 版本过低。 | 解决：维护者要求参考 GitHub workflow，未给直接补丁。 | 教训：Bazel/传统构建都应保留版本生成所需 git 元数据，并把 CI 配置当权威模板。
- #2421 | 问：给 Momo 授 `su` 后看到 SELinux `neverallow` 异常。 | 解决：NOT_PLANNED；维护者质疑为何授予该 app `su`，未提供修复。 | 教训：安全检测 app 不应默认获得 root；模块的 SELinux 调试应在最小授权测试环境进行。
- #2433 | 问：最新源码整合自定义 ROM，却显示旧 KSU 版本。 | 解决：评论定位 `--depth=1` 浅克隆令 git 版本变成 10201，需保留足够版本历史/正确版本生成。 | 教训：模块构建不要用破坏版本描述的浅克隆，或显式注入已验证的版本号。
- #2437 | 问：如何直接把 `/system` remount 为 R/W 以改属性并回避 app 检测。 | 解决：维护者要求使用模块 systemless 修改 `/system`。 | 教训：模块应以 `system/` overlay、`system.prop` 等 systemless 机制实现修改，不直接写只读分区。
- #2441 | 问：修补 `boot.img` 时创建临时目录失败。 | 解决：DUPLICATE 且零评论，未证实关闭。 | 教训：复现这类问题应附路径、存储权限、OEM 和 bugreport，并查找已知 temp-dir issue。
- #2442 | 问：非 GKI 是否还能升级、最后可用版本是什么。 | 解决：明确边界：驱动最后 v0.9.5，Manager 为 `fd09ccf` 之前的 v1.0.0，之后无官方更新。 | 教训：legacy 模块发布要硬性声明这两个上限，避免在不匹配 userspace 下安装/更新。
- #2446 | 问：Samsung A15 5G bootloop，缺少环境/日志。 | 解决：NOT_PLANNED；维护者仅要求先搜已有 issue，未证实修复。 | 教训：bootloop 报告至少要提供刷写分区、镜像来源、内核、模式和可恢复路径。
- #2462 | 问：Superuser app 设置页快速双击返回后 Manager 白屏。 | 解决：关联 PR #2477“避免导航后立即 pop back stack”已闭环。 | 教训：Manager/模块页面要对快速 back 做 navigation 去抖和状态保护。

## 871–918

- #2464 | 问：Samsung A54 刷 KSU 后 SD 卡不识别。 | 解决：维护者称 Google GKI 上常见，需从官方 Samsung 源码自编内核，指向 #697。 | 教训：预编译 GKI 不含厂商私有存储驱动时，模块无法补救；设备兼容需基于 OEM 源码构建。
- #2466 | 问：模块想开机关闭、60 秒后重开“默认卸载模块”，询问命令。 | 解决：NOT_PLANNED；维护者确认没有该命令，若需要须改代码并 PR。 | 教训：模块不可依赖未公开的全局开关；把功能做成明确的 Manager/API 扩展而非定时篡改状态。
- #2469 | 问：怎样限制 `su` 权限并让少数 app 访问 `/data/data`。 | 解决：社区发现需相应 capability 和 `su -M`；维护者说明涉及 Android scoped storage，文档 PR 受欢迎。 | 教训：模块需显式选择 capability 和 mount namespace；最小权限不等于所有应用数据天然可见。
- #2478 | 问：运行模块 action 时按返回，偶发“内核不支持/排序乱”。 | 解决：维护者归因为模块安装占用全局 root shell，建议升级最新 Release。 | 教训：模块 action/安装不能并发占用同一全局 shell；UI 应显示忙态并串行化任务。
- #2487 | 问：Redmi Note 12 5G（5.4 GKI 1.0）自编 KSU 后无 root。 | 解决：NOT_PLANNED，只有另一用户追问，未证实关闭。 | 教训：GKI 1.0/旧 5.4 设备要先确认官方支持线；无日志的手工 kprobe 集成不可作为模板。
- #2494 | 问：shell 权限不足使 Frida 无法读 SELinux policy。 | 解决：NOT_PLANNED；维护者指出提问者根本未执行 `su`。 | 教训：模块调试先验证有效 UID、SELinux context 和实际 `su` 会话，不要把普通 shell 权限当 root。
- #2500 | 问：同为 GKI 的镜像为何 OnePlus Pad Pro 都刷不进。 | 解决：报告者称该类后续 OnePlus 设备需先格式化再刷；无维护者普遍确认。 | 教训：刷机步骤必须按设备/OEM 说明核验数据清除与 AVB 风险，单机经验不可泛化。
- #2501 | 问：OnePlus Ace 5 修补 LKM `init_boot` 后无限重启。 | 解决：报告者称在 recovery 删除 `/data/adb` 后恢复，非官方方案且会清 KSU 状态。 | 教训：模块开发/测试前必须备份 `/data/adb` 和原始分区；救援删除要精确限定目录并告知数据损失。
- #2508 | 问：模块 WebView 不显示 JS alert/confirm/prompt。 | 解决：维护者让提 PR，另一评论称不重写也能弹窗；无合并或验证，未证实关闭。 | 教训：WebUI 需以真实 WebView 行为和最小 demo 验证，不要凭假设重写安全敏感弹窗逻辑。
- #2510 | 问：请求可选挂载、跳过 sparse image/tmpfs 的“lite mode”以减少检测。 | 解决：维护者称欢迎 PR，但模块目录复制/更新先要处理，并指向 #2929；未实现证明。 | 教训：可选挂载设计必须覆盖安装、更新、回滚和模块功能，不能把规避检测作为唯一验收。
- #2520 | 问：重启后 KSU 模块列表自动消失。 | 解决：维护者从日志判断 KSU loop 被全局 namespace 进程卸载，要求 debug 日志；一次 OTA 后不可复现但后续仍有报告，未证实最终修复。 | 教训：模块可见性要检查 loop/namespace；“默认卸载模块”开关与系统更新都应纳入重启回归。
- #2522 | 问：`faccessat` kprobe 触发 BRK、随机卡死/重启。 | 解决：维护者一度称最新 CI 已修，但随后认为现象不同，未有最终确认。 | 教训：hook 崩溃要保留完整 kprobe/Oops 日志并只在验证过的 CI 版本上回归，不能据关闭状态宣称稳定。
- #2523 | 问：`su --shell` 忽略指定 shell。 | 解决：关联 PR #2529 “Fix su --shell argument handling”已闭环。 | 教训：模块调用 `su` 应显式测试参数解析和不同 shell，避免假设 `/system/bin/sh` 总是目标解释器。
- #2528 | 问：设备仅有一个用户 app 时内核找不到 Manager。 | 解决：关联 PR #2530 修复目录遍历 off-by-one。 | 教训：模块/Manager 探测代码需测试空、单项、多项目录；链表遍历的边界条件是安装检测的一部分。
- #2533 | 问：提交 Redmi Note 10 5G 设备支持。 | 解决：Actions 无法创建 PR，未证实收录。 | 教训：设备源码/分支/模板字段需完整，失败的自动化不能转化为兼容承诺。
- #2537 | 问：安装/更新模块重启后，解锁立刻重启循环。 | 解决：调查怀疑 loop 与 `apexd`，移除修改 `/apex` 的模块后暂不复现，但根因未定。 | 教训：模块不要覆盖 `/apex`；出现解锁后崩溃应收集 `apexd` tombstone 并逐个禁用模块。
- #2539 | 问：怎样获得匹配的 AVD。 | 解决：NOT_PLANNED；维护者说明只替换 kernel 不够，还需匹配 kernel modules，AVD 内核构建已取消。 | 教训：虚拟设备集成必须把 kernel 与模块包作为不可分割的一组，不能单刷内核。
- #2541 | 问：Samsung S23 root 后耳机音频异常。 | 解决：NOT_PLANNED/重复 #756；维护者要求从 Samsung 官方源码加入 KSU 后自编。 | 教训：厂商音频驱动问题不是模块可修补层；对私有硬件必须构建设备原生内核。
- #2544 | 问：仅贴一个 Magisk 模块的 track URL。 | 解决：NOT_PLANNED 且零评论，未证实关闭。 | 教训：模块仓库元数据 URL 需配合具体问题/协议说明，不能作为无上下文 issue。
- #2545 | 问：Android 16 Private Space 用过 KSU 后主空间显示未安装，重启恢复。 | 解决：NOT_PLANNED；仅有“Expected behavior”评论，无修复。 | 教训：多用户/Private Space 必须作为安装检测和授权隔离的独立场景，不能只测主用户。
- #2552 | 问：`setup.sh` 自动选到未发布 1.0.4 且编译失败。 | 解决：COMPLETED 但零评论/PR，未证实关闭。 | 教训：构建脚本应只解析公开、可复现的 tag/release，并在选择版本前输出来源与 commit。
- #2553 | 问：请求开源 `ksuinit`。 | 解决：NOT_PLANNED 状态下，最终评论称其已公开并给出 `Kernel-SU/ksuinit` 链接。 | 教训：LKM 启动链依赖应有可审计源码与明确版本，不依赖不透明闭源组件。
- #2556 | 问：大模块更新时下载通知瞬间消失，小模块正常。 | 解决：评论称最新 Actions 已解决，但无关联 PR/提问者回归，属社区确认。 | 教训：模块更新器要对大文件做下载完整性、进度、存储和失败重试测试，不能只验证小 ZIP。
- #2557 | 问：提交 OnePlus 7/7 Pro/7T 设备支持。 | 解决：Actions 无法建 PR，提示手工 PR，未证实收录。 | 教训：自动化拒绝后应跟踪手工 PR 的实际合并，不把 issue 状态写入兼容表。
- #2563 | 问：非 GKI 旧内核的 namespace/include 兼容编译错误。 | 解决：COMPLETED 但零评论/PR，未证实关闭。 | 教训：旧内核条件编译要有版本化补丁和 CI，不能把题干 workaround 当上游兼容保证。
- #2569 | 问：提交 OnePlus 7 Android 15 设备支持。 | 解决：Actions 无法创建 PR，未证实收录。 | 教训：设备添加需提供满足机器人规则的仓库与字段，并验证 PR 合并后再发布。
- #2578 | 问：Samsung S22U 刷官方 GKI 后距离传感器/旋转照片异常。 | 解决：评论建议 Samsung GKI 源码，维护者指向 #2291/#263；无成功构建回执。 | 教训：传感器等 OEM 私有驱动必须在设备源码内回归，通用 GKI 镜像不能保证硬件完整性。
- #2582 | 问：Redmi Note 11T Pro HyperOS 2.0.5 修补 boot 后卡 Redmi。 | 解决：维护者仅建议试 v2.0，无成功确认。 | 教训：新系统 bootloop 的升级建议必须经同设备验证；模块用户应先保留原始 boot 和 fastboot 回退。
- #2583 | 问：Samsung F14 5G 刷 KSU 后 SD 卡不识别。 | 解决：NOT_PLANNED 且零评论，未证实关闭。 | 教训：存储驱动回归应记录设备内核来源与分区配置，不能将无日志问题列为模块兼容。
- #2599 | 问：OnePlus Nord N200 5.4.289 集成 KSU 出 SELinux 编译错误。 | 解决：维护者说明非 GKI/GKI 1.0 已停止，使用 v0.9.5；提问者确认该 tag 可用。 | 教训：legacy 内核必须固定到最后受支持 tag，并避免把新主线 SELinux 代码硬移植过去。
- #2604 | 问：模块 `REMOVE` 无法删除 `/my_product`、`/my_stock` 内容。 | 解决：COMPLETED 但零评论/PR，未证实关闭。 | 教训：`REMOVE` 语义/目录支持必须先按当前代码和实机验证；不要假设任意逻辑分区路径都可删除。
- #2605 | 问：高通内核镜像应选 gz 还是未压缩。 | 解决：NOT_PLANNED；维护者说明由 OEM 决定，与 CPU 类型无关。 | 教训：修补/刷机工具应检测原始镜像格式，不按 SoC 品牌硬编码压缩方式。
- #2615 | 问：`adb shell; su` 可用但 `adb root` 不可用。 | 解决：NOT_PLANNED；维护者说明二者不同，建议专用 `tiann/adb_root` 模块。 | 教训：模块不能把 `su` 当作 adbd root；需要 ADB root 时使用专用、显式启用的方案。
- #2618 | 问：仅提交 `faccessat` kprobe BRK/Oops 日志。 | 解决：NOT_PLANNED 且零评论，未证实关闭。 | 教训：内核崩溃报告要附设备、KSU/内核版本和可复现条件；原始栈不能替代诊断上下文。
- #2621 | 问：OnePlus 13 日常使用随机重启，怀疑 Audio Opt/获 root app。 | 解决：维护者先建议禁 Audio Opt，随后明确 `7dd8818` 已修复。 | 教训：模块回归应覆盖高频 root app 与音频优化；随机重启要先二分模块和 root 授权，再升级到含修复的提交。
- #2637 | 问：`apply_kernelsu_rules()` 在 RCU read lock 内调用 `cond_resched()`，触发原子睡眠告警。 | 解决：关联 PR #2646 修正非法 RCU lock 使用。 | 教训：模块/内核规则应用不可在 RCU 临界区调用可睡眠函数；开启 `DEBUG_ATOMIC_SLEEP` 做回归。
- #2647 | 问：在 ADB/终端直接运行 `su` 失败，`exec su`/`sh -c su` 才可用。 | 解决：维护者建议试 v2.0，报告者确认 v2.0.0 同时修复 LKM/GKI。 | 教训：模块的 `su` 兼容性测试要覆盖直接 exec、wrapper 和多种 hook 模式。
- #2649 | 问：请求 OnePlus 11 CPH2449 固件支持。 | 解决：NOT_PLANNED 且零评论，未证实关闭。 | 教训：设备请求需给出可复现内核来源、KMI 和维护者，而非只列固件版本。
- #2652 | 问：无法加入 KernelSU Telegram 讨论组，bot/prejoin 均不可用。 | 解决：另一用户同样复现，没有管理员解决，未证实关闭。 | 教训：模块文档应提供不依赖单一聊天机器人渠道的 issue/文档支持路径。
- #2660 | 问：刷文件时出错（题干缺细节）。 | 解决：NOT_PLANNED；评论指出刷的不是 Magisk module ZIP，且仓库不是 KernelSU-Next。 | 教训：安装器文档必须标清 ZIP 格式、目标项目和模式，错误报告要带完整输出。
- #2661 | 问：小米 14 Pro 重启后模块消失，关默认卸载模块才回来。 | 解决：维护者认为模块镜像损坏并指向 #2929；删除/重建镜像未恢复，未证实最终解决。 | 教训：模块状态要有可恢复的元数据/备份；镜像损坏应有检测、重建和数据迁移策略。
- #2667 | 问：题干仅为“1”。 | 解决：COMPLETED 但零内容、零评论，未证实关闭。 | 教训：无问题定义不能产生模块知识；有效 bug 模板必须强制环境和复现信息。
- #2668 | 问：题干仅为“1”。 | 解决：COMPLETED 但零内容、零评论，未证实关闭。 | 教训：无问题定义不能产生模块知识；有效 bug 模板必须强制环境和复现信息。
- #2669 | 问：题干仅为“1”。 | 解决：COMPLETED 但零内容、零评论，未证实关闭。 | 教训：无问题定义不能产生模块知识；有效 bug 模板必须强制环境和复现信息。
- #2670 | 问：题干仅为“1”。 | 解决：COMPLETED 但零内容、零评论，未证实关闭。 | 教训：无问题定义不能产生模块知识；有效 bug 模板必须强制环境和复现信息。
- #2671 | 问：OverlayFS/Magic Mount 模块挂载 APK 后服务启动报“无此文件”，发现目标 APK 为 f2fs 而系统 APK 为 erofs。 | 解决：COMPLETED 但零评论/PR，未证实关闭。 | 教训：模块替换 APK 必须验证挂载文件系统、SELinux 标签、路径大小写和 PackageManager 可见性。
- #2674 | 问：root shell 下 Frida-server 被 SELinux 拒绝，app 卡死/软重启。 | 解决：维护者建议按官方模块文档自行添加 SELinux 规则。 | 教训：模块必须把最小 `sepolicy.rule` 与 audit 验证一并提供，避免用 permissive 作为发布方案。
- #2676 | 问：KernelSU 群机器人验证无响应。 | 解决：NOT_PLANNED 且零评论，未证实关闭。 | 教训：社区运维问题不应混入模块功能判断；提供多个可验证的支持入口。
